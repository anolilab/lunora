# `@lunora/vite` — public API

- Package: `packages/vite`
- Tier: core

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/vite`

### `CLASS_A_WIRING` (const)

```ts
const CLASS_A_WIRING: Readonly<Partial<Record<DetectedFramework, ClassAWiring>>>;
```

### `ClassAWiring` (interface)

```ts
interface ClassAWiring {
    handler: string;
    imports: string;
}
```

### `CloudflarePluginOptions` (type)

```ts
type CloudflarePluginOptions = Record<string, unknown>;
```

### `DEV_WORKER_ENV_VALUE` (const)

```ts
const DEV_WORKER_ENV_VALUE = "development";
```

### `DEV_WORKER_ENV_VAR` (const)

```ts
const DEV_WORKER_ENV_VAR = "WORKER_ENV";
```

### `DetectedFramework` (type)

Re-exported from `@lunora/config` — signature tracked at its source.

### `FrameworkClass` (type)

Re-exported from `@lunora/config` — signature tracked at its source.

### `FrameworkDetection` (interface)

Re-exported from `@lunora/config` — signature tracked at its source.

### `GENERATED_CLASS_MODULES` (const)

```ts
const GENERATED_CLASS_MODULES: readonly [
    "agents",
    "containers",
    "workflows"
];
```

### `GeneratedClassModule` (type)

```ts
type GeneratedClassModule = (typeof GENERATED_CLASS_MODULES)[number];
```

### `LUNORA_API_UPDATED_EVENT` (const)

```ts
const LUNORA_API_UPDATED_EVENT = "lunora:api-updated";
```

### `LUNORA_WORKER_VIRTUAL_ID` (const)

```ts
const LUNORA_WORKER_VIRTUAL_ID: string;
```

### `LunoraPluginOptions` (interface)

```ts
interface LunoraPluginOptions {
    allowUnauthenticatedShardAccess?: boolean;
    apiSpec?: CodegenOptions["apiSpec"];
    cloudflare?: boolean | CloudflarePluginOptions;
    overlay?: boolean | OverlayPluginOptions;
    projectRoot?: string;
    schemaDir?: string;
    shard?: LunoraShardConfig;
    studio?: boolean;
    target?: string;
    validateWrangler?: boolean;
}
```

### `LunoraPlugins` (type)

```ts
type LunoraPlugins = Plugin[];
```

### `LunoraShardConfig` (interface)

```ts
interface LunoraShardConfig {
    cdc?: boolean;
    maxRelationKeys?: number;
    reactiveCache?: boolean | {
        maxBytes?: number;
        maxEntries?: number;
    };
    relationExistsPushDown?: "always" | "auto" | "never";
}
```

### `OverlayPluginOptions` (type)

```ts
type OverlayPluginOptions = NonNullable<Parameters<typeof errorOverlayPlugin>[0]>;
```

### `PlanViteRemoteOptions` (interface)

```ts
interface PlanViteRemoteOptions {
    materialize?: typeof materializeRemoteWranglerConfig;
    projectRoot: string;
    readPreference?: typeof readProjectRemotePreference;
    remoteEnv?: string;
}
```

### `ReconcileResult` (interface)

Re-exported from `@lunora/config` — signature tracked at its source.

### `ResolvedLunoraPluginOptions` (interface)

```ts
interface ResolvedLunoraPluginOptions {
    allowUnauthenticatedShardAccess: boolean;
    apiSpec: NonNullable<CodegenOptions["apiSpec"]>;
    cloudflare: false | CloudflarePluginOptions;
    generatedDir: string;
    overlay: false | OverlayPluginOptions;
    projectRoot: string;
    schemaDir: string;
    shard: LunoraShardConfig;
    studio: boolean;
    target: string;
    validateWrangler: boolean;
}
```

### `STUDIO_PATH` (const)

```ts
const STUDIO_PATH = "/__lunora";
```

### `Solution` (type)

```ts
type Solution = NonNullable<Awaited<ReturnType<SolutionFinder["handle"]>>>;
```

### `SolutionFinder` (type)

```ts
type SolutionFinder = NonNullable<OverlayPluginOptions["solutionFinders"]>[number];
```

### `VERSION` (const)

```ts
const VERSION: string;
```

### `ViteRemotePlan` (interface)

```ts
interface ViteRemotePlan {
    cleanup: () => void;
    configPath?: string;
    enabled: boolean;
    reason?: string;
}
```

### `WORKER_STARTUP_HINT` (const)

```ts
const WORKER_STARTUP_HINT: string;
```

### `augmentWorkerStartupError` (const)

```ts
const augmentWorkerStartupError: (error: unknown) => unknown;
```

### `buildStudioUrl` (const)

```ts
const buildStudioUrl: (input: {
    address?: AddressInfo | string;
    base?: string;
    resolvedLocal?: string;
}) => string;
```

### `buildWorkerEntrySource` (const)

```ts
const buildWorkerEntrySource: (framework: DetectedFramework, generatedImportBase: string, classModules?: ReadonlyArray<GeneratedClassModule>, useUmbrella?: boolean, allowUnauthenticatedShardAccess?: boolean, shard?: LunoraShardConfig) => string;
```

### `checkLunoraProxy` (const)

```ts
const checkLunoraProxy: (proxy: Record<string, unknown> | undefined, label: string) => string[];
```

### `codegenPlugin` (const)

```ts
const codegenPlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```

### `containerLogsPlugin` (const)

```ts
const containerLogsPlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```

### `detectFramework` (const)

Re-exported from `@lunora/config` — signature tracked at its source.

### `devStatePlugin` (const)

```ts
const devStatePlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```

### `devVariablesPlugin` (const)

```ts
const devVariablesPlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```

### `frameworkComposePlugin` (const)

```ts
const frameworkComposePlugin: (options: ResolvedLunoraPluginOptions, context: LunoraPluginContext) => Plugin;
```

### `isAutoComposable` (const)

```ts
const isAutoComposable: (context: LunoraPluginContext) => boolean;
```

### `isWorkerEntryEvalError` (const)

```ts
const isWorkerEntryEvalError: (error: unknown) => boolean;
```

### `logStreamPlugin` (const)

```ts
const logStreamPlugin: () => Plugin;
```

### `lunora` (const)

```ts
const lunora: (options?: LunoraPluginOptions) => LunoraPlugins;
```

### `lunoraSolutionFinder` (const)

```ts
const lunoraSolutionFinder: SolutionFinder;
```

### `lunoraSolutionFinders` (const)

```ts
const lunoraSolutionFinders: ReadonlyArray<SolutionFinder>;
```

### `planViteRemoteBindings` (const)

```ts
const planViteRemoteBindings: (options: PlanViteRemoteOptions) => ViteRemotePlan;
```

### `proxyCheckPlugin` (const)

```ts
const proxyCheckPlugin: () => Plugin;
```

### `reconcileWranglerCrons` (const)

Re-exported from `@lunora/config` — signature tracked at its source.

### `remoteBindingsCleanupPlugin` (const)

```ts
const remoteBindingsCleanupPlugin: (cleanup: () => void) => Plugin;
```

### `remoteBindingsConfigPlugin` (const)

```ts
const remoteBindingsConfigPlugin: (options: CloudflarePluginOptions | undefined, plan: ViteRemotePlan) => Plugin;
```

### `resolveOverlayOption` (const)

```ts
const resolveOverlayOption: (overlay: LunoraPluginOptions["overlay"]) => false | OverlayPluginOptions;
```

### `studioPlugin` (const)

```ts
const studioPlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```

### `withRemoteBindings` (const)

```ts
const withRemoteBindings: (options: CloudflarePluginOptions, plan: ViteRemotePlan) => CloudflarePluginOptions;
```

### `withWorkerStartupHint` (const)

```ts
const withWorkerStartupHint: (plugins: ReadonlyArray<Plugin>) => Plugin[];
```

### `wranglerValidatorPlugin` (const)

```ts
const wranglerValidatorPlugin: (options: ResolvedLunoraPluginOptions) => Plugin;
```
