# `@lunora/astro` — public API

- Package: `packages/astro`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/astro`

### `AstroIntegrationLike` (interface)

```ts
interface AstroIntegrationLike {
    readonly hooks: {
        readonly [hook: string]: ((...arguments_: never[]) => unknown) | undefined;
    };
    readonly name: string;
}
```

### `AstroWorkerHandler` (type)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `ComposedWorker` (interface)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `LunoraIntegrationOptions` (interface)

```ts
interface LunoraIntegrationOptions {
    readonly serverEntry?: string;
}
```

### `LunoraOptions` (type)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `lunora` (const)

```ts
const lunora: (options?: LunoraIntegrationOptions) => AstroIntegrationLike;
```

### `withLunora` (const)

Re-exported from `@lunora/runtime` — signature tracked at its source.

## `@lunora/astro/server`

### `ArgsOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `AuthLike` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `FunctionReference` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `HeadersSource` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `Preloaded` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ReturnOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ServerClientOptions` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ServerSession` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `createServerClient` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `deserializePreloaded` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `getServerSession` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `preloadQuery` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `preloadedQueryResult` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `serializePreloaded` (const)

Re-exported from `@lunora/client` — signature tracked at its source.
