# `@lunora/mail` — public API

- Package: `packages/mail`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/mail`

### `CapturedMail` (interface)

```ts
interface CapturedMail extends SendPayload {
    capturedAt: number;
    id: string;
}
```

### `CloudflareSend` (type)

```ts
type CloudflareSend = (from: string, to: string, raw: string) => Promise<void>;
```

### `CloudflareTransportOptions` (interface)

```ts
interface CloudflareTransportOptions {
    from: string;
    send: CloudflareSend;
}
```

### `FromEnvOptions` (interface)

```ts
interface FromEnvOptions {
    cloudflareSend?: CloudflareSend;
    jurisdiction?: DurableObjectJurisdiction;
    rootShard?: string;
}
```

### `LunoraMailOptions` (interface)

```ts
interface LunoraMailOptions {
    apiKey?: string;
    cloudflareSend?: (from: string, to: string, raw: string) => Promise<void>;
    from: string;
    queue?: QueueLike;
    transport?: MailTransport;
}
```

### `MailEnv` (type)

```ts
type MailEnv = Record<string, unknown>;
```

### `MailTransport` (interface)

```ts
interface MailTransport {
    send: (payload: SendPayload) => Promise<{
        id: string;
    }>;
}
```

### `MailboxSink` (interface)

```ts
interface MailboxSink {
    record: (mail: SendPayload) => Promise<{
        id: string;
    }>;
}
```

### `Mailer` (interface)

```ts
interface Mailer {
    queue: (options: SendOptions) => Promise<{
        queued: true;
    }>;
    send: (options: SendOptions) => Promise<{
        id: string;
    }>;
}
```

### `QueueLike` (interface)

```ts
interface QueueLike {
    send: (payload: unknown, options?: Record<string, unknown>) => Promise<void>;
}
```

### `QueuedSend` (interface)

```ts
interface QueuedSend {
    bcc?: string[];
    cc?: string[];
    from?: string;
    headers?: Record<string, string>;
    html?: string;
    idempotencyKey?: string;
    replyTo?: string;
    subject: string;
    text?: string;
    to: string | string[];
}
```

### `SendOptions` (interface)

```ts
interface SendOptions {
    bcc?: string[];
    cc?: string[];
    from?: string;
    headers?: Record<string, string>;
    html?: string;
    idempotencyKey?: string;
    react?: ReactElement;
    replyTo?: string;
    subject: string;
    text?: string;
    to: string | string[];
}
```

### `SendPayload` (interface)

```ts
interface SendPayload {
    bcc?: string[];
    cc?: string[];
    from?: string;
    headers?: Record<string, string>;
    html?: string;
    replyTo?: string;
    subject: string;
    text?: string;
    to: string | string[];
}
```

### `consumeQueuedSend` (const)

```ts
const consumeQueuedSend: (mailer: Mailer, payload: unknown) => Promise<{
    id: string;
}>;
```

### `createCaptureSink` (const)

```ts
const createCaptureSink: (env: MailEnv, rootShard?: string, jurisdiction?: DurableObjectJurisdiction) => MailboxSink;
```

### `createCaptureTransport` (const)

```ts
const createCaptureTransport: (sink: MailboxSink) => MailTransport;
```

### `createCloudflareTransport` (const)

```ts
const createCloudflareTransport: (options: CloudflareTransportOptions) => MailTransport;
```

### `createMailer` (const)

```ts
const createMailer: (options: LunoraMailOptions) => Mailer;
```

### `createMailerFromEnv` (const)

```ts
const createMailerFromEnv: (env: MailEnv, options?: FromEnvOptions) => Mailer;
```

### `createResendTransport` (const)

```ts
const createResendTransport: (apiKey: string, defaultFrom: string) => MailTransport;
```

### `renderEmail` (const)

```ts
const renderEmail: (element: ReactElement) => Promise<{
    html: string;
    text: string;
}>;
```

### `shouldCaptureMail` (const)

```ts
const shouldCaptureMail: (env: MailEnv) => boolean;
```

### `toQueuedPayload` (const)

```ts
const toQueuedPayload: (options: SendOptions) => QueuedSend;
```

## `@lunora/mail/inbound`

### `DispatchToLunoraFunctionOptions` (interface)

```ts
interface DispatchToLunoraFunctionOptions<TEnv = Record<string, unknown>> {
    adminToken?: string;
    functionPath: string;
    jurisdiction?: DurableObjectJurisdiction;
    resolveArgs?: (email: InboundEmail, context: InboundDispatchContext<TEnv>) => unknown;
    shard: ShardNamespaceLike;
    shardKey?: string;
}
```

### `ForwardableEmailMessageLike` (interface)

```ts
interface ForwardableEmailMessageLike {
    forward: (rcptTo: string, headers?: Headers) => Promise<unknown>;
    readonly from: string;
    readonly headers: Headers;
    readonly raw: ReadableStream<Uint8Array>;
    reply: (message: {
        from: string;
        raw: string;
        to: string;
    }) => Promise<unknown>;
    setReject: (reason: string) => void;
    readonly to: string;
}
```

### `InboundAttachment` (interface)

```ts
interface InboundAttachment {
    content: ArrayBuffer | string | Uint8Array;
    disposition: "attachment" | "inline" | null;
    encoding?: "base64" | "utf8";
    filename: string | null;
    mimeType: string;
}
```

### `InboundAuthResult` (interface)

```ts
interface InboundAuthResult {
    domain: string | null;
    result: string;
}
```

### `InboundAuthentication` (interface)

```ts
interface InboundAuthentication {
    dkim: InboundAuthResult[];
    dmarc: InboundAuthResult[];
    spf: InboundAuthResult[];
}
```

### `InboundDispatch` (type)

```ts
type InboundDispatch<TEnv = Record<string, unknown>> = (email: InboundEmail, context: InboundDispatchContext<TEnv>) => Promise<void>;
```

### `InboundDispatchContext` (interface)

```ts
interface InboundDispatchContext<TEnv = Record<string, unknown>> {
    ctx: unknown;
    env: TEnv;
    message: ForwardableEmailMessageLike;
}
```

### `InboundEmail` (interface)

```ts
interface InboundEmail {
    attachments: InboundAttachment[];
    authentication: InboundAuthentication;
    from: string;
    headers: Record<string, string>;
    html?: string;
    inReplyTo?: string;
    messageId?: string;
    references?: string;
    subject?: string;
    text?: string;
    to: string[];
}
```

### `InboundEmailHandler` (type)

```ts
type InboundEmailHandler<TEnv = Record<string, unknown>> = (message: ForwardableEmailMessageLike, env: TEnv, context: unknown) => Promise<void>;
```

### `InboundEmailHandlerOptions` (interface)

```ts
interface InboundEmailHandlerOptions<TEnv = Record<string, unknown>> {
    dispatch: InboundDispatch<TEnv>;
    onError?: (error: unknown, context: InboundDispatchContext<TEnv>) => Promise<void> | void;
    parse: (raw: RawInboundEmail) => Promise<InboundEmail>;
    retain?: InboundRetain<TEnv>;
    verify?: InboundVerify<TEnv>;
}
```

### `InboundVerify` (type)

```ts
type InboundVerify<TEnv = Record<string, unknown>> = (email: InboundEmail, context: InboundDispatchContext<TEnv>) => Promise<boolean | void> | boolean | void;
```

### `RawInboundEmail` (type)

```ts
type RawInboundEmail = ArrayBuffer | ReadableStream<Uint8Array> | string | Uint8Array;
```

### `RpcEnvelope` (interface)

```ts
interface RpcEnvelope {
    args: unknown;
    functionPath: string;
    shardKey?: string;
}
```

### `ShardNamespaceLike` (interface)

```ts
interface ShardNamespaceLike {
    get: (id: unknown) => ShardStubLike;
    idFromName: (name: string) => unknown;
    jurisdiction?: (jurisdiction: DurableObjectJurisdiction) => ShardNamespaceLike;
}
```

### `ShardStubLike` (interface)

```ts
interface ShardStubLike {
    fetch: (input: string, init?: {
        body?: string;
        headers?: Record<string, string>;
        method?: string;
    }) => Promise<{
        json: () => Promise<unknown>;
        ok?: boolean;
        status?: number;
    }>;
}
```

### `createInboundEmailHandler` (const)

```ts
const createInboundEmailHandler: <TEnv = Record<string, unknown>>(options: InboundEmailHandlerOptions<TEnv>) => InboundEmailHandler<TEnv>;
```

### `dispatchToLunoraFunction` (const)

```ts
const dispatchToLunoraFunction: <TEnv extends Record<string, unknown> = Record<string, unknown>>(options: DispatchToLunoraFunctionOptions<TEnv>) => InboundDispatch<TEnv>;
```

### `parseInboundEmail` (const)

```ts
const parseInboundEmail: (raw: RawInboundEmail) => Promise<InboundEmail>;
```

## `@lunora/mail/testing`

### `InboxOptions` (interface)

```ts
interface InboxOptions {
    adminToken: string;
    baseUrl: string;
    fetch?: FetchLike;
    limit?: number;
}
```

### `WaitForMailOptions` (interface)

```ts
interface WaitForMailOptions extends InboxOptions {
    pollMs?: number;
    subjectMatch?: string;
    timeoutMs?: number;
    to: string;
}
```

### `extractLink` (const)

```ts
const extractLink: (mail: CapturedMail, options?: {
    match?: string;
}) => string;
```

### `listCapturedMail` (const)

```ts
const listCapturedMail: (options: InboxOptions) => Promise<CapturedMail[]>;
```

### `waitForMail` (const)

```ts
const waitForMail: (options: WaitForMailOptions) => Promise<CapturedMail>;
```
