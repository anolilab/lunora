# `@lunora/bindings` — public API

- Package: `packages/bindings`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/bindings/analytics`

### `AnalyticsClient` (interface)

```ts
interface AnalyticsClient {
    track: (name: string, event?: TrackEvent) => TrackSchema;
    writeDataPoint: (event: AnalyticsEngineDataPoint) => void;
}
```

### `AnalyticsEngineDataPoint` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `AnalyticsEngineDatasetLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `AnalyticsSqlClient` (interface)

```ts
interface AnalyticsSqlClient {
    query: (sql: string) => Promise<AnalyticsSqlResult>;
}
```

### `AnalyticsSqlColumnMeta` (interface)

```ts
interface AnalyticsSqlColumnMeta {
    name: string;
    type: string;
}
```

### `AnalyticsSqlConfig` (interface)

```ts
interface AnalyticsSqlConfig {
    accountId: string;
    apiToken: string;
    fetch?: typeof globalThis.fetch;
    timeoutMs?: number;
}
```

### `AnalyticsSqlError` (class)

```ts
class AnalyticsSqlError extends LunoraError {
    constructor(status: number, body: string);
}
```

### `AnalyticsSqlResult` (interface)

```ts
interface AnalyticsSqlResult {
    columns: AnalyticsSqlColumnMeta[];
    rowCount: number;
    rows: Record<string, unknown>[];
}
```

### `TrackColumn` (interface)

```ts
interface TrackColumn {
    column: string;
    field: string;
}
```

### `TrackEvent` (interface)

```ts
interface TrackEvent {
    dimensions?: Record<string, string>;
    index?: string;
    metrics?: Record<string, number>;
}
```

### `TrackSchema` (interface)

```ts
interface TrackSchema {
    dimensions: TrackColumn[];
    index: TrackColumn | null;
    metrics: TrackColumn[];
    name: string;
}
```

### `createAnalytics` (const)

```ts
const createAnalytics: (binding: AnalyticsEngineDatasetLike) => AnalyticsClient;
```

### `createAnalyticsSqlClient` (const)

```ts
const createAnalyticsSqlClient: (config: AnalyticsSqlConfig) => AnalyticsSqlClient;
```

## `@lunora/bindings/images`

### `ImageCompositeMode` (type)

```ts
type ImageCompositeMode = "atop" | "in" | "lighter" | "out" | "over" | "xor";
```

### `ImageDeliveryUrlOptions` (interface)

```ts
interface ImageDeliveryUrlOptions {
    baseUrl: string;
    imageId?: string;
    key?: string;
    transform?: TransformOptions;
    variant?: string;
}
```

### `ImageDrawOptions` (interface)

```ts
interface ImageDrawOptions {
    bottom?: number;
    composite?: ImageCompositeMode;
    left?: number;
    opacity?: number;
    repeat?: "x" | "y" | boolean;
    right?: number;
    top?: number;
}
```

### `ImageInfoLike` (type)

```ts
type ImageInfoLike = {
    fileSize: number;
    format: string;
    height: number;
    width: number;
} | {
    format: string;
};
```

### `ImageInput` (type)

```ts
type ImageInput = ArrayBuffer | Blob | R2ObjectBodyLike | ReadableStream | Uint8Array;
```

### `ImageOutputFormat` (type)

```ts
type ImageOutputFormat = "image/avif" | "image/gif" | "image/jpeg" | "image/png" | "image/webp";
```

### `ImageOverlay` (interface)

```ts
interface ImageOverlay extends ImageDrawOptions {
    image: ImageInput;
    transform?: TransformOptions;
}
```

### `ImageTransformationResultLike` (interface)

```ts
interface ImageTransformationResultLike {
    contentType: () => string;
    image: () => ReadableStream<Uint8Array>;
    response: () => Response;
}
```

### `ImageTransformerLike` (interface)

```ts
interface ImageTransformerLike {
    draw: (image: ImageTransformerLike | ReadableStream<Uint8Array>, options?: ImageDrawOptions) => ImageTransformerLike;
    output: (options: OutputOptions) => Promise<ImageTransformationResultLike>;
    transform: (transform: TransformOptions) => ImageTransformerLike;
}
```

### `Images` (interface)

```ts
interface Images {
    info: (input: ImageInput) => Promise<ImageInfoLike>;
    transform: (input: ImageInput, transform?: TransformOptions, output?: OutputOptions, overlays?: ImageOverlay[]) => Promise<ImageTransformationResultLike>;
}
```

### `ImagesBindingLike` (interface)

```ts
interface ImagesBindingLike {
    info: (stream: ReadableStream<Uint8Array>) => Promise<ImageInfoLike>;
    input: (stream: ReadableStream<Uint8Array>) => ImageTransformerLike;
}
```

### `LunoraImagesOptions` (interface)

```ts
interface LunoraImagesOptions {
    binding: ImagesBindingLike;
    maxDimension?: number;
}
```

### `OutputOptions` (interface)

```ts
interface OutputOptions {
    anim?: boolean;
    background?: string;
    format?: ImageOutputFormat;
    quality?: number;
}
```

### `R2ObjectBodyLike` (interface)

```ts
interface R2ObjectBodyLike {
    body: ReadableStream | null;
}
```

### `SignedImageUrlOptions` (interface)

```ts
interface SignedImageUrlOptions {
    baseUrl: string;
    expiresInSeconds?: number;
    key: string;
    secret: string;
    transform?: TransformOptions;
}
```

### `TransformOptions` (interface)

```ts
interface TransformOptions {
    background?: string;
    blur?: number;
    brightness?: number;
    contrast?: number;
    fit?: "aspect-crop" | "contain" | "cover" | "crop" | "pad" | "scale-down" | "scale-up" | "squeeze";
    flip?: "h" | "hv" | "v";
    gamma?: number;
    gravity?: "auto" | "bottom" | "center" | "entropy" | "face" | "left" | "right" | "top" | {
        mode: "box-center" | "remainder";
        x?: number;
        y?: number;
    };
    height?: number;
    rotate?: 0 | 90 | 180 | 270;
    saturation?: number;
    segment?: "foreground";
    sharpen?: number;
    upscale?: "generate" | "interpolate";
    width?: number;
}
```

### `VerifyImageResult` (interface)

```ts
interface VerifyImageResult {
    key?: string;
    reason?: "bad_signature" | "expired" | "malformed";
    transform?: string;
    transformOptions?: TransformOptions;
    valid: boolean;
}
```

### `buildImageDeliveryUrl` (const)

```ts
const buildImageDeliveryUrl: (options: ImageDeliveryUrlOptions) => string;
```

### `buildSignedImageUrl` (const)

```ts
const buildSignedImageUrl: (options: SignedImageUrlOptions) => Promise<string>;
```

### `createImages` (const)

```ts
const createImages: (options: LunoraImagesOptions) => Images;
```

### `parseSignedTransform` (const)

```ts
const parseSignedTransform: (t: string) => TransformOptions;
```

### `verifySignedImageUrl` (const)

```ts
const verifySignedImageUrl: (input: string | URL, secret: string, options?: {
    expectedHost?: string;
}) => Promise<VerifyImageResult>;
```

## `@lunora/bindings/kv`

### `CreateKvIntrospectorOptions` (interface)

```ts
interface CreateKvIntrospectorOptions {
    namespaces: Record<string, KVNamespaceLike>;
}
```

### `KVNamespaceLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `Kv` (interface)

```ts
interface Kv {
    delete: (key: string) => Promise<void>;
    get: <T = unknown>(key: string, options?: {
        cacheTtl?: number;
    }) => Promise<T | null>;
    getRaw: <T = string>(key: string, options?: KvGetOptions) => Promise<T | null>;
    getWithMetadata: <T = unknown, M = unknown>(key: string, options?: {
        cacheTtl?: number;
    }) => Promise<KvValueWithMetadata<T, M>>;
    list: <M = unknown>(options?: KvListOptions) => Promise<KvListResult<M>>;
    put: <T = unknown>(key: string, value: T, options?: KvPutOptions) => Promise<void>;
}
```

### `KvGetOptions` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvIntrospectorLike` (interface)

```ts
interface KvIntrospectorLike {
    deleteKey: (options: {
        key: string;
        namespace: string;
    }) => Promise<void>;
    getValue: (options: {
        key: string;
        namespace: string;
    }) => Promise<KvValueResultLike>;
    listKeys: (options: {
        cursor?: string;
        limit?: number;
        namespace: string;
        prefix?: string;
    }) => Promise<KvKeyListResultLike>;
    listNamespaces: () => Promise<KvNamespaceSummaryLike[]>;
    putValue: (options: {
        expiration?: number;
        expirationTtl?: number;
        key: string;
        metadata?: unknown;
        namespace: string;
        value: string;
    }) => Promise<void>;
}
```

### `KvListKey` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvListOptions` (interface)

```ts
interface KvListOptions {
    cursor?: string;
    limit?: number;
    prefix?: string;
}
```

### `KvListResult` (interface)

```ts
interface KvListResult<Metadata = unknown> {
    cursor?: string;
    keys: KvListKey<Metadata>[];
    listComplete: boolean;
}
```

### `KvNamespaceListResult` (type)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvNamespacePutOptions` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvPutOptions` (interface)

```ts
interface KvPutOptions {
    expiration?: number;
    expirationTtl?: number;
    metadata?: unknown;
    raw?: boolean;
}
```

### `KvValue` (type)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvValueType` (type)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `KvValueWithMetadata` (interface)

```ts
interface KvValueWithMetadata<Value, Metadata> {
    metadata: Metadata | null;
    value: Value | null;
}
```

### `LunoraKvOptions` (interface)

```ts
interface LunoraKvOptions {
    keyPrefix?: string;
    namespace: KVNamespaceLike;
}
```

### `createKv` (const)

```ts
const createKv: (options: LunoraKvOptions) => Kv;
```

### `createKvIntrospector` (const)

```ts
const createKvIntrospector: (options: CreateKvIntrospectorOptions) => KvIntrospectorLike;
```

### `createKvIntrospectorFromEnv` (const)

```ts
const createKvIntrospectorFromEnv: (env: unknown) => KvIntrospectorLike;
```

### `scopeKey` (const)

```ts
const scopeKey: (prefix: string, key: string) => string;
```

## `@lunora/bindings/pipelines`

### `PipelineBindingLike` (interface)

```ts
interface PipelineBindingLike<T extends PipelineRecord = PipelineRecord> {
    send: (records: T[]) => Promise<void>;
}
```

### `PipelineClient` (interface)

```ts
interface PipelineClient<T extends PipelineRecord = PipelineRecord> {
    send: (records: T | T[]) => Promise<void>;
}
```

### `PipelineRecord` (type)

```ts
type PipelineRecord = Record<string, unknown>;
```

### `createPipelines` (const)

```ts
const createPipelines: <T extends PipelineRecord = PipelineRecord>(options: {
    binding: PipelineBindingLike<T>;
}) => PipelineClient<T>;
```

## `@lunora/bindings/r2sql`

### `Condition` (type)

```ts
type Condition = Sql | string;
```

### `OrderTerm` (type)

```ts
type OrderTerm = Sql | string | {
    dir: "ASC" | "DESC";
    expr: Sql | string;
};
```

### `OverSpec` (interface)

```ts
interface OverSpec {
    frame?: string;
    orderBy?: OrderTerm | OrderTerm[];
    partitionBy?: Sql | string | (Sql | string)[];
}
```

### `QueryExecutor` (type)

```ts
type QueryExecutor = (statement: string) => Promise<R2SqlResult>;
```

### `Queryable` (interface)

```ts
interface Queryable<Row = Record<string, unknown>> {
    readonly needsWrapForSetOperation?: boolean;
    run: () => Promise<R2SqlResult<Row>>;
    toSQL: () => string;
}
```

### `R2SqlClient` (interface)

```ts
interface R2SqlClient {
    describe: (table: string) => Promise<R2SqlResult>;
    explain: (statement: Sql | string, options?: R2SqlExplainOptions) => Promise<R2SqlResult>;
    from: <Row = Record<string, unknown>>(table: string) => SelectBuilder<Row>;
    query: <Row = Record<string, unknown>>(statement: Sql | string) => Promise<R2SqlResult<Row>>;
    showDatabases: () => Promise<R2SqlResult>;
    showTables: (namespace: string) => Promise<R2SqlResult>;
}
```

### `R2SqlColumn` (interface)

```ts
interface R2SqlColumn {
    name: string;
    type?: string;
}
```

### `R2SqlConfig` (interface)

```ts
interface R2SqlConfig {
    accountId: string;
    apiToken: string;
    bucket: string;
    endpoint?: string;
    fetch?: typeof globalThis.fetch;
    timeoutMs?: number;
}
```

### `R2SqlError` (class)

```ts
class R2SqlError extends LunoraError {
    constructor(status: number, body: string);
}
```

### `R2SqlExplainOptions` (interface)

```ts
interface R2SqlExplainOptions {
    format?: "json" | "text";
}
```

### `R2SqlResult` (interface)

```ts
interface R2SqlResult<Row = Record<string, unknown>> {
    columns: R2SqlColumn[];
    rowCount: number;
    rows: Row[];
}
```

### `SelectBuilder` (class)

```ts
class SelectBuilder<Row = Record<string, unknown>> implements Queryable<Row> {
    constructor(exec: QueryExecutor, table: string);
    select(...items: (Sql | string)[]): this;
    distinct(): this;
    distinctOn(...columns: (Sql | string)[]): this;
    innerJoin(table: string, on: Condition): this;
    leftJoin(table: string, on: Condition): this;
    rightJoin(table: string, on: Condition): this;
    fullJoin(table: string, on: Condition): this;
    crossJoin(table: string): this;
    where(...conditions: Condition[]): this;
    groupBy(...columns: (Sql | string)[]): this;
    having(...conditions: Condition[]): this;
    qualify(condition: Condition): this;
    orderBy(...terms: OrderTerm[]): this;
    limit(n: number): this;
    returns<NextRow>(): SelectBuilder<NextRow>;
    union(other: Queryable<unknown>): SetOperation<Row>;
    unionAll(other: Queryable<unknown>): SetOperation<Row>;
    intersect(other: Queryable<unknown>): SetOperation<Row>;
    except(other: Queryable<unknown>): SetOperation<Row>;
    get needsWrapForSetOperation(): boolean;
    toSQL(): string;
    run(): Promise<R2SqlResult<Row>>;
}
```

### `SetOperation` (class)

```ts
class SetOperation<Row = Record<string, unknown>> implements Queryable<Row> {
    readonly needsWrapForSetOperation = true;
    constructor(exec: QueryExecutor, members: SetMember[]);
    union(other: Queryable<unknown>): this;
    unionAll(other: Queryable<unknown>): this;
    intersect(other: Queryable<unknown>): this;
    except(other: Queryable<unknown>): this;
    orderBy(...terms: OrderTerm[]): this;
    limit(n: number): this;
    returns<NextRow>(): SetOperation<NextRow>;
    toSQL(): string;
    run(): Promise<R2SqlResult<Row>>;
}
```

### `Sql` (class)

```ts
class Sql {
    readonly text: string;
    constructor(text: string);
    toString(): string;
}
```

### `WindowExpression` (class)

```ts
class WindowExpression extends Sql {
    as(alias: string): Sql;
    between(low: unknown, high: unknown): Sql;
    eq(value: unknown): Sql;
    gt(value: unknown): Sql;
    gte(value: unknown): Sql;
    lt(value: unknown): Sql;
    lte(value: unknown): Sql;
}
```

### `WindowFunction` (class)

```ts
class WindowFunction {
    constructor(callText: string);
    over(spec?: OverSpec): WindowExpression;
}
```

### `asc` (const)

```ts
const asc: (expr: Sql | string) => OrderTerm;
```

### `createR2Sql` (const)

```ts
const createR2Sql: (config: R2SqlConfig) => R2SqlClient;
```

### `desc` (const)

```ts
const desc: (expr: Sql | string) => OrderTerm;
```

### `fn` (const)

```ts
const fn: {
    avg: (column: Sql | string) => WindowFunction;
    count: (column?: Sql | string) => WindowFunction;
    cumeDist: () => WindowFunction;
    denseRank: () => WindowFunction;
    firstValue: (column: Sql | string) => WindowFunction;
    lag: (column: Sql | string, offset?: number, fallback?: unknown) => WindowFunction;
    lastValue: (column: Sql | string) => WindowFunction;
    lead: (column: Sql | string, offset?: number, fallback?: unknown) => WindowFunction;
    max: (column: Sql | string) => WindowFunction;
    min: (column: Sql | string) => WindowFunction;
    nthValue: (column: Sql | string, n: number) => WindowFunction;
    ntile: (buckets: number) => WindowFunction;
    percentRank: () => WindowFunction;
    rank: () => WindowFunction;
    rowNumber: () => WindowFunction;
    sum: (column: Sql | string) => WindowFunction;
};
```

### `isSql` (const)

```ts
const isSql: (value: unknown) => value is Sql;
```

### `joinSql` (const)

```ts
const joinSql: (parts: ReadonlyArray<Sql | string>, separator: string) => Sql;
```

### `lit` (const)

```ts
const lit: (value: unknown) => string;
```

### `raw` (const)

```ts
const raw: (text: string) => Sql;
```

### `renderOrderTerm` (const)

```ts
const renderOrderTerm: (term: OrderTerm) => string;
```

### `sql` (const)

```ts
const sql: (strings: TemplateStringsArray, ...values: unknown[]) => Sql;
```

### `toText` (const)

```ts
const toText: (value: Sql | string) => string;
```

## `@lunora/bindings/vectors`

### `EmbedFunction` (type)

```ts
type EmbedFunction<TInput = unknown> = (input: TInput) => Promise<ReadonlyArray<number>> | ReadonlyArray<number>;
```

### `LunoraVectors` (interface)

```ts
interface LunoraVectors {
    deleteByIds: (indexName: string, ids: ReadonlyArray<string>) => Promise<VectorizeDeleteMutation>;
    describe: (indexName: string) => Promise<VectorizeIndexDetails>;
    getByIds: (indexName: string, ids: ReadonlyArray<string>) => Promise<ReadonlyArray<VectorizeVector>>;
    query: <TInput>(indexName: string, input: QueryInput<TInput>) => Promise<VectorizeMatches>;
    upsert: <TInput>(indexName: string, input: UpsertInput<TInput>) => Promise<VectorizeUpsertMutation>;
    upsertMany: <TInput>(indexName: string, inputs: ReadonlyArray<UpsertInput<TInput>>) => Promise<VectorizeUpsertMutation>;
}
```

### `LunoraVectorsOptions` (interface)

```ts
interface LunoraVectorsOptions {
    indexes: Record<string, VectorizeIndexLike>;
}
```

### `QueryInput` (interface)

```ts
interface QueryInput<TInput = unknown> {
    embed?: EmbedFunction<TInput>;
    filter?: Record<string, unknown>;
    input?: TInput;
    namespace?: string;
    returnMetadata?: "none" | "indexed" | "all";
    returnValues?: boolean;
    topK?: number;
    vector?: ReadonlyArray<number>;
}
```

### `SchemaLike` (interface)

```ts
interface SchemaLike {
    tables: Record<string, TableDefinitionLike>;
    vectorIndexes: Record<string, VectorIndexDefinitionLike>;
}
```

### `TableDefinitionLike` (interface)

```ts
interface TableDefinitionLike {
    vectorIndexes?: ReadonlyArray<TableVectorIndexLike>;
}
```

### `TableVectorIndexLike` (interface)

```ts
interface TableVectorIndexLike {
    embed: VectorEmbedderLike;
    field: string;
    metadata?: ReadonlyArray<string>;
    name: string;
}
```

### `UpsertInput` (interface)

```ts
interface UpsertInput<TInput = unknown> {
    embed: EmbedFunction<TInput>;
    id: string;
    input: TInput;
    metadata?: Record<string, unknown>;
    namespace?: string;
}
```

### `VectorAdminIndexSummary` (interface)

```ts
interface VectorAdminIndexSummary extends VectorIndexRegistryEntry {
    processedUpToMutation?: string;
    vectorsCount?: number;
}
```

### `VectorAdminIntrospector` (interface)

```ts
interface VectorAdminIntrospector {
    listIndexes: () => Promise<VectorAdminIndexSummary[]>;
    queryIndex?: (options: {
        name: string;
        text: string;
        topK?: number;
    }) => Promise<{
        matches: VectorAdminQueryMatch[];
    }>;
}
```

### `VectorAdminIntrospectorOptions` (interface)

```ts
interface VectorAdminIntrospectorOptions {
    embedders?: Record<string, EmbedFunction<string>>;
    indexes: Record<string, VectorizeIndexLike>;
    registry: ReadonlyArray<VectorIndexRegistryEntry>;
}
```

### `VectorAdminQueryMatch` (interface)

```ts
interface VectorAdminQueryMatch {
    id: string;
    metadata?: Record<string, unknown>;
    score: number;
}
```

### `VectorEmbedderLike` (type)

```ts
type VectorEmbedderLike = (input: string) => Promise<ReadonlyArray<number>> | ReadonlyArray<number>;
```

### `VectorIndexDefinitionLike` (interface)

```ts
interface VectorIndexDefinitionLike {
    embed: VectorEmbedderLike;
    metadata?: (row: Record<string, unknown>) => Record<string, unknown>;
    select: (row: Record<string, unknown>) => string;
    table: string;
}
```

### `VectorIndexRegistryEntry` (interface)

```ts
interface VectorIndexRegistryEntry {
    dimensions?: number;
    field?: string;
    metadata?: ReadonlyArray<string>;
    metric?: VectorMetric;
    name: string;
    table: string;
}
```

### `VectorMatchLike` (interface)

```ts
interface VectorMatchLike {
    id: string;
    metadata?: Record<string, unknown>;
    score: number;
}
```

### `VectorMatchesLike` (interface)

```ts
interface VectorMatchesLike {
    count: number;
    matches: ReadonlyArray<VectorMatchLike>;
}
```

### `VectorMetric` (type)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorQueryInputLike` (interface)

```ts
interface VectorQueryInputLike {
    embed?: VectorEmbedderLike;
    filter?: Record<string, unknown>;
    input?: string;
    namespace?: string;
    returnMetadata?: "none" | "indexed" | "all";
    topK?: number;
    vector?: ReadonlyArray<number>;
}
```

### `VectorRecordLike` (interface)

```ts
interface VectorRecordLike {
    id: string;
    metadata?: Record<string, unknown>;
    namespace?: string;
    values: ReadonlyArray<number>;
}
```

### `VectorSearchLike` (interface)

```ts
interface VectorSearchLike {
    deleteByIds: (indexName: string, ids: ReadonlyArray<string>, namespace?: string) => Promise<void>;
    getByIds: (indexName: string, ids: ReadonlyArray<string>, namespace?: string) => Promise<ReadonlyArray<VectorRecordLike>>;
    query: (indexName: string, input: VectorQueryInputLike) => Promise<VectorMatchesLike>;
    upsert: (indexName: string, input: VectorUpsertInputLike) => Promise<void>;
    upsertNow: (indexName: string, input: VectorUpsertInputLike) => Promise<void>;
}
```

### `VectorUpsertInputLike` (interface)

```ts
interface VectorUpsertInputLike {
    embed: VectorEmbedderLike;
    id: string;
    input: string;
    metadata?: Record<string, unknown>;
    namespace?: string;
}
```

### `VectorizeDeleteMutation` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeIndexDetails` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeIndexLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeMatch` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeMatches` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeQueryOptions` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeUpsertMutation` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `VectorizeVector` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `WriteEvent` (interface)

```ts
interface WriteEvent {
    doc?: Record<string, unknown>;
    id: string;
    op: "delete" | "insert" | "update";
    table: string;
}
```

### `WriteHook` (type)

```ts
type WriteHook = (event: WriteEvent) => Promise<void>;
```

### `createContextVectors` (const)

```ts
const createContextVectors: (lunora: LunoraVectors, options?: CreateContextVectorsOptions) => VectorSearchLike;
```

### `createVectorAdminIntrospector` (const)

```ts
const createVectorAdminIntrospector: (options: VectorAdminIntrospectorOptions) => VectorAdminIntrospector;
```

### `createVectorSyncHook` (const)

```ts
const createVectorSyncHook: (options: {
    allowSharedNamespace?: boolean;
    namespace?: string;
    schema: SchemaLike;
    vectors: VectorSearchLike;
}) => WriteHook;
```

### `createVectors` (const)

```ts
const createVectors: (options: LunoraVectorsOptions) => LunoraVectors;
```
