# `@lunora/d1` — public API

- Package: `packages/d1`
- Tier: core

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/d1`

### `D1Client` (class)

```ts
class D1Client {
    constructor(database: D1DatabaseLike);
    withSession(bookmark?: string): D1Session;
    prepare(sql: string): D1PreparedStatementLike;
    get drizzle(): DrizzleD1Database<Record<string, unknown>>;
    drizzleSession(bookmark?: string): DrizzleD1Database<Record<string, unknown>>;
    batch<U extends BatchItem<"sqlite">, T extends Readonly<[
        U,
        ...U[]
    ]>>(items: T): Promise<BatchResponse<T>>;
    get raw(): D1DatabaseLike;
}
```

### `D1CtxDbOptions` (type)

```ts
type D1ContextDatabaseOptions = Omit<SqlCtxDbOptions, "dialect">;
```

### `D1DatabaseLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `D1Exec` (interface)

Re-exported from `@lunora/sql-store` — signature tracked at its source.

### `D1PreparedStatementLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `D1RetryOptions` (interface)

```ts
interface D1RetryOptions {
    attempts?: number;
    deadlineMs?: number;
    sleep?: (ms: number) => Promise<void>;
    timeoutMs?: number;
}
```

### `D1Session` (class)

```ts
class D1Session {
    constructor(session: D1SessionLike);
    prepare(sql: string): D1PreparedStatementLike;
    run<T = unknown>(sql: string, ...binds: unknown[]): Promise<{
        meta?: Record<string, unknown>;
        results?: T[];
        success: boolean;
    }>;
    all<T = unknown>(sql: string, ...binds: unknown[]): Promise<{
        results: T[];
        success: boolean;
    }>;
    first<T = unknown>(sql: string, ...binds: unknown[]): Promise<T | null>;
    getBookmark(): string | undefined;
}
```

### `D1SessionLike` (interface)

Re-exported from `@lunora/platform` — signature tracked at its source.

### `D1TimeoutError` (class)

_Tagged `@experimental` — signature not tracked; churn here does not fail the gate._

### `ExportGlobalArgs` (interface)

```ts
interface ExportGlobalArgs {
    batchSize?: number;
    tables?: ReadonlyArray<string>;
}
```

### `FacetGlobalColumnOptions` (interface)

```ts
interface FacetGlobalColumnOptions {
    column: string;
    filters?: GlobalFilterClause[];
    limit?: number;
    table: string;
}
```

### `GlobalExportRow` (interface)

```ts
interface ExportRow {
    doc: Record<string, unknown>;
    line?: number;
    table: string;
}
```

### `GlobalFacetResult` (interface)

```ts
interface GlobalFacetResult {
    truncated: boolean;
    values: GlobalFacetValue[];
}
```

### `GlobalFacetValue` (interface)

```ts
interface GlobalFacetValue {
    count: number;
    value: unknown;
}
```

### `GlobalFilterClause` (interface)

```ts
interface GlobalFilterClause {
    column: string;
    value: unknown;
}
```

### `GlobalImportError` (interface)

```ts
interface ImportError {
    code: string;
    line: number;
    message: string;
    table: string;
}
```

### `GlobalImportResult` (interface)

```ts
interface ImportResult {
    conflicts: number;
    errors: ImportError[];
    inserted: Record<string, number>;
}
```

### `GlobalTableInfo` (interface)

```ts
interface GlobalTableInfo {
    name: string;
    rowCount: number;
}
```

### `GlobalTablePage` (interface)

```ts
interface GlobalTablePage {
    columns: string[];
    refs?: Record<string, string>;
    rows: Record<string, unknown>[];
    total: number;
}
```

### `ImportGlobalArgs` (interface)

```ts
interface ImportGlobalArgs {
    exec?: D1ExecLike;
    rows: ReadonlyArray<ExportRow>;
    startLine?: number;
}
```

### `Migration` (interface)

```ts
interface Migration {
    name: string;
    sql: string;
    version: number;
}
```

### `MigrationRunner` (class)

```ts
class MigrationRunner {
    constructor(database: D1Client | D1DatabaseLike, migrations: Migration[]);
    run(): Promise<MigrationRunnerResult>;
}
```

### `MigrationRunnerResult` (interface)

```ts
interface MigrationRunnerResult {
    applied: {
        name: string;
        version: number;
    }[];
    skipped: {
        name: string;
        version: number;
    }[];
}
```

### `ReadGlobalTablePageOptions` (interface)

```ts
interface ReadGlobalTablePageOptions {
    filters?: GlobalFilterClause[];
    limit?: number;
    offset?: number;
    table: string;
}
```

### `SqlCtxDbOptions` (interface)

Re-exported from `@lunora/sql-store` — signature tracked at its source.

### `SqlCtxExec` (interface)

Re-exported from `@lunora/sql-store` — signature tracked at its source.

### `applyCdcChanges` (const)

Re-exported from `@lunora/shard-engine` — signature tracked at its source.

### `backfillD1SearchIndexes` (const)

```ts
const backfillD1SearchIndexes: (exec: SqlCtxExec, schema: SchemaLike) => Promise<void>;
```

### `createD1CtxDb` (const)

```ts
const createD1ContextDatabase: (options: D1ContextDatabaseOptions) => ReturnType<typeof createSqlCtxDb>;
```

### `createSqlCtxDb` (const)

Re-exported from `@lunora/sql-store` — signature tracked at its source.

### `exportGlobalRows` (const)

```ts
const exportGlobalRows: (exec: SqlCtxExec, schema: SchemaLike, args: ExportGlobalArgs) => AsyncGenerator<ExportRow, void, undefined>;
```

### `facetGlobalColumn` (const)

```ts
const facetGlobalColumn: (exec: SqlCtxExec, schema: SchemaLike, options: FacetGlobalColumnOptions) => Promise<GlobalFacetResult>;
```

### `importGlobalRows` (const)

```ts
const importGlobalRows: (writer: DatabaseWriterLike, schema: SchemaLike, args: ImportGlobalArgs) => Promise<ImportResult>;
```

### `isTransientD1Error` (const)

_Tagged `@experimental` — signature not tracked; churn here does not fail the gate._

### `listGlobalTables` (const)

```ts
const listGlobalTables: (exec: SqlCtxExec, schema: SchemaLike) => Promise<GlobalTableInfo[]>;
```

### `readD1CdcChanges` (const)

```ts
const readD1CdcChanges: (exec: SqlCtxExec, options?: {
    limit?: number;
    sinceSeq?: number;
}) => ReturnType<typeof readSqlCdcChanges>;
```

### `readGlobalTablePage` (const)

```ts
const readGlobalTablePage: (exec: SqlCtxExec, schema: SchemaLike, options: ReadGlobalTablePageOptions) => Promise<GlobalTablePage>;
```

### `retryingExec` (const)

_Tagged `@experimental` — signature not tracked; churn here does not fail the gate._

### `runD1AggregateMigrations` (const)

```ts
const runD1AggregateMigrations: (exec: SqlCtxExec, schema: SchemaLike) => Promise<void>;
```

### `runD1CdcMigration` (const)

```ts
const runD1CdcMigration: (exec: SqlCtxExec) => Promise<void>;
```

### `runD1GlobalTableMigrations` (const)

```ts
const runD1GlobalTableMigrations: (exec: SqlCtxExec, schema: SchemaLike) => Promise<void>;
```

### `runD1RankMigrations` (const)

```ts
const runD1RankMigrations: (exec: SqlCtxExec, schema: SchemaLike) => Promise<void>;
```

### `runD1SearchMigrations` (const)

```ts
const runD1SearchMigrations: (exec: SqlCtxExec, schema: SchemaLike) => Promise<void>;
```

### `selectGlobalTables` (const)

```ts
const selectGlobalTables: (schema: SchemaLike, requested?: ReadonlyArray<string>) => string[];
```

### `sqliteDialect` (const)

```ts
const sqliteDialect: SqlDialect;
```

### `sweepD1CdcRetention` (const)

```ts
const sweepD1CdcRetention: (exec: SqlCtxExec, retentionMs: number, now: number) => Promise<void>;
```

### `withD1Retry` (const)

_Tagged `@experimental` — signature not tracked; churn here does not fail the gate._

## `@lunora/d1/dialect`

### `MAX_D1_TABLE_COLUMNS` (const)

```ts
const MAX_D1_TABLE_COLUMNS = 100;
```

### `OCC_VERSION_COLUMN` (const)

```ts
const OCC_VERSION_COLUMN = "_version";
```

### `SqlAffinity` (type)

```ts
type SqlAffinity = "BLOB" | "INTEGER" | "REAL" | "TEXT";
```

### `columnRef` (const)

```ts
const columnRef: (field: string) => string;
```

### `frameworkColumnDdl` (const)

```ts
const frameworkColumnDdl: () => ReadonlyArray<string>;
```

### `physicalIndexName` (const)

```ts
const physicalIndexName: (tableName: string, indexName: string) => string;
```

### `quoteIdentifier` (const)

```ts
const quoteIdentifier: (name: string) => string;
```

### `sqlAffinityForKind` (const)

```ts
const sqlAffinityForKind: (kind: string | undefined) => SqlAffinity;
```
