# `@lunora/platform-rivet` — public API

- Package: `packages/platform-rivet`
- Tier: experimental

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one. This package is
Experimental: the snapshot records how its surface moves, and carries NO
SemVer promise until the package graduates.

## `@lunora/platform-rivet`

### `RIVET_ALARM_ACTION` (const)

```ts
const RIVET_ALARM_ACTION = "__lunoraShardAlarm";
```

### `RIVET_CRON_ACTION` (const)

```ts
const RIVET_CRON_ACTION = "__lunoraCronDispatch";
```

### `RIVET_SCHEDULER_ACTION` (const)

```ts
const RIVET_SCHEDULER_ACTION = "__lunoraSchedulerDispatch";
```

### `RivetActorHandleLike` (interface)

```ts
interface RivetActorHandleLike {
    fetch: (input: Request | string, init?: RequestInit) => Promise<Response>;
}
```

### `RivetActorLike` (interface)

```ts
interface RivetActorLike {
    readonly actorId: string;
    readonly cron: RivetCronLike;
    readonly db: RivetRawDatabaseLike;
    readonly key: ReadonlyArray<string>;
    readonly name: string;
    readonly schedule: RivetScheduleLike;
    waitUntil: (promise: Promise<unknown>) => void;
}
```

### `RivetActorNamespaceLike` (interface)

```ts
interface RivetActorNamespaceLike {
    getOrCreate: (key: string | string[], options?: RivetGetOrCreateOptions) => RivetActorHandleLike;
}
```

### `RivetCronLike` (interface)

```ts
interface RivetCronLike {
    delete: (name: string) => Promise<boolean>;
    set: (options: RivetCronSetOptions) => Promise<void>;
}
```

### `RivetCronSetOptions` (interface)

```ts
interface RivetCronSetOptions {
    action: string;
    args?: unknown[];
    expression: string;
    maxHistory?: number;
    name: string;
    timezone?: string;
}
```

### `RivetGetOrCreateOptions` (interface)

```ts
interface RivetGetOrCreateOptions {
    createInRegion?: string;
}
```

### `RivetPlatform` (interface)

```ts
interface RivetPlatform {
    capabilities: PlatformCapabilities;
    close: () => Promise<void>;
    deliverAlarm: () => Promise<void>;
    deliverCronTick: (functionPath: string, args: Record<string, unknown>) => Promise<void>;
    deliverScheduledJob: (id: string) => Promise<boolean>;
    drain: () => Promise<void>;
    kv: ShardKvStore;
    scheduler: SchedulerHost;
    shard: ShardHost;
    sockets: SocketHost;
    state: RivetShardState;
}
```

### `RivetPlatformOptions` (type)

```ts
type RivetPlatformOptions = RivetSchedulerHostOptions & RivetShardHostOptions;
```

### `RivetRawDatabaseLike` (interface)

```ts
interface RivetRawDatabaseLike {
    execute: <Row extends Record<string, unknown> = Record<string, unknown>>(query: string, ...args: unknown[]) => Promise<Row[]>;
    transaction: <T>(callback: (tx: RivetRawDatabaseLike) => Promise<T> | T, options?: {
        timeout?: number;
    }) => Promise<T>;
}
```

### `RivetScheduleLike` (interface)

```ts
interface RivetScheduleLike {
    after: (duration: number, action: string, ...args: unknown[]) => Promise<string>;
    at: (timestamp: number, action: string, ...args: unknown[]) => Promise<string>;
    cancel: (id: string) => Promise<boolean>;
    get: (id: string) => Promise<RivetScheduledEventLike | undefined>;
    list: () => Promise<RivetScheduledEventLike[]>;
}
```

### `RivetScheduledEventLike` (interface)

```ts
interface RivetScheduledEventLike {
    action: string;
    args: unknown[];
    id: string;
    runAt: number;
}
```

### `RivetSchedulerHost` (interface)

```ts
interface RivetSchedulerHost {
    deliverCronTick: (functionPath: string, args: Record<string, unknown>) => Promise<void>;
    deliverScheduledJob: (id: string) => Promise<boolean>;
    parkJob: (id: string) => Promise<boolean>;
    scheduler: SchedulerHost;
}
```

### `RivetSchedulerHostOptions` (interface)

```ts
interface RivetSchedulerHostOptions {
    onDispatch?: (functionPath: string, args: Record<string, unknown>, job: ScheduledJob) => Promise<void> | void;
}
```

### `RivetShardDatabase` (type)

```ts
type RivetShardDatabase = Database.Database;
```

### `RivetShardDirectoryOptions` (interface)

```ts
interface RivetShardDirectoryOptions {
    resolveRegion?: (hint: ShardRegionHint) => string | undefined;
}
```

### `RivetShardHost` (interface)

```ts
interface RivetShardHost {
    deliverAlarm: () => Promise<void>;
    drain: () => Promise<void>;
    host: ShardHost;
}
```

### `RivetShardHostOptions` (interface)

```ts
interface RivetShardHostOptions {
    onAlarm?: () => Promise<void> | void;
}
```

### `RivetShardState` (interface)

```ts
interface RivetShardState {
    close: () => void;
    readonly database: Database.Database;
    flush: () => Promise<void>;
    readonly isDirty: boolean;
    markDirty: () => void;
}
```

### `RivetSocketHost` (interface)

```ts
interface RivetSocketHost {
    restoreSocket: (id: string, attachment: unknown) => SocketHandle;
    simulateRecycle: () => void;
    socket: SocketHost;
}
```

### `RivetWebSocketLike` (interface)

```ts
interface RivetWebSocketLike {
    readonly bufferedAmount?: number;
    close: (code?: number, reason?: string) => void;
    send: (data: string | ArrayBufferLike | ArrayBufferView | Blob) => void;
}
```

### `clearRivetShardSnapshot` (const)

```ts
const clearRivetShardSnapshot: (database: RivetRawDatabaseLike) => Promise<void>;
```

### `createRivetPlatform` (const)

```ts
const createRivetPlatform: (actor: RivetActorLike, options?: RivetPlatformOptions) => Promise<RivetPlatform>;
```

### `createRivetSchedulerHost` (const)

```ts
const createRivetSchedulerHost: (actor: Pick<RivetActorLike, "cron" | "db" | "schedule">, options?: RivetSchedulerHostOptions) => RivetSchedulerHost;
```

### `createRivetShardDirectory` (const)

```ts
const createRivetShardDirectory: (namespace: RivetActorNamespaceLike, options?: RivetShardDirectoryOptions) => DirectShardDirectory;
```

### `createRivetShardHost` (const)

```ts
const createRivetShardHost: (actor: Pick<RivetActorLike, "key" | "schedule" | "waitUntil">, state: RivetShardState, options?: RivetShardHostOptions) => RivetShardHost;
```

### `createRivetShardKvStore` (const)

```ts
const createRivetShardKvStore: (database: RivetRawDatabaseLike) => {
    kv: ShardKvStore;
    ready: Promise<void>;
};
```

### `createRivetSocketHost` (const)

```ts
const createRivetSocketHost: (state: RivetShardState) => RivetSocketHost;
```

### `openRivetShardState` (const)

```ts
const openRivetShardState: (actor: Pick<RivetActorLike, "db">) => Promise<RivetShardState>;
```

### `restoreRivetAlarm` (const)

```ts
const restoreRivetAlarm: (actor: Pick<RivetActorLike, "schedule">, host: ShardHost) => Promise<number | undefined>;
```

## `@lunora/platform-rivet/conformance`

### `RivetActionHandler` (type)

```ts
type RivetActionHandler = (...args: unknown[]) => unknown;
```

### `RivetActorDouble` (interface)

```ts
interface RivetActorDouble extends RivetActorLike {
    readonly actions: Map<string, RivetActionHandler>;
    cleanup: () => void;
    readonly crons: Map<string, RivetDoubleCronJob>;
    settle: () => Promise<void>;
}
```

### `RivetActorDoubleOptions` (interface)

```ts
interface RivetActorDoubleOptions {
    key?: ReadonlyArray<string>;
    path?: string;
}
```

### `RivetDoubleCronJob` (interface)

```ts
interface RivetDoubleCronJob {
    action: string;
    args: ReadonlyArray<unknown>;
    expression: string;
    name: string;
}
```

### `RivetNamespaceDouble` (interface)

```ts
interface RivetNamespaceDouble extends RivetActorNamespaceLike {
    readonly createdRegions: Map<string, string | undefined>;
    readonly resolutions: Map<string, number>;
}
```

### `createRivetActorDouble` (const)

```ts
const createRivetActorDouble: (options?: RivetActorDoubleOptions) => RivetActorDouble;
```

### `createRivetConformanceHost` (const)

```ts
const createRivetConformanceHost: () => Promise<ConformanceHost>;
```

### `createRivetNamespaceDouble` (const)

```ts
const createRivetNamespaceDouble: () => RivetNamespaceDouble;
```
