# `@lunora/platform-node` — public API

- Package: `packages/platform-node`
- Tier: experimental

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one. This package is
Experimental: the snapshot records how its surface moves, and carries NO
SemVer promise until the package graduates.

## `@lunora/platform-node`

### `NodeGlobalContextDatabaseOptions` (type)

```ts
type NodeGlobalContextDatabaseOptions = Omit<SqlCtxDbOptions, "dialect" | "exec">;
```

### `NodeGlobalStore` (interface)

```ts
interface NodeGlobalStore {
    database: Database.Database;
    dispose: () => void;
    exec: SqlCtxExec;
    migrate: (schema: SchemaLike, options?: {
        cdc?: boolean;
    }) => Promise<void>;
    writer: (options: NodeGlobalContextDatabaseOptions) => ReturnType<typeof createSqlCtxDb>;
}
```

### `NodeGlobalStoreOptions` (interface)

```ts
interface NodeGlobalStoreOptions {
    path?: string;
}
```

### `NodePlatform` (interface)

```ts
interface NodePlatform<Queues extends Record<string, {
    isLunoraQueue: true;
}> = Record<string, never>, Workflows extends Record<string, {
    isLunoraWorkflow: true;
}> = Record<string, never>> {
    [Symbol.dispose]: () => void;
    capabilities: PlatformCapabilities;
    close: () => void;
    directory: ShardDirectory;
    drain: () => Promise<void>;
    globalTables?: NodeGlobalStore;
    kv: ShardKvStore;
    objectStorage?: R2BucketLike;
    queues?: NodeQueueHost<Queues>;
    scheduler: SchedulerHost;
    shard: ShardHost;
    sockets: SocketHost;
    workflows?: NodeWorkflowHost<Workflows>;
}
```

### `NodePlatformOptions` (type)

```ts
type NodePlatformOptions<Queues extends Record<string, {
    isLunoraQueue: true;
}> = Record<string, never>, Workflows extends Record<string, {
    isLunoraWorkflow: true;
}> = Record<string, never>> = {
    globalTablesPath?: string;
    objectStorageDirectory?: string;
    onQueueBatch?: NodeQueueHostOptions<Queues>["onBatch"];
    queues?: Queues;
    workflows?: Workflows;
} & NodeSchedulerHostOptions & NodeShardHostOptions & NodeShardRegistryOptions;
```

### `NodeQueueHost` (interface)

```ts
interface NodeQueueHost<Queues extends Record<string, {
    isLunoraQueue: true;
}>> {
    readonly bindings: {
        [K in keyof Queues]: QueueBindingLike;
    };
    deadLetters: {
        list: (queue: string) => {
            attempts: number;
            body: unknown;
            id: string;
        }[];
        requeue: (id: string) => boolean;
    };
    readonly env: Record<string, unknown>;
    poll: (now?: number) => Promise<number>;
}
```

### `NodeQueueHostOptions` (interface)

```ts
interface NodeQueueHostOptions<Queues extends Record<string, {
    isLunoraQueue: true;
}>> {
    env?: Record<string, unknown>;
    now?: () => number;
    onBatch: (batch: MessageBatchLike) => Promise<void> | void;
    queues: Queues;
    visibilityTimeoutMs?: number;
}
```

### `NodeR2BucketOptions` (interface)

```ts
interface NodeR2BucketOptions {
    directory: string;
}
```

### `NodeSchedulerHost` (interface)

```ts
interface NodeSchedulerHost {
    dispose: () => void;
    scheduler: SchedulerHost;
    simulateDeadLetter: (id: string) => Promise<boolean>;
}
```

### `NodeSchedulerHostOptions` (interface)

```ts
interface NodeSchedulerHostOptions {
    onDispatch?: (functionPath: string, args: Record<string, unknown>, job: {
        attempts: number;
        id: string;
    }) => Promise<void> | void;
}
```

### `NodeShard` (interface)

```ts
interface NodeShard {
    kv: ShardKvStore;
    shard: ShardHost;
    shardKey: string;
    sockets: SocketHost;
}
```

### `NodeShardHostOptions` (interface)

```ts
interface NodeShardHostOptions {
    onAlarm?: () => Promise<void> | void;
    path?: string;
    shardKey?: string;
}
```

### `NodeShardRegistry` (interface)

```ts
interface NodeShardRegistry {
    close: () => void;
    directory: DirectShardDirectory;
    listShardKeys: (table?: string) => ReadonlyArray<string>;
    shardFor: (key: string) => NodeShard;
}
```

### `NodeShardRegistryOptions` (interface)

```ts
interface NodeShardRegistryOptions {
    directory?: string;
    onAlarm?: (shard: NodeShard) => Promise<void> | void;
    onFetch?: (request: Request, shard: NodeShard) => Promise<Response> | Response;
}
```

### `NodeShardState` (interface)

```ts
interface NodeShardState {
    acceptWebSocket: (socket: unknown, tags?: string[]) => void;
    blockConcurrencyWhile: <T>(callback: () => Promise<T>) => Promise<T>;
    getWebSockets: (tag?: string) => unknown[];
    id: {
        name?: string;
    };
    storage: {
        delete: (key: string) => Promise<boolean>;
        deleteAlarm: () => Promise<void>;
        get: <T = unknown>(key: string) => Promise<T | undefined>;
        getAlarm: () => Promise<number | null>;
        list: <T = unknown>(options?: {
            prefix?: string;
        }) => Promise<Map<string, T>>;
        put: (key: string, value: unknown) => Promise<void>;
        setAlarm: (scheduledTime: Date | number) => Promise<void>;
        sql: {
            readonly databaseSize?: number;
            exec: (query: string, ...bindings: unknown[]) => unknown;
        };
        transaction: <T>(closure: () => Promise<T>) => Promise<T>;
    };
    waitUntil: (promise: Promise<unknown>) => void;
}
```

### `NodeSocketHost` (interface)

```ts
interface NodeSocketHost {
    readFrames: (handle: SocketHandle) => string[];
    restoreSocket: (id: string, attachment: unknown) => SocketHandle;
    simulateRecycle: () => void;
    socket: SocketHost;
}
```

### `NodeWorkflowHost` (interface)

```ts
interface NodeWorkflowHost<Workflows extends Record<string, {
    isLunoraWorkflow: true;
}>> {
    readonly bindings: {
        [K in keyof Workflows]: WorkflowBindingLike;
    };
    readonly env: Record<string, unknown>;
    readonly runtime: WorkflowRuntime;
}
```

### `NodeWorkflowHostOptions` (interface)

```ts
interface NodeWorkflowHostOptions<Workflows extends Record<string, {
    isLunoraWorkflow: true;
}>> {
    env?: Record<string, unknown>;
    leaseTtlMs?: number;
    store: WorkflowStore;
    workflows: Workflows;
}
```

### `createNodeGlobalStore` (const)

```ts
const createNodeGlobalStore: (options?: NodeGlobalStoreOptions) => NodeGlobalStore;
```

### `createNodePlatform` (const)

```ts
const createNodePlatform: <Queues extends Record<string, {
    isLunoraQueue: true;
}> = Record<string, never>, Workflows extends Record<string, {
    isLunoraWorkflow: true;
}> = Record<string, never>>(options?: NodePlatformOptions<Queues, Workflows>) => NodePlatform<Queues, Workflows>;
```

### `createNodeQueueHost` (const)

```ts
const createNodeQueueHost: <Queues extends Record<string, {
    isLunoraQueue: true;
}>>(database: Database.Database, options: NodeQueueHostOptions<Queues>) => NodeQueueHost<Queues>;
```

### `createNodeR2Bucket` (const)

```ts
const createNodeR2Bucket: (options: NodeR2BucketOptions) => R2BucketLike;
```

### `createNodeSchedulerHost` (const)

```ts
const createNodeSchedulerHost: (database: Database.Database, options?: NodeSchedulerHostOptions) => NodeSchedulerHost;
```

### `createNodeShardHost` (const)

```ts
const createNodeShardHost: (options?: NodeShardHostOptions) => {
    database: Database.Database;
    dispose: () => void;
    drain: () => Promise<void>;
    host: ShardHost;
};
```

### `createNodeShardKvStore` (const)

```ts
const createNodeShardKvStore: (database: Database.Database) => ShardKvStore;
```

### `createNodeShardRegistry` (const)

```ts
const createNodeShardRegistry: (options?: NodeShardRegistryOptions) => NodeShardRegistry;
```

### `createNodeShardState` (const)

```ts
const createNodeShardState: (shard: NodeShard) => NodeShardState;
```

### `createNodeSocketHost` (const)

```ts
const createNodeSocketHost: (database: Database.Database) => NodeSocketHost;
```

### `createNodeSqlExec` (const)

```ts
const createNodeSqlExec: (database: Database.Database) => SqlCtxExec;
```

### `createNodeWorkflowHost` (const)

```ts
const createNodeWorkflowHost: <Workflows extends Record<string, {
    isLunoraWorkflow: true;
}>>(options: NodeWorkflowHostOptions<Workflows>) => NodeWorkflowHost<Workflows>;
```

### `createNodeWorkflowStore` (const)

```ts
const createNodeWorkflowStore: (database: Database.Database) => WorkflowStore;
```

## `@lunora/platform-node/conformance`

### `createNodeConformanceHost` (const)

```ts
const createNodeConformanceHost: () => ConformanceHost;
```
