# `@lunora/workflow` — public API

- Package: `packages/workflow`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/workflow`

### `ArgsOf` (type)

```ts
type ArgsOf<F> = F extends FunctionReference<infer _K, infer A, infer _R> ? A : never;
```

### `BranchCompensationParams` (interface)

```ts
interface BranchCompensationParams {
    [key: string]: unknown;
    branch: string;
    error: {
        message: string;
        name: string;
    };
    index: number;
    output?: unknown;
}
```

### `FunctionKind` (type)

```ts
type FunctionKind = "action" | "mutation" | "query" | "stream";
```

### `FunctionReference` (interface)

```ts
interface FunctionReference<Kind extends FunctionKind = FunctionKind, Args = unknown, Return = unknown> {
    readonly __lunoraPhantom?: {
        args: Args;
        kind: Kind;
        returns: Return;
    };
    readonly __lunoraRef: string;
}
```

### `InferStepArgs` (type)

```ts
type InferStepArgs<A extends StepArgsValidator> = InferValidatorMap<A>;
```

### `LunoraWorkflowsOptions` (interface)

```ts
interface LunoraWorkflowsOptions {
    bindings: Record<string, WorkflowBindingLike>;
}
```

### `MAX_BRANCHES` (const)

```ts
const MAX_BRANCHES = 100;
```

### `NativeNonRetryableErrorConstructor` (type)

```ts
type NativeNonRetryableErrorConstructor = new (message: string, name?: string) => Error;
```

### `NonRetryableError` (class)

```ts
class NonRetryableError extends Error {
    constructor(message: string, name?: string);
}
```

### `RunFunctionOptions` (interface)

```ts
interface RunFunctionOptions {
    shardKey?: string;
}
```

### `RunStepOptions` (interface)

```ts
interface RunStepOptions {
    config?: WorkflowStepConfigLike;
    name?: string;
}
```

### `StepArgsValidator` (type)

```ts
type StepArgsValidator = ValidatorMap;
```

### `StepConfig` (interface)

```ts
interface StepConfig<A extends StepArgsValidator, Result> {
    args: A;
    config?: WorkflowStepConfigLike;
    handler: StepHandler<A, Result>;
    returns?: Validator<Result>;
    rollback?: StepRollbackHandler<A, Result>;
    rollbackConfig?: WorkflowStepConfigLike;
}
```

### `StepDefinition` (interface)

```ts
interface StepDefinition<A extends StepArgsValidator = StepArgsValidator, Result = unknown> {
    readonly args: A;
    readonly config?: WorkflowStepConfigLike;
    readonly handler: StepHandler<A, Result>;
    readonly isLunoraStep: true;
    readonly name: string;
    readonly returns?: Validator<Result>;
    readonly rollback?: StepRollbackHandler<A, Result>;
    readonly rollbackConfig?: WorkflowStepConfigLike;
}
```

### `StepHandler` (type)

```ts
type StepHandler<A extends StepArgsValidator, Result> = (context: StepRunContext, args: InferStepArgs<A>) => Promise<Result> | Result;
```

### `StepRollbackContext` (interface)

```ts
interface StepRollbackContext<A extends StepArgsValidator, Result> {
    readonly args: InferStepArgs<A>;
    readonly env: Record<string, unknown>;
    readonly error: Error;
    readonly log: WorkflowLogger;
    readonly output: Result | undefined;
    readonly run: WorkflowRunFunction;
}
```

### `StepRollbackHandler` (type)

```ts
type StepRollbackHandler<A extends StepArgsValidator, Result> = (context: StepRollbackContext<A, Result>) => Promise<void> | void;
```

### `StepRunContext` (interface)

```ts
interface StepRunContext {
    readonly attempt: number;
    readonly config: WorkflowStepConfigLike;
    readonly env: Record<string, unknown>;
    readonly log: WorkflowLogger;
    readonly run: WorkflowRunFunction;
    readonly step: {
        count: number;
        name: string;
    };
}
```

### `WaitForEventOptions` (interface)

```ts
interface WaitForEventOptions {
    name?: string;
    timeout?: number | string;
}
```

### `WorkflowBindingLike` (interface)

```ts
interface WorkflowBindingLike<Params = Record<string, unknown>> {
    create: (options?: WorkflowCreateOptions<Params>) => Promise<WorkflowInstanceLike>;
    createBatch: (batch: ReadonlyArray<WorkflowCreateOptions<Params>>) => Promise<WorkflowInstanceLike[]>;
    get: (id: string) => Promise<WorkflowInstanceLike>;
}
```

### `WorkflowBindingSpec` (interface)

```ts
interface WorkflowBindingSpec {
    binding: string;
    exportName: string;
}
```

### `WorkflowBranch` (interface)

```ts
interface WorkflowBranch<Output = unknown> {
    readonly __output?: Output;
    readonly compensateWith?: string;
    readonly id?: string;
    readonly params?: Record<string, unknown>;
    readonly timeout?: number | string;
    readonly workflow: string;
}
```

### `WorkflowBranchOutputs` (type)

```ts
type WorkflowBranchOutputs<B extends ReadonlyArray<WorkflowBranch>> = {
    -readonly [K in keyof B]: B[K] extends WorkflowBranch<infer Output> ? Output : never;
};
```

### `WorkflowConfig` (interface)

```ts
interface WorkflowConfig<Params = Record<string, unknown>, Output = unknown> {
    handler: WorkflowHandler<Params, Output>;
    name?: string;
}
```

### `WorkflowCreateOptions` (interface)

```ts
interface WorkflowCreateOptions<Params = Record<string, unknown>> {
    id?: string;
    params?: Params;
    retention?: {
        errorRetention?: string;
        successRetention?: string;
    };
}
```

### `WorkflowDefinition` (interface)

```ts
interface WorkflowDefinition<Params = Record<string, unknown>, Output = unknown> extends WorkflowConfig<Params, Output> {
    readonly __output?: Output;
    readonly __params?: Params;
    readonly isLunoraWorkflow: true;
}
```

### `WorkflowEventDefinition` (interface)

```ts
interface WorkflowEventDefinition<Payload = unknown> {
    readonly isLunoraWorkflowEvent: true;
    readonly payload: Validator<Payload>;
    readonly type: string;
}
```

### `WorkflowEventLike` (interface)

```ts
interface WorkflowEventLike<Params = Record<string, unknown>> {
    readonly instanceId: string;
    readonly payload: Readonly<Params>;
    readonly timestamp: Date;
    readonly workflowName: string;
}
```

### `WorkflowHandle` (interface)

```ts
interface WorkflowHandle<Params = Record<string, unknown>> {
    create: (options?: WorkflowCreateOptions<Params>) => Promise<WorkflowInstanceLike>;
    createBatch: (batch: ReadonlyArray<WorkflowCreateOptions<Params>>) => Promise<WorkflowInstanceLike[]>;
    get: (id: string) => Promise<WorkflowInstanceLike>;
    sendEvent: <Payload>(instanceId: string, event: WorkflowEventDefinition<Payload>, payload: Payload) => Promise<void>;
}
```

### `WorkflowHandler` (type)

```ts
type WorkflowHandler<Params = Record<string, unknown>, Output = unknown> = (context: WorkflowRunContext<Params>) => Output | Promise<Output>;
```

### `WorkflowInstanceAction` (type)

```ts
type WorkflowInstanceAction = "pause" | "resume" | "terminate";
```

### `WorkflowInstanceDetail` (interface)

```ts
interface WorkflowInstanceDetail extends WorkflowInstanceSummary {
    error?: unknown;
    output?: unknown;
    params?: unknown;
    steps: WorkflowStepDetail[];
}
```

### `WorkflowInstanceLike` (interface)

```ts
interface WorkflowInstanceLike {
    readonly id: string;
    pause: () => Promise<void>;
    restart: () => Promise<void>;
    resume: () => Promise<void>;
    sendEvent: (event: {
        payload: unknown;
        type: string;
    }) => Promise<void>;
    status: () => Promise<WorkflowStatusResult>;
    terminate: () => Promise<void>;
}
```

### `WorkflowInstancePage` (interface)

```ts
interface WorkflowInstancePage {
    instances: WorkflowInstanceSummary[];
    page: number;
    perPage: number;
    totalCount?: number;
}
```

### `WorkflowInstanceStatus` (type)

```ts
type WorkflowInstanceStatus = "complete" | "errored" | "paused" | "queued" | "running" | "terminated" | "unknown" | "waiting" | "waitingForPause";
```

### `WorkflowInstanceSummary` (interface)

```ts
interface WorkflowInstanceSummary {
    createdOn?: string;
    endedOn?: string;
    id: string;
    startedOn?: string;
    status: WorkflowInstanceStatus;
}
```

### `WorkflowLogger` (interface)

```ts
interface WorkflowLogger {
    debug: (message: string, ...rest: unknown[]) => void;
    error: (message: string, ...rest: unknown[]) => void;
    info: (message: string, ...rest: unknown[]) => void;
    warn: (message: string, ...rest: unknown[]) => void;
}
```

### `WorkflowParallelFunction` (type)

```ts
type WorkflowParallelFunction = <const B extends ReadonlyArray<WorkflowBranch>>(branches: B) => Promise<WorkflowBranchOutputs<B>>;
```

### `WorkflowRollbackContextLike` (interface)

```ts
interface WorkflowRollbackContextLike<T = unknown> {
    ctx: WorkflowStepContextLike;
    error: Error;
    output: T | undefined;
    stepName: string;
}
```

### `WorkflowRollbackHandlerLike` (type)

```ts
type WorkflowRollbackHandlerLike<T = unknown> = (context: WorkflowRollbackContextLike<T>) => Promise<void>;
```

### `WorkflowRunContext` (interface)

```ts
interface WorkflowRunContext<Params = Record<string, unknown>> {
    readonly env: Record<string, unknown>;
    readonly event: WorkflowEventLike<Params>;
    readonly log: WorkflowLogger;
    readonly parallel: WorkflowParallelFunction;
    readonly params: Readonly<Params>;
    readonly run: WorkflowRunFunction;
    readonly runStep: WorkflowRunStepFunction;
    readonly spawn: WorkflowSpawnFunction;
    readonly step: WorkflowStepLike;
    readonly waitForEvent: WorkflowWaitForEventFunction;
}
```

### `WorkflowRunFunction` (type)

```ts
type WorkflowRunFunction = <F extends FunctionReference>(function_: F, args?: ArgsOf<F>, options?: RunFunctionOptions) => Promise<unknown>;
```

### `WorkflowRunStepFunction` (type)

```ts
type WorkflowRunStepFunction = <A extends StepArgsValidator, Result>(step: StepDefinition<A, Result>, args: InferStepArgs<A>, options?: RunStepOptions) => Promise<Result>;
```

### `WorkflowSpawnFunction` (type)

```ts
type WorkflowSpawnFunction = (workflow: string, params?: Record<string, unknown>, options?: WorkflowSpawnOptions) => Promise<WorkflowInstanceLike>;
```

### `WorkflowSpawnOptions` (interface)

```ts
interface WorkflowSpawnOptions {
    id?: string;
}
```

### `WorkflowStatusResult` (interface)

```ts
interface WorkflowStatusResult {
    error?: {
        message: string;
        name: string;
    };
    output?: unknown;
    status: WorkflowInstanceStatus;
}
```

### `WorkflowStepConfigLike` (interface)

```ts
interface WorkflowStepConfigLike {
    retries?: {
        backoff?: "constant" | "exponential" | "linear";
        delay?: number | string;
        limit: number;
    };
    timeout?: number | string;
}
```

### `WorkflowStepContextLike` (interface)

```ts
interface WorkflowStepContextLike {
    attempt: number;
    config: WorkflowStepConfigLike;
    step: {
        count: number;
        name: string;
    };
}
```

### `WorkflowStepDetail` (interface)

```ts
interface WorkflowStepDetail {
    attempts?: number;
    end?: string;
    error?: unknown;
    name: string;
    output?: unknown;
    start?: string;
    success?: boolean;
    type?: string;
}
```

### `WorkflowStepLike` (interface)

```ts
interface WorkflowStepLike {
    do: {
        <T>(name: string, callback: (context: WorkflowStepContextLike) => Promise<T>, rollback?: WorkflowStepRollbackOptionsLike<T>): Promise<T>;
        <T>(name: string, config: WorkflowStepConfigLike, callback: (context: WorkflowStepContextLike) => Promise<T>, rollback?: WorkflowStepRollbackOptionsLike<T>): Promise<T>;
    };
    sleep: (name: string, duration: number | string) => Promise<void>;
    sleepUntil: (name: string, timestamp: Date | number) => Promise<void>;
    waitForEvent: <T = unknown>(name: string, options: {
        timeout?: number | string;
        type: string;
    }) => Promise<{
        payload: Readonly<T>;
        type: string;
    }>;
}
```

### `WorkflowStepRollbackOptionsLike` (interface)

```ts
interface WorkflowStepRollbackOptionsLike<T = unknown> {
    rollback?: WorkflowRollbackHandlerLike<T>;
    rollbackConfig?: WorkflowStepConfigLike;
}
```

### `WorkflowWaitForEventFunction` (type)

```ts
type WorkflowWaitForEventFunction = <Payload>(event: WorkflowEventDefinition<Payload>, options?: WaitForEventOptions) => Promise<Payload>;
```

### `Workflows` (interface)

```ts
interface Workflows {
    get: <Params = Record<string, unknown>>(name: string) => WorkflowHandle<Params>;
}
```

### `WorkflowsRestClient` (interface)

```ts
interface WorkflowsRestClient {
    getInstance: (args: {
        instanceId: string;
        workflowName: string;
    }) => Promise<WorkflowInstanceDetail>;
    listInstances: (args: {
        page?: number;
        perPage?: number;
        status?: WorkflowInstanceStatus;
        workflowName: string;
    }) => Promise<WorkflowInstancePage>;
    setInstanceStatus: (args: {
        action: WorkflowInstanceAction;
        instanceId: string;
        workflowName: string;
    }) => Promise<{
        status: WorkflowInstanceStatus;
    }>;
}
```

### `WorkflowsRestConfig` (interface)

```ts
interface WorkflowsRestConfig {
    accountId: string;
    apiToken: string;
    fetch?: typeof globalThis.fetch;
}
```

### `WorkflowsRestError` (class)

```ts
class WorkflowsRestError extends LunoraError {
    constructor(status: number, body: string);
}
```

### `branch` (const)

```ts
const branch: <Output = unknown>(workflow: string, params?: Record<string, unknown>, options?: {
    compensateWith?: string;
    id?: string;
    timeout?: number | string;
}) => WorkflowBranch<Output>;
```

### `convertNonRetryableError` (const)

```ts
const convertNonRetryableError: (error: unknown, NativeNonRetryableError: NativeNonRetryableErrorConstructor | undefined) => never;
```

### `createRunStep` (const)

```ts
const createRunStep: (deps: RunStepDeps) => WorkflowRunStepFunction;
```

### `createWaitForEvent` (const)

```ts
const createWaitForEvent: (deps: WaitForEventDeps) => WorkflowWaitForEventFunction;
```

### `createWorkflowContext` (const)

```ts
const createWorkflowContext: (env: Record<string, unknown>, specs: ReadonlyArray<WorkflowBindingSpec>) => Workflows;
```

### `createWorkflowRunContext` (const)

```ts
const createWorkflowRunContext: <Params = Record<string, unknown>>(options: RunContextOptions<Params>) => WorkflowRunContext<Params>;
```

### `createWorkflows` (const)

```ts
const createWorkflows: (options: LunoraWorkflowsOptions) => Workflows;
```

### `createWorkflowsRestClient` (const)

```ts
const createWorkflowsRestClient: (config: WorkflowsRestConfig) => WorkflowsRestClient;
```

### `defineStep` (const)

```ts
const defineStep: <A extends StepArgsValidator, Result>(name: string, config: StepConfig<A, Result>) => StepDefinition<A, Result>;
```

### `defineWorkflow` (const)

```ts
const defineWorkflow: <Params = Record<string, unknown>, Output = unknown>(config: WorkflowConfig<Params, Output>) => WorkflowDefinition<Params, Output>;
```

### `defineWorkflowEvent` (const)

```ts
const defineWorkflowEvent: <Payload>(type: string, payload: Validator<Payload>) => WorkflowEventDefinition<Payload>;
```

### `isDuplicateInstanceError` (const)

```ts
const isDuplicateInstanceError: (error: unknown) => boolean;
```

### `isNonRetryableError` (const)

```ts
const isNonRetryableError: (value: unknown) => value is NonRetryableError;
```

### `isStepDefinition` (const)

```ts
const isStepDefinition: (value: unknown) => value is StepDefinition;
```

### `isWorkflowDefinition` (const)

```ts
const isWorkflowDefinition: (value: unknown) => value is WorkflowDefinition;
```

### `isWorkflowEventDefinition` (const)

```ts
const isWorkflowEventDefinition: (value: unknown) => value is WorkflowEventDefinition;
```

### `toNativeNonRetryableError` (const)

```ts
const toNativeNonRetryableError: (error: NonRetryableError, NativeNonRetryableError: NativeNonRetryableErrorConstructor) => Error;
```

### `validateStepArgs` (const)

```ts
const validateStepArgs: (validators: StepArgsValidator, source: Record<string, unknown>) => Record<string, unknown>;
```

### `workflowBindingName` (const)

```ts
const workflowBindingName: (exportName: string) => string;
```

### `workflowClassName` (const)

```ts
const workflowClassName: (exportName: string) => string;
```

### `workflowDefaultName` (const)

```ts
const workflowDefaultName: (exportName: string) => string;
```

## `@lunora/workflow/do`

### `default` (class)

```ts
class LunoraWorkflow<Params = Record<string, unknown>, Output = unknown> extends WorkflowEntrypoint<Record<string, unknown>, Params> {
    #private;
    constructor(context: ConstructorParameters<typeof WorkflowEntrypoint>[0], env: Record<string, unknown>, definition: WorkflowDefinition<Params, Output>, exportName?: string);
    override run(event: Readonly<WorkflowEvent<Params>>, step: WorkflowStep): Promise<Output>;
}
```
