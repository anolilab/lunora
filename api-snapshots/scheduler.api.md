# `@lunora/scheduler` — public API

- Package: `packages/scheduler`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/scheduler`

### `ArgsOf` (type)

```ts
type ArgsOf<F> = F extends FunctionReference<infer _K, infer A, infer _R> ? A : never;
```

### `CRON_SCHEDULE_KINDS` (const)

```ts
const CRON_SCHEDULE_KINDS: ReadonlySet<CronScheduleKind>;
```

### `CronJob` (interface)

```ts
interface CronJob {
    args: Record<string, unknown>;
    cron: string;
    functionPath?: string;
    name: string;
    workflow?: string;
}
```

### `CronJobsBuilder` (interface)

```ts
interface CronJobsBuilder {
    cron: <T extends CronTarget>(name: string, cronExpr: string, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
    daily: <T extends CronTarget>(name: string, schedule: DailySchedule, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
    hourly: <T extends CronTarget>(name: string, schedule: HourlySchedule, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
    interval: <T extends CronTarget>(name: string, schedule: IntervalSchedule, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
    jobs: () => ReadonlyArray<CronJob>;
    monthly: <T extends CronTarget>(name: string, schedule: MonthlySchedule, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
    weekly: <T extends CronTarget>(name: string, schedule: WeeklySchedule, target: T, args?: CronTargetArgs<T>) => CronJobsBuilder;
}
```

### `CronScheduleKind` (type)

```ts
type CronScheduleKind = "daily" | "hourly" | "interval" | "monthly" | "weekly";
```

### `CronTarget` (type)

```ts
type CronTarget = SchedulableReference | WorkflowReference;
```

### `CronTriggerOptions` (interface)

```ts
interface CronTriggerOptions {
    args?: Record<string, unknown>;
    fn: FunctionReference;
    schedule: string;
}
```

### `CronTriggerSnippet` (interface)

```ts
interface CronTriggerSnippet {
    crons: string[];
    dispatcher: {
        args: Record<string, unknown>;
        functionPath: string;
    };
    wranglerJsonc: string;
}
```

### `DailySchedule` (interface)

```ts
interface DailySchedule {
    hourUTC: number;
    minuteUTC: number;
}
```

### `DurableObjectIdLike` (interface)

```ts
interface DurableObjectIdLike {
    toString: () => string;
}
```

### `DurableObjectJurisdiction` (type)

```ts
type DurableObjectJurisdiction = "eu" | "fedramp" | "us";
```

### `DurableObjectNamespaceLike` (interface)

```ts
interface DurableObjectNamespaceLike {
    get: (id: DurableObjectIdLike) => DurableObjectStubLike;
    idFromName: (name: string) => DurableObjectIdLike;
    jurisdiction?: (jurisdiction: DurableObjectJurisdiction) => DurableObjectNamespaceLike;
}
```

### `DurableObjectStubLike` (interface)

```ts
interface DurableObjectStubLike {
    fetch: (input: Request | string, init?: RequestInit) => Promise<Response>;
}
```

### `EnqueueOptions` (interface)

```ts
interface EnqueueOptions {
    delayMs?: number;
    retry?: RetryPolicy;
    shardKey?: string;
}
```

### `FunctionKind` (type)

```ts
type FunctionKind = "action" | "mutation" | "query" | "stream";
```

### `FunctionReference` (interface)

```ts
interface FunctionReference<Kind extends FunctionKind = FunctionKind, Args = unknown, Return = unknown> {
    readonly __lunoraPhantom?: {
        args: Args;
        kind: Kind;
        returns: Return;
    };
    readonly __lunoraRef: string;
}
```

### `HourlySchedule` (interface)

```ts
interface HourlySchedule {
    minuteUTC: number;
}
```

### `HttpDispatcherOptions` (interface)

```ts
interface HttpDispatcherOptions {
    adminToken: string;
    fetchImpl?: typeof fetch;
    originUrl: string;
    timeoutMs?: number;
}
```

### `IntervalSchedule` (interface)

```ts
interface IntervalSchedule {
    hours?: number;
    minutes?: number;
    seconds?: number;
}
```

### `LunoraSchedulerOptions` (interface)

```ts
interface LunoraSchedulerOptions {
    instanceName?: string;
    jurisdiction?: DurableObjectJurisdiction;
    namespace: DurableObjectNamespaceLike;
}
```

### `MAX_RETRY_ATTEMPTS` (const)

```ts
const MAX_RETRY_ATTEMPTS = 5;
```

### `MessageBatchLike` (interface)

```ts
interface MessageBatchLike<Body = unknown> {
    ackAll: () => void;
    readonly messages: ReadonlyArray<QueueMessageLike<Body>>;
    readonly queue: string;
    retryAll: (options?: {
        delaySeconds?: number;
    }) => void;
}
```

### `MonthlySchedule` (interface)

```ts
interface MonthlySchedule extends DailySchedule {
    day: number;
}
```

### `QueueConsumerOptions` (interface)

```ts
interface QueueConsumerOptions {
    dispatch: QueueDispatch;
}
```

### `QueueDispatch` (type)

```ts
type QueueDispatch = (job: QueueJob, messageId?: string) => Promise<void>;
```

### `QueueEnqueueOptions` (interface)

```ts
interface QueueEnqueueOptions {
    delaySeconds?: number;
    shardKey?: string;
}
```

### `QueueJob` (interface)

```ts
interface QueueJob {
    args?: Record<string, unknown>;
    functionPath: string;
    shardKey?: string;
}
```

### `QueueLike` (interface)

```ts
interface QueueLike<Body = unknown> {
    send: (body: Body, options?: QueueSendOptionsLike) => Promise<void>;
    sendBatch: (messages: Iterable<QueueSendRequestLike<Body>>, options?: QueueSendOptionsLike) => Promise<void>;
}
```

### `QueueMessageLike` (interface)

```ts
interface QueueMessageLike<Body = unknown> {
    ack: () => void;
    readonly attempts: number;
    readonly body: Body;
    readonly id: string;
    retry: (options?: {
        delaySeconds?: number;
    }) => void;
    readonly timestamp: Date;
}
```

### `QueueSendOptionsLike` (interface)

```ts
interface QueueSendOptionsLike {
    delaySeconds?: number;
}
```

### `QueueSendRequestLike` (interface)

```ts
interface QueueSendRequestLike<Body = unknown> {
    body: Body;
    delaySeconds?: number;
}
```

### `QueueWorkpool` (interface)

```ts
interface QueueWorkpool {
    enqueue: <F extends SchedulableReference>(function_: F, args: ArgsOf<F>, options?: QueueEnqueueOptions) => Promise<void>;
    enqueueBatch: (jobs: ReadonlyArray<{
        args?: Record<string, unknown>;
        ref: FunctionReference;
        shardKey?: string;
    }>, options?: QueueSendOptionsLike) => Promise<void>;
}
```

### `QueueWorkpoolOptions` (interface)

```ts
interface QueueWorkpoolOptions {
    queue: QueueLike<QueueJob>;
}
```

### `RETRY_BASE_DELAY_MS` (const)

```ts
const RETRY_BASE_DELAY_MS = 3e4;
```

### `RetryPolicy` (interface)

```ts
interface RetryPolicy {
    backoff?: "exponential" | "linear";
    baseMs?: number;
    maxAttempts?: number;
    maxMs?: number;
}
```

### `RunOptions` (interface)

```ts
interface RunOptions {
    id?: string;
    maxConcurrency?: number;
    pool?: string;
    retry?: RetryPolicy;
    shardKey?: string;
}
```

### `ScheduleRecord` (interface)

```ts
interface ScheduleRecord {
    args: Record<string, unknown>;
    attempts?: number;
    enqueuedAt: number;
    functionPath?: string;
    id: string;
    instanceName?: string;
    pool?: string;
    retry?: RetryPolicy;
    scheduledFor: number;
    shardKey?: string;
    workflow?: string;
}
```

### `Scheduler` (interface)

```ts
interface Scheduler {
    cancel: (id: string) => Promise<{
        cancelled: boolean;
    }>;
    dead: () => Promise<ScheduleRecord[]>;
    deadRetry: (id: string) => Promise<boolean>;
    get: (id: string) => Promise<ScheduleRecord | null>;
    list: () => Promise<ScheduleRecord[]>;
    runAfter: <T extends CronTarget>(delayMs: number, target: T, args: ScheduleTargetArgs<T>, options?: RunOptions) => Promise<string>;
    runAt: <T extends CronTarget>(date: Date | number, target: T, args: ScheduleTargetArgs<T>, options?: RunOptions) => Promise<string>;
}
```

### `SchedulerDO` (class)

```ts
class SchedulerDO {
    protected readonly state: SchedulerDOState;
    protected readonly env: SchedulerEnv;
    constructor(state: SchedulerDOState, env: SchedulerEnv);
    fetch(request: Request): Promise<Response>;
    alarm(): Promise<void>;
    protected dispatch(record: ScheduleRecord): Promise<boolean>;
}
```

### `SchedulerDOState` (interface)

```ts
interface SchedulerDOState {
    acceptWebSocket?: (ws: WebSocket) => void;
    getWebSockets?: () => WebSocket[];
    setWebSocketAutoResponse?: (pair: WebSocketRequestResponsePair) => void;
    storage: {
        delete: (key: string | string[]) => Promise<number | boolean>;
        deleteAlarm: () => Promise<void> | void;
        get: <T = unknown>(key: string) => Promise<T | undefined>;
        getAlarm: () => Promise<number | null>;
        list: <T = unknown>(options?: {
            end?: string;
            limit?: number;
            prefix?: string;
            startAfter?: string;
        }) => Promise<Map<string, T>>;
        put: <T = unknown>(entries: Record<string, T> | string, value?: T) => Promise<void>;
        setAlarm: (scheduledTime: number | Date) => Promise<void> | void;
    };
}
```

### `SchedulerEnv` (interface)

```ts
interface SchedulerEnv {
    [key: string]: unknown;
    LUNORA_ADMIN_TOKEN?: string;
    LUNORA_ORIGIN_URL?: string;
    LUNORA_SCHEDULER_SECRET?: string;
}
```

### `SchedulerHostOptions` (interface)

```ts
interface SchedulerHostOptions {
    instanceName?: string;
    jurisdiction?: "eu" | "fedramp" | "us";
    namespace: Parameters<typeof createScheduler>[0]["namespace"];
}
```

### `SchedulerPoolStatus` (interface)

```ts
interface SchedulerPoolStatus {
    inFlight: number;
    maxConcurrency: number;
    name: string;
    queued: number;
}
```

### `SchedulerStatus` (interface)

```ts
interface SchedulerStatus {
    backlog: number;
    inFlight: number;
    pools: SchedulerPoolStatus[];
}
```

### `WeeklySchedule` (interface)

```ts
interface WeeklySchedule extends DailySchedule {
    dayOfWeek: "friday" | "monday" | "saturday" | "sunday" | "thursday" | "tuesday" | "wednesday";
}
```

### `WorkflowReference` (interface)

```ts
interface WorkflowReference<Params = Record<string, unknown>> {
    readonly __params?: Params;
    readonly binding?: string;
    readonly isLunoraWorkflow: true;
    readonly name?: string;
}
```

### `Workpool` (interface)

```ts
interface Workpool {
    cancel: (id: string) => Promise<{
        cancelled: boolean;
    }>;
    enqueue: <F extends SchedulableReference>(function_: F, args: ArgsOf<F>, options?: EnqueueOptions) => Promise<{
        id: string;
        scheduledFor: number;
    }>;
    readonly name: string;
    status: () => Promise<{
        inFlight: number;
        maxConcurrency: number;
        queued: number;
    }>;
}
```

### `WorkpoolOptions` (interface)

```ts
interface WorkpoolOptions extends LunoraSchedulerOptions {
    maxConcurrency: number;
    name?: string;
}
```

### `assertScheduleDelay` (const)

```ts
const assertScheduleDelay: (delayMs: number, surface: string, argument?: string) => void;
```

### `assertScheduleInstant` (const)

```ts
const assertScheduleInstant: (timestampMs: number, nowMs: number, surface: string) => void;
```

### `assertValidCronExpression` (const)

```ts
const assertValidCronExpression: (schedule: string, context?: string) => void;
```

### `compileCronSchedule` (const)

```ts
const compileCronSchedule: (kind: CronScheduleKind, schedule: DailySchedule | HourlySchedule | IntervalSchedule | MonthlySchedule | WeeklySchedule, jobName?: string) => string;
```

### `createCronTrigger` (const)

```ts
const createCronTrigger: (options: CronTriggerOptions) => CronTriggerSnippet;
```

### `createQueueConsumer` (const)

```ts
const createQueueConsumer: (options: QueueConsumerOptions) => ((batch: MessageBatchLike) => Promise<void>);
```

### `createQueueWorkpool` (const)

```ts
const createQueueWorkpool: (options: QueueWorkpoolOptions) => QueueWorkpool;
```

### `createScheduler` (const)

```ts
const createScheduler: (options: LunoraSchedulerOptions) => Scheduler;
```

### `createSchedulerHost` (const)

```ts
const createSchedulerHost: (options: SchedulerHostOptions) => SchedulerHost;
```

### `createWorkpool` (const)

```ts
const createWorkpool: (options: WorkpoolOptions) => Workpool;
```

### `cronJobs` (const)

```ts
const cronJobs: () => CronJobsBuilder;
```

### `httpDispatcher` (const)

```ts
const httpDispatcher: (options: HttpDispatcherOptions) => QueueDispatch;
```

### `isValidCronExpression` (const)

```ts
const isValidCronExpression: (schedule: string) => boolean;
```

### `isWorkflowReference` (const)

```ts
const isWorkflowReference: (target: unknown) => target is WorkflowReference;
```

### `resolveScheduleId` (const)

```ts
const resolveScheduleId: (requested: unknown) => string;
```

### `warnIfSecondsLeading` (const)

```ts
const warnIfSecondsLeading: (schedule: string, context: string) => void;
```
