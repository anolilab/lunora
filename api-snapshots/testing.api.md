# `@lunora/testing` — public API

- Package: `packages/testing`
- Tier: core

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/testing`

### `AgentHarness` (interface)

```ts
interface AgentHarness {
    readonly dispatches: ReadonlyArray<HarnessDispatch>;
    messages: (threadKey: string) => AgentMessageRow[];
    run: (params: AgentRunInput, overrides?: AgentRunOverrides) => Promise<AgentRunResult>;
    thread: (threadKey: string) => HarnessThread | undefined;
}
```

### `AgentHarnessOptions` (interface)

```ts
interface AgentHarnessOptions {
    env?: Record<string, unknown>;
    exportName?: string;
    functions?: Record<string, HarnessFunction>;
    script: AgentGenerateResult[];
}
```

### `AgentRunOverrides` (interface)

```ts
interface AgentRunOverrides {
    instanceId?: string;
    script?: AgentGenerateResult[];
}
```

### `EvalCase` (interface)

```ts
interface EvalCase {
    expected?: string;
    input: string;
    metadata?: Record<string, unknown>;
}
```

### `EvalItemResult` (interface)

```ts
interface EvalItemResult {
    average: number;
    input: string;
    output: string;
    scores: Record<string, ScoreResult>;
}
```

### `EvalResult` (interface)

```ts
interface EvalResult {
    average: number;
    items: EvalItemResult[];
}
```

### `FakeScheduledJob` (interface)

```ts
interface FakeScheduledJob extends ScheduledJob {
    args: Record<string, unknown>;
}
```

### `FakeSchedulerControls` (interface)

```ts
interface FakeSchedulerControls {
    advance: (ms: number, options?: SweepOptions) => Promise<number>;
    failures: () => ScheduledJobFailure[];
    list: () => FakeScheduledJob[];
    runPending: (options?: SweepOptions) => Promise<number>;
}
```

### `FunctionRegistry` (type)

```ts
type FunctionRegistry = Record<string, RegisteredAction<any, any> | RegisteredMutation<any, any> | RegisteredQuery<any, any>>;
```

### `HarnessDispatch` (interface)

```ts
interface HarnessDispatch {
    args: Record<string, unknown> | undefined;
    path: string;
}
```

### `HarnessMessage` (interface)

```ts
interface HarnessMessage extends AgentMessageRow {
    messageKey: string;
    threadKey: string;
}
```

### `HarnessThread` (interface)

```ts
interface HarnessThread {
    agent: string;
    error?: string;
    instanceId?: string;
    key: string;
    messageCount: number;
    owner?: string;
    status: string;
    title?: string;
    usage?: unknown;
}
```

### `InboxOptions` (interface)

Re-exported from `@lunora/mail` — signature tracked at its source.

### `LunoraTestOptions` (interface)

```ts
interface LunoraTestOptions {
    env?: Record<string, unknown>;
    fetch?: typeof globalThis.fetch;
    functions?: FunctionRegistry;
    now?: number;
}
```

### `ScheduledJobFailure` (interface)

```ts
interface ScheduledJobFailure {
    args: Record<string, unknown>;
    error: unknown;
    functionPath: string;
    id: string;
}
```

### `ScoreResult` (interface)

```ts
interface ScoreResult {
    reason?: string;
    score: number;
}
```

### `Scorer` (interface)

```ts
interface Scorer {
    name: string;
    score: (sample: ScorerSample) => Promise<ScoreResult | number> | ScoreResult | number;
}
```

### `ScorerSample` (interface)

```ts
interface ScorerSample {
    expected?: string;
    input?: string;
    metadata?: Record<string, unknown>;
    output: string;
}
```

### `SweepOptions` (interface)

```ts
interface SweepOptions {
    throwOnError?: boolean;
}
```

### `TestHarness` (interface)

```ts
interface TestHarness {
    action: {
        <A extends ArgsValidator, R>(reference: RegisteredAction<A, R>, args: InferArgs<A>): Promise<R>;
        <R>(inline: InlineActionFunction<R>): Promise<R>;
    };
    close: () => void;
    mutation: {
        <A extends ArgsValidator, R>(reference: RegisteredMutation<A, R>, args: InferArgs<A>): Promise<R>;
        <R>(inline: InlineMutationFunction<R>): Promise<R>;
    };
    query: {
        <A extends ArgsValidator, R>(reference: RegisteredQuery<A, R>, args: InferArgs<A>): Promise<R>;
        <R>(inline: InlineQueryFunction<R>): Promise<R>;
    };
    run: <R>(function_: InlineMutationFunction<R>) => Promise<R>;
    scheduler: FakeSchedulerControls;
    subscribe: {
        <A extends ArgsValidator, R>(reference: RegisteredQuery<A, R>, args: InferArgs<A>): TestSubscription<R>;
        <R>(inline: InlineQueryFunction<R>): TestSubscription<R>;
    };
    withIdentity: (identity: TestIdentity) => TestHarness;
}
```

### `TestIdentity` (interface)

```ts
interface TestIdentity extends Record<string, unknown> {
    userId?: null | string;
}
```

### `TestSubscription` (interface)

```ts
interface TestSubscription<R> extends AsyncIterable<R> {
    next: () => Promise<IteratorResult<R, R>>;
    return: () => Promise<IteratorResult<R, R>>;
}
```

### `WaitForMailOptions` (interface)

Re-exported from `@lunora/mail` — signature tracked at its source.

### `agentHarness` (const)

```ts
const agentHarness: (agent: AgentDefinition, options: AgentHarnessOptions) => AgentHarness;
```

### `containsScorer` (const)

```ts
const containsScorer: (needle: string, options?: {
    caseSensitive?: boolean;
}) => Scorer;
```

### `evaluate` (const)

```ts
const evaluate: (cases: ReadonlyArray<EvalCase>, produce: (input: string) => Promise<string> | string, scorers: ReadonlyArray<Scorer>) => Promise<EvalResult>;
```

### `exactMatchScorer` (const)

```ts
const exactMatchScorer: () => Scorer;
```

### `extractLink` (const)

Re-exported from `@lunora/mail` — signature tracked at its source.

### `finalTurn` (const)

```ts
const finalTurn: (text: string, extra?: Partial<AgentGenerateResult>) => AgentGenerateResult;
```

### `keywordScorer` (const)

```ts
const keywordScorer: (keywords: ReadonlyArray<string>) => Scorer;
```

### `listCapturedMail` (const)

Re-exported from `@lunora/mail` — signature tracked at its source.

### `llmScorer` (const)

```ts
const llmScorer: (options: {
    criteria: string;
    judge: (prompt: string) => Promise<string>;
    name?: string;
}) => Scorer;
```

### `lunoraTest` (const)

```ts
const lunoraTest: (schema: TestSchema, options?: LunoraTestOptions) => TestHarness;
```

### `regexScorer` (const)

```ts
const regexScorer: (pattern: RegExp, name?: string) => Scorer;
```

### `scoreSample` (const)

```ts
const scoreSample: (sample: ScorerSample, scorers: ReadonlyArray<Scorer>) => Promise<{
    average: number;
    scores: Record<string, ScoreResult>;
}>;
```

### `toolCallTurn` (const)

```ts
const toolCallTurn: (id: string, name: string, input: unknown, text?: string) => AgentGenerateResult;
```

### `waitForMail` (const)

Re-exported from `@lunora/mail` — signature tracked at its source.
