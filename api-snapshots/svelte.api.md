# `@lunora/svelte` — public API

- Package: `packages/svelte`
- Tier: stable-adapter

Generated by `pnpm run api:update` from the built `dist` types; verified by
`pnpm run api:check` (CI: Lint / api-surface). Do not edit by hand — a diff
here is a public-API change and must be reviewed as one (SemVer applies).

## `@lunora/svelte`

### `AgentApi` (interface)

```ts
interface AgentApi {
    agents: {
        agentThread: FunctionReference<"query", {
            key: string;
        }, Record<string, unknown> | undefined>;
    };
}
```

### `AgentChatApi` (interface)

```ts
interface AgentChatApi {
    agents: {
        agentMessages: AgentMessagesReference$1;
        agentResolveApproval: AgentApprovalReference;
        agentThread: AgentThreadReference;
    };
}
```

### `AgentChatHandle` (interface)

```ts
interface AgentChatHandle {
    approve: (toolCallId: string, note?: string) => Promise<void>;
    cancel: () => Promise<void>;
    messages: Readable<ReadonlyArray<AgentChatMessage>>;
    reject: (toolCallId: string, note?: string) => Promise<void>;
    send: (input: string, args?: Record<string, unknown>) => Promise<void>;
    status: Readable<AgentThreadStatus | undefined>;
    streamingText: Readable<string>;
    teardown: () => void;
}
```

### `AgentChatMessage` (interface)

```ts
interface AgentChatMessage {
    content: string;
    createdAt?: number;
    optimistic?: boolean;
    role: "assistant" | "system" | "tool" | "user";
    seq: number;
    status?: "approved" | "awaiting_approval" | "rejected";
    toolCallId?: string;
    toolCalls?: ReadonlyArray<{
        id: string;
        input: unknown;
        name: string;
    }>;
    toolName?: string;
}
```

### `AgentChatOptions` (interface)

```ts
interface AgentChatOptions {
    api: AgentChatApi;
    cancel?: FunctionReference<"mutation">;
    limit?: number;
    send: FunctionReference<"mutation">;
    sendArgs?: Record<string, unknown>;
    stream?: AgentTokenStreamReference;
    threadKey: string;
}
```

### `AgentHandle` (interface)

```ts
interface AgentHandle {
    cancel: () => Promise<void>;
    pending: Readable<boolean>;
    run: (input: string, args?: Record<string, unknown>) => Promise<void>;
    status: Readable<AgentThreadStatus | undefined>;
    teardown: () => void;
    thread: Readable<AgentThreadRecord | undefined>;
}
```

### `AgentLiveEvent` (type)

```ts
type AgentLiveEvent = AgentProgressEvent | AgentTokenDelta;
```

### `AgentOptions` (interface)

```ts
interface AgentOptions {
    api: AgentApi;
    cancel?: FunctionReference<"mutation">;
    run: FunctionReference<"mutation">;
    runArgs?: Record<string, unknown>;
    threadKey: string;
}
```

### `AgentProgressEvent` (interface)

```ts
interface AgentProgressEvent {
    data: unknown;
    kind: "progress";
    threadKey: string;
    toolCallId: string;
}
```

### `AgentStateApi` (interface)

```ts
interface AgentStateApi {
    agents: {
        agentState: FunctionReference<"query", {
            key: string;
        }, Record<string, unknown> | undefined>;
    };
}
```

### `AgentStateHandle` (interface)

```ts
interface AgentStateHandle<T> {
    error: Readable<Error | undefined>;
    state: Readable<T | undefined>;
}
```

### `AgentStateOptions` (interface)

```ts
interface AgentStateOptions {
    api: AgentStateApi;
    threadKey: string;
}
```

### `AgentThreadRecord` (interface)

```ts
interface AgentThreadRecord {
    createdAt?: number;
    error?: string;
    instanceId?: string;
    messageCount?: number;
    owner?: string;
    status: AgentThreadStatus;
    title?: string;
    updatedAt?: number;
}
```

### `AgentThreadStatus` (type)

```ts
type AgentThreadStatus = "awaiting_input" | "cancelled" | "error" | "idle" | "running";
```

### `AgentTokenDelta` (interface)

```ts
interface AgentTokenDelta {
    kind?: "token";
    text: string;
    threadKey: string;
    turn: number;
}
```

### `AgentToolEvent` (type)

```ts
type AgentToolEvent = {
    data: unknown;
    toolCallId: string;
    type: "progress";
} | {
    input: unknown;
    seq: number;
    toolCallId: string;
    toolName: string;
    type: "call";
} | {
    output: string;
    seq: number;
    status?: "approved" | "rejected";
    toolCallId?: string;
    toolName?: string;
    type: "result";
} | {
    seq: number;
    toolCallId?: string;
    toolName?: string;
    type: "awaiting-approval";
};
```

### `AgentToolEventsApi` (interface)

```ts
interface AgentToolEventsApi {
    agents: {
        agentMessages: AgentMessagesReference;
    };
}
```

### `AgentToolEventsHandle` (interface)

```ts
interface AgentToolEventsHandle {
    events: Readable<ReadonlyArray<AgentToolEvent>>;
}
```

### `AgentToolEventsOptions` (interface)

```ts
interface AgentToolEventsOptions {
    api: AgentToolEventsApi;
    limit?: number;
    stream?: AgentLiveStreamReference;
    threadKey: string;
}
```

### `ArgsOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `AuthStore` (interface)

```ts
interface AuthStore {
    setToken: (token: string | null) => void;
    token: Readable<string | null>;
    user: Readable<User | null>;
}
```

### `ConnectionStatus` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ConnectionStatusStore` (type)

```ts
type ConnectionStatusStore = Readable<ConnectionStatus>;
```

### `FlagContext` (type)

```ts
type FlagContext = Record<string, unknown>;
```

### `FlagValue` (type)

```ts
type FlagValue = boolean | number | string | {
    [key: string]: unknown;
} | unknown[] | null;
```

### `FunctionReference` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `HeartbeatReference` (type)

```ts
type HeartbeatReference = FunctionReference<"mutation", {
    data?: Record<string, unknown>;
    roomId: string;
    sessionId: string;
}>;
```

### `InfiniteQueryHandle` (interface)

```ts
interface InfiniteQueryHandle<T> {
    fetchNextPage: (numberItems?: number) => void;
    hasNextPage: Readable<boolean>;
    isFetchingNextPage: Readable<boolean>;
    isLoading: Readable<boolean>;
    pages: Readable<T[][]>;
    status: Readable<PaginationStatus>;
}
```

### `InfiniteQueryOptions` (interface)

```ts
interface InfiniteQueryOptions {
    initialNumItems: number;
    shardKey?: string;
}
```

### `ListPresentReference` (type)

```ts
type ListPresentReference = FunctionReference<"query", {
    roomId: string;
}>;
```

### `LunoraClient` (class)

Re-exported from `@lunora/client` — signature tracked at its source.

### `MutationCallOptions` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `MutationHandle` (interface)

```ts
interface MutationHandle<F extends FunctionReference> {
    data: Readable<ReturnOf<F> | undefined>;
    error: Readable<Error | undefined>;
    mutate: (args: ArgsOf<F>, options?: MutationCallOptions<unknown, unknown, ArgsOf<F>>) => Promise<ReturnOf<F>>;
    pending: Readable<boolean>;
    reset: () => void;
}
```

### `MutatorHandle` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `MutatorHandleStore` (interface)

```ts
interface MutatorHandleStore<TArgs> {
    error: Readable<Error | undefined>;
    isError: Readable<boolean>;
    mutate: (args: TArgs) => Promise<void>;
    pending: Readable<boolean>;
    reset: () => void;
}
```

### `MutatorTransaction` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `PageItemOf` (type)

```ts
type PageItemOf<F extends FunctionReference> = ReturnOf<F> extends {
    page: (infer T)[];
} ? T : unknown;
```

### `PaginatedArgs` (type)

```ts
type PaginatedArgs<F extends FunctionReference> = Omit<ArgsOf<F>, "paginationOpts">;
```

### `PaginatedQueryHandle` (interface)

```ts
interface PaginatedQueryHandle<T> {
    isLoading: Readable<boolean>;
    loadMore: (numberItems: number) => void;
    results: Readable<T[]>;
    status: Readable<PaginationStatus>;
}
```

### `PaginatedQueryOptions` (interface)

```ts
interface PaginatedQueryOptions {
    initialNumItems: number;
    shardKey?: string;
}
```

### `Preloaded` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `PresenceHandle` (interface)

```ts
interface PresenceHandle<L extends ListPresentReference> {
    present: Readable<ReturnOf<L> | undefined>;
    sessionId: string;
    setData: (data: Record<string, unknown> | undefined) => void;
    teardown: () => void;
}
```

### `PresenceOptions` (interface)

```ts
interface PresenceOptions<H extends HeartbeatReference, L extends ListPresentReference> {
    data?: Record<string, unknown>;
    heartbeat: H;
    intervalMs?: number;
    listPresent: L;
    sessionId?: string;
    shardKey?: string;
}
```

### `QueryStore` (type)

```ts
type QueryStore<F extends FunctionReference> = Readable<ReturnOf<F> | undefined>;
```

### `QueryStoreOptions` (interface)

```ts
interface QueryStoreOptions {
    onError?: SubscriptionErrorCallback;
    shardKey?: string;
}
```

### `RateLimitHandle` (interface)

```ts
interface RateLimitHandle {
    check: (count?: number) => boolean;
    consume: (count?: number) => RateLimitStatus;
    disabled: Readable<boolean>;
    ok: Readable<boolean>;
    reset: () => void;
    retryAfter: Readable<number>;
    teardown: () => void;
}
```

### `RateLimitOptions` (interface)

```ts
interface RateLimitOptions {
    now?: () => number;
    tickMs?: number;
}
```

### `ReturnOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `StreamHandle` (interface)

```ts
interface StreamHandle<T> {
    cancel: () => void;
    chunks: Readable<ReadonlyArray<T>>;
    error: Readable<Error | undefined>;
    status: Readable<StreamStatus>;
    teardown: () => void;
}
```

### `StreamStatus` (type)

```ts
type StreamStatus = "complete" | "error" | "idle" | "streaming";
```

### `StreamStoreOptions` (interface)

```ts
interface StreamStoreOptions {
    maxBuffer?: number;
    shardKey?: string;
}
```

### `SubscriptionHandle` (interface)

```ts
interface SubscriptionHandle<T> {
    data: Readable<T | undefined>;
    error: Readable<Error | undefined>;
}
```

### `SubscriptionStoreOptions` (interface)

```ts
interface SubscriptionStoreOptions {
    onError?: (error: Error) => void;
    shardKey?: string;
}
```

### `VoiceAgentHandle` (interface)

```ts
interface VoiceAgentHandle {
    audioLevel: Readable<number>;
    connected: Readable<boolean>;
    endCall: () => void;
    error: Readable<Error | undefined>;
    interimTranscript: Readable<string>;
    isMuted: Readable<boolean>;
    sendText: (text: string) => void;
    startCall: () => Promise<void>;
    status: Readable<VoiceStatus>;
    toggleMute: () => boolean;
    transcript: Readable<string>;
}
```

### `VoiceAgentOptions` (interface)

```ts
interface VoiceAgentOptions {
    createMicrophone?: CreateMicrophone;
    createSocket?: CreateSocket;
    createSpeaker?: CreateSpeaker;
    interruptChunks?: number;
    interruptThreshold?: number;
    silenceDurationMs?: number;
    silenceThreshold?: number;
    threadKey: string;
    voice: VoiceReference;
}
```

### `VoiceAudioFormat` (type)

```ts
type VoiceAudioFormat = "mp3" | "wav";
```

### `VoiceReference` (type)

```ts
type VoiceReference = FunctionReference<"stream", {
    threadKey: string;
}, Record<string, unknown>>;
```

### `VoiceStatus` (type)

```ts
type VoiceStatus = "idle" | "listening" | "speaking" | "thinking";
```

### `agent` (function)

```ts
function agent(options: AgentOptions): AgentHandle;

function agent(client: LunoraClient, options: AgentOptions): AgentHandle;
```

### `agentChat` (function)

```ts
function agentChat(options: AgentChatOptions): AgentChatHandle;

function agentChat(client: LunoraClient, options: AgentChatOptions): AgentChatHandle;
```

### `agentState` (function)

```ts
function agentState<T extends Record<string, unknown> = Record<string, unknown>>(options: AgentStateOptions): AgentStateHandle<T>;

function agentState<T extends Record<string, unknown> = Record<string, unknown>>(client: LunoraClient, options: AgentStateOptions): AgentStateHandle<T>;
```

### `agentToolEvents` (function)

```ts
function agentToolEvents(options: AgentToolEventsOptions): AgentToolEventsHandle;

function agentToolEvents(client: LunoraClient, options: AgentToolEventsOptions): AgentToolEventsHandle;
```

### `auth` (const)

```ts
const auth: (explicitClient?: ReturnType<typeof getLunoraClient>) => AuthStore;
```

### `connectionStatus` (const)

```ts
const connectionStatus: (client?: LunoraClient) => ConnectionStatusStore;
```

### `flag` (function)

```ts
function flag<T extends FlagValue>(key: string, defaultValue: T, context?: FlagContext): Readable<T>;

function flag<T extends FlagValue>(client: LunoraClient, key: string, defaultValue: T, context?: FlagContext): Readable<T>;
```

### `flags` (function)

```ts
function flags<T extends Record<string, FlagValue>>(flagDefaults: T, context?: FlagContext): Readable<T>;

function flags<T extends Record<string, FlagValue>>(client: LunoraClient, flagDefaults: T, context?: FlagContext): Readable<T>;
```

### `getLunoraClient` (const)

```ts
const getLunoraClient: () => LunoraClient;
```

### `hydratePreloaded` (const)

```ts
const hydratePreloaded: <T>(preloaded: Preloaded<T>, client?: LunoraClient) => Readable<T>;
```

### `infiniteQuery` (function)

```ts
function infiniteQuery<F extends FunctionReference>(function_: F, args: "skip" | PaginatedArgs<F>, options: InfiniteQueryOptions): InfiniteQueryHandle<PageItemOf<F>>;

function infiniteQuery<F extends FunctionReference>(client: LunoraClient, function_: F, args: "skip" | PaginatedArgs<F>, options: InfiniteQueryOptions): InfiniteQueryHandle<PageItemOf<F>>;
```

### `mutation` (function)

```ts
function mutation<F extends FunctionReference>(function_: F): MutationHandle<F>;

function mutation<F extends FunctionReference>(client: LunoraClient, function_: F): MutationHandle<F>;
```

### `mutator` (const)

```ts
const mutator: <TArgs = Record<string, unknown>>(handle: MutatorHandle<TArgs>) => MutatorHandleStore<TArgs>;
```

### `paginatedQuery` (function)

```ts
function paginatedQuery<F extends FunctionReference>(function_: F, args: "skip" | PaginatedArgs<F>, options: PaginatedQueryOptions): PaginatedQueryHandle<PageItemOf<F>>;

function paginatedQuery<F extends FunctionReference>(client: LunoraClient, function_: F, args: "skip" | PaginatedArgs<F>, options: PaginatedQueryOptions): PaginatedQueryHandle<PageItemOf<F>>;
```

### `presence` (function)

```ts
function presence<H extends HeartbeatReference, L extends ListPresentReference>(roomId: string, options: PresenceOptions<H, L>): PresenceHandle<L>;

function presence<H extends HeartbeatReference, L extends ListPresentReference>(client: LunoraClient, roomId: string, options: PresenceOptions<H, L>): PresenceHandle<L>;
```

### `query` (function)

```ts
function query<F extends FunctionReference>(function_: F, args: ArgsOf<F> | "skip", options?: QueryStoreOptions): QueryStore<F>;

function query<F extends FunctionReference>(client: LunoraClient, function_: F, args: ArgsOf<F> | "skip", options?: QueryStoreOptions): QueryStore<F>;
```

### `rateLimit` (const)

```ts
const rateLimit: (config: RateLimitConfig, options?: RateLimitOptions) => RateLimitHandle;
```

### `setLunoraClient` (const)

```ts
const setLunoraClient: (client: LunoraClient) => LunoraClient;
```

### `stream` (function)

```ts
function stream<F extends FunctionReference<"stream">>(function_: F, args: ArgsOf<F> | "skip", options?: StreamStoreOptions): StreamHandle<ReturnOf<F>>;

function stream<F extends FunctionReference<"stream">>(client: LunoraClient, function_: F, args: ArgsOf<F> | "skip", options?: StreamStoreOptions): StreamHandle<ReturnOf<F>>;
```

### `subscription` (function)

```ts
function subscription<F extends FunctionReference>(function_: F, args: ArgsOf<F> | "skip", options?: SubscriptionStoreOptions): SubscriptionHandle<ReturnOf<F>>;

function subscription<F extends FunctionReference>(client: LunoraClient, function_: F, args: ArgsOf<F> | "skip", options?: SubscriptionStoreOptions): SubscriptionHandle<ReturnOf<F>>;
```

### `voiceAgent` (function)

```ts
function voiceAgent(options: VoiceAgentOptions): VoiceAgentHandle;

function voiceAgent(client: LunoraClient, options: VoiceAgentOptions): VoiceAgentHandle;
```

## `@lunora/svelte/server`

### `ArgsOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `AuthLike` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `FunctionReference` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `HeadersSource` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `Preloaded` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ReturnOf` (type)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ServerClientOptions` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `ServerSession` (interface)

Re-exported from `@lunora/client` — signature tracked at its source.

### `createServerClient` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `deserializePreloaded` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `getServerSession` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `preloadQuery` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `preloadedQueryResult` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

### `serializePreloaded` (const)

Re-exported from `@lunora/client` — signature tracked at its source.

## `@lunora/svelte/worker`

### `ExecutionContextLike` (interface)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `LunoraWorkerOptions` (type)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `LunoraWorkerOptionsInput` (type)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `SvelteKitWorker` (type)

Re-exported from `@lunora/runtime` — signature tracked at its source.

### `withLunora` (const)

Re-exported from `@lunora/runtime` — signature tracked at its source.
