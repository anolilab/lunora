{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "outDir": "dist",
        "rootDir": "../..",
        "moduleResolution": "bundler",
        "types": ["node"],
        "noEmit": true
    },
    "include": [
        "**/*.ts",
        "../../.vis/templates/**/*.ts"
    ],
    "exclude": ["node_modules", "dist"]
}
