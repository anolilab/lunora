{
    "name": "@lunora/ai-evals-tests",
    "version": "0.0.0",
    "private": true,
    "description": "Behavioural eval set for the Studio's AI assistant engine (shared/ai-chat.ts), scored with @lunora/testing.",
    "type": "module",
    "sideEffects": false,
    "scripts": {
        "lint:types": "tsc --noEmit",
        "test": "vitest run"
    },
    "devDependencies": {
        "@lunora/cli": "workspace:*",
        "@lunora/testing": "workspace:*",
        "@types/node": "catalog:types",
        "typescript": "catalog:tsc",
        "vitest": "catalog:test"
    },
    "engines": {
        "node": "^22.15.0 || >=24.11.0"
    }
}
