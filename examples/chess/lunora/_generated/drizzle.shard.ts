// GENERATED by @lunora/codegen — do not edit.
// Run `lunora codegen` to regenerate.

import { index, integer, real, sqliteTable, text, uniqueIndex } from "lunorash/server/drizzle";
import type { AnySQLiteColumn } from "lunorash/server/drizzle";

export const profiles = sqliteTable("profiles", {
    _id: text("_id").primaryKey(),
    _creationTime: integer("_creationTime").notNull(),
    userId: text("userId").notNull(),
    displayName: text("displayName").notNull(),
    rating: real("rating").notNull(),
    gamesPlayed: real("gamesPlayed").notNull(),
    gamesWon: real("gamesWon").notNull(),
}, (t) => ({
    by_rating: index("by_rating").on(t.rating),
    by_user: uniqueIndex("by_user").on(t.userId),
}));

export const lobbies = sqliteTable("lobbies", {
    _id: text("_id").primaryKey(),
    _creationTime: integer("_creationTime").notNull(),
    hostId: text("hostId").notNull(),
    guestId: text("guestId", { mode: "json" }).$type<string | null>(),
    isPrivate: integer("isPrivate", { mode: "boolean" }).notNull(),
    isOpen: integer("isOpen", { mode: "boolean" }).notNull(),
    inviteCode: text("inviteCode"),
    createdAt: real("createdAt").notNull(),
    gameId: text("gameId").references((): AnySQLiteColumn => games._id),
}, (t) => ({
    by_open_public: index("by_open_public").on(t.isPrivate, t.isOpen, t.createdAt),
    by_invite_code: index("by_invite_code").on(t.inviteCode),
    by_guest: index("by_guest").on(t.guestId),
    by_host: uniqueIndex("by_host").on(t.hostId),
}));

export const games = sqliteTable("games", {
    _id: text("_id").primaryKey(),
    _creationTime: integer("_creationTime").notNull(),
    status: text("status", { mode: "json" }).$type<"active" | "completed" | "abandoned">().notNull(),
    whiteId: text("whiteId").notNull(),
    blackId: text("blackId").notNull(),
    position: text("position").notNull(),
    result: text("result", { mode: "json" }).$type<"white_wins" | "black_wins" | "draw">(),
    winnerId: text("winnerId"),
    moveCount: real("moveCount").notNull(),
    startedAt: real("startedAt").notNull(),
    endedAt: real("endedAt"),
    drawOfferedBy: text("drawOfferedBy", { mode: "json" }).$type<string | null>(),
}, (t) => ({
    by_black: index("by_black").on(t.blackId),
    by_white: index("by_white").on(t.whiteId),
    by_status: index("by_status").on(t.status),
}));

export const moves = sqliteTable("moves", {
    _id: text("_id").primaryKey(),
    _creationTime: integer("_creationTime").notNull(),
    gameId: text("gameId").references((): AnySQLiteColumn => games._id).notNull(),
    playerId: text("playerId").notNull(),
    turnNumber: real("turnNumber").notNull(),
    notation: text("notation").notNull(),
    from: text("from").notNull(),
    to: text("to").notNull(),
    captured: text("captured"),
    special: text("special"),
}, (t) => ({
    by_game_turn: uniqueIndex("by_game_turn").on(t.gameId, t.turnNumber),
}));

export const ratelimit_buckets = sqliteTable("ratelimit_buckets", {
    _id: text("_id").primaryKey(),
    _creationTime: integer("_creationTime").notNull(),
    key: text("key").notNull(),
    value: real("value").notNull(),
    ts: real("ts").notNull(),
    prev: real("prev"),
}, (t) => ({
    by_key: index("by_key").on(t.key),
}));
