// GENERATED by @lunora/codegen — do not edit.
// Run `lunora codegen` to regenerate.

import * as lunora_games_0 from "../games.js";
import * as lunora_lobby_1 from "../lobby.js";
import * as lunora_players_2 from "../players.js";

import { DEFER_VALIDATION as DEFER, installCompiledValidatorMap } from "lunorash/values";
import { LunoraError } from "lunorash/server";
import type { ActionCtx, MutationCtx, QueryCtx } from "./server.js";
import type { Id } from "./dataModel.js";

/**
 * Single registered function, narrowed to the shape `handleRpc` needs.
 * The real argument validators / return types are checked elsewhere — at
 * the dispatch site we erase to `unknown` so a single map can host every
 * registered function.
 */
export interface RegisteredLunoraFunction {
    kind: "action" | "mutation" | "query" | "stream";
    args: Record<string, unknown>;
    /**
     * Present on a `"stream"` declared `durable`: its run is persisted and
     * survives the socket that opened it. Absent on every other kind, and on an
     * ephemeral stream.
     */
    durable?: { ttlMs?: number };
    /**
     * For `"action" | "mutation" | "query"` the handler is awaited and its result returned.
     * For `"stream"` the handler returns an `AsyncIterable` synchronously and takes an
     * `AbortSignal` as a third argument — the runtime drives it frame-by-frame.
     */
    handler: ((context: unknown, args: Record<string, unknown>) => Promise<unknown> | unknown) | ((context: unknown, args: Record<string, unknown>, signal?: AbortSignal) => AsyncIterable<unknown>);
    /**
     * The lifecycle moment a hook fires on, when this registration came from
     * `onConnect`/`onDisconnect`/`onShardInit`/`onQueryChange`. Read at
     * dispatch to decide whether the function runs system-trusted: `init` and
     * `reactor` have no caller identity, so RLS has no user to scope to.
     */
    lifecycle?: "connect" | "disconnect" | "init" | "reactor";
    /** `"internal"` functions are rejected on the external RPC path; absence === public. */
    visibility?: "internal" | "public";
    /**
     * `.x402({ price })` tag on a paid public procedure. The origin worker
     * paywalls it; the shard refuses to subscribe it (`isPaidFunction`).
     */
    x402?: { readonly price: number | string };
}

/**
 * Static dispatch table. The key matches the `__lunoraRef` the client
 * emits (`api[namespace][fn].__lunoraRef === "namespace:fn"`).
 */
export const LUNORA_FUNCTIONS: Record<string, RegisteredLunoraFunction> = {
    "games:get": lunora_games_0.get as unknown as RegisteredLunoraFunction,
    "games:listActive": lunora_games_0.listActive as unknown as RegisteredLunoraFunction,
    "games:makeMove": lunora_games_0.makeMove as unknown as RegisteredLunoraFunction,
    "games:mine": lunora_games_0.mine as unknown as RegisteredLunoraFunction,
    "games:moves": lunora_games_0.moves as unknown as RegisteredLunoraFunction,
    "games:offerDraw": lunora_games_0.offerDraw as unknown as RegisteredLunoraFunction,
    "games:resign": lunora_games_0.resign as unknown as RegisteredLunoraFunction,
    "games:respondToDraw": lunora_games_0.respondToDraw as unknown as RegisteredLunoraFunction,
    "games:start": lunora_games_0.start as unknown as RegisteredLunoraFunction,
    "lobby:create": lunora_lobby_1.create as unknown as RegisteredLunoraFunction,
    "lobby:join": lunora_lobby_1.join as unknown as RegisteredLunoraFunction,
    "lobby:joinByCode": lunora_lobby_1.joinByCode as unknown as RegisteredLunoraFunction,
    "lobby:leave": lunora_lobby_1.leave as unknown as RegisteredLunoraFunction,
    "lobby:listOpen": lunora_lobby_1.listOpen as unknown as RegisteredLunoraFunction,
    "lobby:mine": lunora_lobby_1.mine as unknown as RegisteredLunoraFunction,
    "lobby:quickMatch": lunora_lobby_1.quickMatch as unknown as RegisteredLunoraFunction,
    "players:claim": lunora_players_2.claim as unknown as RegisteredLunoraFunction,
    "players:leaderboard": lunora_players_2.leaderboard as unknown as RegisteredLunoraFunction,
    "players:list": lunora_players_2.list as unknown as RegisteredLunoraFunction,
    "players:me": lunora_players_2.me as unknown as RegisteredLunoraFunction,
};

/**
 * AOT-compiled argument validators (Worker-safe, no `eval`). Each is installed
 * onto its function's live `.args` object and consulted by the interpreted
 * parser as a zero-allocation fast path; anything it can't model is deferred.
 */
installCompiledValidatorMap(lunora_games_0.get.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
return { "gameId": source["gameId"] };
});
installCompiledValidatorMap(lunora_games_0.makeMove.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
if (typeof source["from"] !== "string") return DEFER;
if (typeof source["to"] !== "string") return DEFER;
let __has1 = false;
let __val1;
if (source["promotion"] !== undefined) {
if (typeof source["promotion"] !== "string") return DEFER;
__val1 = source["promotion"];
__has1 = true;
}
return { "gameId": source["gameId"], "from": source["from"], "to": source["to"], ...(__has1 ? { "promotion": __val1 } : {}) };
});
installCompiledValidatorMap(lunora_games_0.moves.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
return { "gameId": source["gameId"] };
});
installCompiledValidatorMap(lunora_games_0.offerDraw.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
return { "gameId": source["gameId"] };
});
installCompiledValidatorMap(lunora_games_0.resign.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
return { "gameId": source["gameId"] };
});
installCompiledValidatorMap(lunora_games_0.respondToDraw.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["gameId"] !== "string") return DEFER;
if (typeof source["accept"] !== "boolean") return DEFER;
return { "gameId": source["gameId"], "accept": source["accept"] };
});
installCompiledValidatorMap(lunora_games_0.start.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["lobbyId"] !== "string") return DEFER;
return { "lobbyId": source["lobbyId"] };
});
installCompiledValidatorMap(lunora_lobby_1.create.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["isPrivate"] !== "boolean") return DEFER;
return { "isPrivate": source["isPrivate"] };
});
installCompiledValidatorMap(lunora_lobby_1.join.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["lobbyId"] !== "string") return DEFER;
let __has1 = false;
let __val1;
if (source["inviteCode"] !== undefined) {
if (typeof source["inviteCode"] !== "string") return DEFER;
__val1 = source["inviteCode"];
__has1 = true;
}
return { "lobbyId": source["lobbyId"], ...(__has1 ? { "inviteCode": __val1 } : {}) };
});
installCompiledValidatorMap(lunora_lobby_1.joinByCode.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["inviteCode"] !== "string") return DEFER;
return { "inviteCode": source["inviteCode"] };
});
installCompiledValidatorMap(lunora_lobby_1.leave.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["lobbyId"] !== "string") return DEFER;
return { "lobbyId": source["lobbyId"] };
});
installCompiledValidatorMap(lunora_players_2.claim.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
let __has1 = false;
let __val1;
if (source["displayName"] !== undefined) {
if (typeof source["displayName"] !== "string") return DEFER;
__val1 = source["displayName"];
__has1 = true;
}
return { ...(__has1 ? { "displayName": __val1 } : {}) };
});

/**
 * Lifecycle manifest: the function paths the generated ShardDO dispatches when a
 * client's WebSocket connects (`connect`) or disconnects (`disconnect`), once
 * per Durable Object instance before any handler runs (`init`), and after a
 * write flush when a watched read's result changed (`reactor`). Each path also
 * resolves through {@link LUNORA_FUNCTIONS}. The socket sides run under the
 * socket's verified identity; `init` and `reactor` have no caller, so they run
 * anonymous — all via system dispatch.
 */
export const LUNORA_LIFECYCLE_HOOKS: { connect: readonly string[]; disconnect: readonly string[]; init: readonly string[]; reactor: readonly string[] } = {
    connect: [],
    disconnect: [],
    init: [],
    reactor: [],
};

/**
 * Resolve and invoke a registered function from an external caller. Throws a
 * LunoraError-shaped object (404) when the path is unknown — the runtime's
 * structural error mapper turns that into the right HTTP status. Internal
 * functions are treated as not-found so their existence never leaks to clients.
 */
export const dispatchLunoraFunction = async (functionPath: string, context: unknown, args: Record<string, unknown>): Promise<unknown> => {
    const registered = LUNORA_FUNCTIONS[functionPath];

    if (!registered || registered.visibility === "internal") {
        throw new LunoraError("FUNCTION_NOT_FOUND", `function not registered: ${functionPath}`);
    }

    return registered.handler(context, args);
};

/**
 * A handler context accepted by {@link createCaller}. The runtime context the
 * shard DO builds is uniform across kinds, so any handler's `ctx` works here.
 */
export type CallerCtx = ActionCtx | MutationCtx | QueryCtx;

/**
 * Typed, in-process server-to-server caller. Every registered function — public
 * *and* internal — is reachable; each call dispatches against the same shard
 * with the supplied `context`, exactly like `ctx.runQuery`/`runMutation`/
 * `runAction` but without re-stating the function path.
 */
export interface Caller {
    games: {
        get: (args: { gameId: Id<"games"> }) => Promise<import("./dataModel.js").Doc_games | null>;
        listActive: (args?: {}) => Promise<import("./dataModel.js").Doc_games[]>;
        makeMove: (args: { gameId: Id<"games">; from: string; to: string; promotion?: string }) => Promise<{ finished: boolean; }>;
        mine: (args?: {}) => Promise<import("./dataModel.js").Doc_games[]>;
        moves: (args: { gameId: Id<"games"> }) => Promise<import("./dataModel.js").Doc_moves[]>;
        offerDraw: (args: { gameId: Id<"games"> }) => Promise<void>;
        resign: (args: { gameId: Id<"games"> }) => Promise<void>;
        respondToDraw: (args: { gameId: Id<"games">; accept: boolean }) => Promise<void>;
        start: (args: { lobbyId: Id<"lobbies"> }) => Promise<Id<"games">>;
    };
    lobby: {
        create: (args: { isPrivate: boolean }) => Promise<Id<"lobbies">>;
        join: (args: { lobbyId: Id<"lobbies">; inviteCode?: string }) => Promise<void>;
        joinByCode: (args: { inviteCode: string }) => Promise<Id<"lobbies">>;
        leave: (args: { lobbyId: Id<"lobbies"> }) => Promise<void>;
        listOpen: (args?: {}) => Promise<import("./dataModel.js").Doc_lobbies[]>;
        mine: (args?: {}) => Promise<(import("./dataModel.js").Doc_lobbies & { isHost: boolean; }) | null>;
        quickMatch: (args?: {}) => Promise<Id<"lobbies">>;
    };
    players: {
        claim: (args: { displayName?: string }) => Promise<void>;
        leaderboard: (args?: {}) => Promise<import("./dataModel.js").Doc_profiles[]>;
        list: (args?: {}) => Promise<import("./dataModel.js").Doc_profiles[]>;
        me: (args?: {}) => Promise<import("./dataModel.js").Doc_profiles | null>;
    };
}

const callRegistered = async <R>(context: CallerCtx, functionPath: string, args: Record<string, unknown> | undefined): Promise<R> => {
    const registered = LUNORA_FUNCTIONS[functionPath];

    if (!registered) {
        throw new LunoraError("FUNCTION_NOT_FOUND", `function not registered: ${functionPath}`);
    }

    return (await registered.handler(context, args ?? {})) as R;
};

/** Build a {@link Caller} bound to `context` (typically a handler's `ctx`). */
export const createCaller = (context: CallerCtx): Caller => ({
    games: {
        get: (args) => callRegistered(context, "games:get", args),
        listActive: (args) => callRegistered(context, "games:listActive", args),
        makeMove: (args) => callRegistered(context, "games:makeMove", args),
        mine: (args) => callRegistered(context, "games:mine", args),
        moves: (args) => callRegistered(context, "games:moves", args),
        offerDraw: (args) => callRegistered(context, "games:offerDraw", args),
        resign: (args) => callRegistered(context, "games:resign", args),
        respondToDraw: (args) => callRegistered(context, "games:respondToDraw", args),
        start: (args) => callRegistered(context, "games:start", args),
    },
    lobby: {
        create: (args) => callRegistered(context, "lobby:create", args),
        join: (args) => callRegistered(context, "lobby:join", args),
        joinByCode: (args) => callRegistered(context, "lobby:joinByCode", args),
        leave: (args) => callRegistered(context, "lobby:leave", args),
        listOpen: (args) => callRegistered(context, "lobby:listOpen", args),
        mine: (args) => callRegistered(context, "lobby:mine", args),
        quickMatch: (args) => callRegistered(context, "lobby:quickMatch", args),
    },
    players: {
        claim: (args) => callRegistered(context, "players:claim", args),
        leaderboard: (args) => callRegistered(context, "players:leaderboard", args),
        list: (args) => callRegistered(context, "players:list", args),
        me: (args) => callRegistered(context, "players:me", args),
    },
});

/**
 * Single registered data migration, narrowed to the shape the per-shard runner
 * consumes. `up`/`down` are erased to a structural transform; the authoring
 * validation lives in `defineMigration`.
 */
export interface RegisteredDataMigration {
    batchSize?: number;
    down?: (document: Record<string, unknown>) => Record<string, unknown> | undefined;
    id: string;
    table: string;
    up: (document: Record<string, unknown>) => Record<string, unknown> | undefined;
}

/**
 * Registry of online data migrations keyed by `defineMigration`'s `id`. The
 * shard DO's admin RPC and the CLI resolve migrations to run through this map.
 */
export const LUNORA_MIGRATIONS: Record<string, RegisteredDataMigration> = {};
