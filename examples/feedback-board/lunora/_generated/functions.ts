// GENERATED by @lunora/codegen — do not edit.
// Run `lunora codegen` to regenerate.

import * as lunora_feedback_0 from "../feedback.js";
import * as lunora_summaries_1 from "../summaries.js";

import { DEFER_VALIDATION as DEFER, installCompiledValidatorMap } from "lunorash/values";
import { LunoraError } from "lunorash/server";
import type { ActionCtx, MutationCtx, QueryCtx } from "./server.js";
import type { Id } from "./dataModel.js";

/**
 * Single registered function, narrowed to the shape `handleRpc` needs.
 * The real argument validators / return types are checked elsewhere — at
 * the dispatch site we erase to `unknown` so a single map can host every
 * registered function.
 */
export interface RegisteredLunoraFunction {
    kind: "action" | "mutation" | "query" | "stream";
    args: Record<string, unknown>;
    /**
     * Present on a `"stream"` declared `durable`: its run is persisted and
     * survives the socket that opened it. Absent on every other kind, and on an
     * ephemeral stream.
     */
    durable?: { ttlMs?: number };
    /**
     * For `"action" | "mutation" | "query"` the handler is awaited and its result returned.
     * For `"stream"` the handler returns an `AsyncIterable` synchronously and takes an
     * `AbortSignal` as a third argument — the runtime drives it frame-by-frame.
     */
    handler: ((context: unknown, args: Record<string, unknown>) => Promise<unknown> | unknown) | ((context: unknown, args: Record<string, unknown>, signal?: AbortSignal) => AsyncIterable<unknown>);
    /**
     * The lifecycle moment a hook fires on, when this registration came from
     * `onConnect`/`onDisconnect`/`onShardInit`/`onQueryChange`. Read at
     * dispatch to decide whether the function runs system-trusted: `init` and
     * `reactor` have no caller identity, so RLS has no user to scope to.
     */
    lifecycle?: "connect" | "disconnect" | "init" | "reactor";
    /** `"internal"` functions are rejected on the external RPC path; absence === public. */
    visibility?: "internal" | "public";
    /**
     * `.x402({ price })` tag on a paid public procedure. The origin worker
     * paywalls it; the shard refuses to subscribe it (`isPaidFunction`).
     */
    x402?: { readonly price: number | string };
}

/**
 * Static dispatch table. The key matches the `__lunoraRef` the client
 * emits (`api[namespace][fn].__lunoraRef === "namespace:fn"`).
 */
export const LUNORA_FUNCTIONS: Record<string, RegisteredLunoraFunction> = {
    "feedback:addComment": lunora_feedback_0.addComment as unknown as RegisteredLunoraFunction,
    "feedback:comments": lunora_feedback_0.comments as unknown as RegisteredLunoraFunction,
    "feedback:create": lunora_feedback_0.create as unknown as RegisteredLunoraFunction,
    "feedback:get": lunora_feedback_0.get as unknown as RegisteredLunoraFunction,
    "feedback:list": lunora_feedback_0.list as unknown as RegisteredLunoraFunction,
    "feedback:myVotes": lunora_feedback_0.myVotes as unknown as RegisteredLunoraFunction,
    "feedback:remove": lunora_feedback_0.remove as unknown as RegisteredLunoraFunction,
    "feedback:setStatus": lunora_feedback_0.setStatus as unknown as RegisteredLunoraFunction,
    "feedback:toggleVote": lunora_feedback_0.toggleVote as unknown as RegisteredLunoraFunction,
    "summaries:generate": lunora_summaries_1.generate as unknown as RegisteredLunoraFunction,
    "summaries:list": lunora_summaries_1.list as unknown as RegisteredLunoraFunction,
    "summaries:remove": lunora_summaries_1.remove as unknown as RegisteredLunoraFunction,
    "summaries:store": lunora_summaries_1.store as unknown as RegisteredLunoraFunction,
};

/**
 * AOT-compiled argument validators (Worker-safe, no `eval`). Each is installed
 * onto its function's live `.args` object and consulted by the interpreted
 * parser as a zero-allocation fast path; anything it can't model is deferred.
 */
installCompiledValidatorMap(lunora_feedback_0.addComment.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["feedbackId"] !== "string") return DEFER;
if (typeof source["authorName"] !== "string") return DEFER;
let __has1 = false;
let __val1;
if (source["authorEmail"] !== undefined) {
if (typeof source["authorEmail"] !== "string") return DEFER;
__val1 = source["authorEmail"];
__has1 = true;
}
if (typeof source["content"] !== "string") return DEFER;
let __has2 = false;
let __val2;
if (source["isOfficial"] !== undefined) {
if (typeof source["isOfficial"] !== "boolean") return DEFER;
__val2 = source["isOfficial"];
__has2 = true;
}
return { "feedbackId": source["feedbackId"], "authorName": source["authorName"], ...(__has1 ? { "authorEmail": __val1 } : {}), "content": source["content"], ...(__has2 ? { "isOfficial": __val2 } : {}) };
});
installCompiledValidatorMap(lunora_feedback_0.comments.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["feedbackId"] !== "string") return DEFER;
return { "feedbackId": source["feedbackId"] };
});
installCompiledValidatorMap(lunora_feedback_0.create.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["title"] !== "string") return DEFER;
if (typeof source["description"] !== "string") return DEFER;
if (typeof source["authorName"] !== "string") return DEFER;
let __has1 = false;
let __val1;
if (source["authorEmail"] !== undefined) {
if (typeof source["authorEmail"] !== "string") return DEFER;
__val1 = source["authorEmail"];
__has1 = true;
}
let __has3 = false;
let __val3;
if (source["tags"] !== undefined) {
if (!Array.isArray(source["tags"])) return DEFER;
const __arr2 = new Array(source["tags"].length);
for (let __i2 = 0; __i2 < source["tags"].length; __i2++) {
const __e2 = source["tags"][__i2];
if (typeof __e2 !== "string") return DEFER;
__arr2[__i2] = __e2;
}
__val3 = __arr2;
__has3 = true;
}
return { "title": source["title"], "description": source["description"], "authorName": source["authorName"], ...(__has1 ? { "authorEmail": __val1 } : {}), ...(__has3 ? { "tags": __val3 } : {}) };
});
installCompiledValidatorMap(lunora_feedback_0.get.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["id"] !== "string") return DEFER;
return { "id": source["id"] };
});
installCompiledValidatorMap(lunora_feedback_0.myVotes.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["voterEmail"] !== "string") return DEFER;
return { "voterEmail": source["voterEmail"] };
});
installCompiledValidatorMap(lunora_feedback_0.remove.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["id"] !== "string") return DEFER;
return { "id": source["id"] };
});
installCompiledValidatorMap(lunora_feedback_0.toggleVote.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["feedbackId"] !== "string") return DEFER;
if (typeof source["voterEmail"] !== "string") return DEFER;
return { "feedbackId": source["feedbackId"], "voterEmail": source["voterEmail"] };
});
installCompiledValidatorMap(lunora_summaries_1.generate.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
let __has1 = false;
let __val1;
if (source["limit"] !== undefined) {
if (typeof source["limit"] !== "number" || !Number.isFinite(source["limit"])) return DEFER;
__val1 = source["limit"];
__has1 = true;
}
return { ...(__has1 ? { "limit": __val1 } : {}) };
});
installCompiledValidatorMap(lunora_summaries_1.remove.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["id"] !== "string") return DEFER;
return { "id": source["id"] };
});
installCompiledValidatorMap(lunora_summaries_1.store.args, (source) => {
if (typeof source !== "object" || source === null || Array.isArray(source)) return DEFER;
if (Object.getPrototypeOf(source) !== Object.prototype && Object.getPrototypeOf(source) !== null) return DEFER;
if (typeof source["title"] !== "string") return DEFER;
if (typeof source["summary"] !== "string") return DEFER;
if (!Array.isArray(source["feedbackIds"])) return DEFER;
const __arr1 = new Array(source["feedbackIds"].length);
for (let __i1 = 0; __i1 < source["feedbackIds"].length; __i1++) {
const __e1 = source["feedbackIds"][__i1];
if (typeof __e1 !== "string") return DEFER;
__arr1[__i1] = __e1;
}
return { "title": source["title"], "summary": source["summary"], "feedbackIds": __arr1 };
});

/**
 * Lifecycle manifest: the function paths the generated ShardDO dispatches when a
 * client's WebSocket connects (`connect`) or disconnects (`disconnect`), once
 * per Durable Object instance before any handler runs (`init`), and after a
 * write flush when a watched read's result changed (`reactor`). Each path also
 * resolves through {@link LUNORA_FUNCTIONS}. The socket sides run under the
 * socket's verified identity; `init` and `reactor` have no caller, so they run
 * anonymous — all via system dispatch.
 */
export const LUNORA_LIFECYCLE_HOOKS: { connect: readonly string[]; disconnect: readonly string[]; init: readonly string[]; reactor: readonly string[] } = {
    connect: [],
    disconnect: [],
    init: [],
    reactor: [],
};

/**
 * Resolve and invoke a registered function from an external caller. Throws a
 * LunoraError-shaped object (404) when the path is unknown — the runtime's
 * structural error mapper turns that into the right HTTP status. Internal
 * functions are treated as not-found so their existence never leaks to clients.
 */
export const dispatchLunoraFunction = async (functionPath: string, context: unknown, args: Record<string, unknown>): Promise<unknown> => {
    const registered = LUNORA_FUNCTIONS[functionPath];

    if (!registered || registered.visibility === "internal") {
        throw new LunoraError("FUNCTION_NOT_FOUND", `function not registered: ${functionPath}`);
    }

    return registered.handler(context, args);
};

/**
 * A handler context accepted by {@link createCaller}. The runtime context the
 * shard DO builds is uniform across kinds, so any handler's `ctx` works here.
 */
export type CallerCtx = ActionCtx | MutationCtx | QueryCtx;

/**
 * Typed, in-process server-to-server caller. Every registered function — public
 * *and* internal — is reachable; each call dispatches against the same shard
 * with the supplied `context`, exactly like `ctx.runQuery`/`runMutation`/
 * `runAction` but without re-stating the function path.
 */
export interface Caller {
    feedback: {
        addComment: (args: { feedbackId: Id<"feedback">; authorName: string; authorEmail?: string; content: string; isOfficial?: boolean }) => Promise<Id<"comments">>;
        comments: (args: { feedbackId: Id<"feedback"> }) => Promise<import("./dataModel.js").Doc_comments[]>;
        create: (args: { title: string; description: string; authorName: string; authorEmail?: string; tags?: Array<string> }) => Promise<Id<"feedback">>;
        get: (args: { id: Id<"feedback"> }) => Promise<import("./dataModel.js").Doc_feedback | null>;
        list: (args: { status?: "open" | "under-review" | "planned" | "in-progress" | "completed" | "closed"; sortBy?: "votes" | "recent" }) => Promise<import("./dataModel.js").Doc_feedback[]>;
        myVotes: (args: { voterEmail: string }) => Promise<Id<"feedback">[]>;
        remove: (args: { id: Id<"feedback"> }) => Promise<void>;
        setStatus: (args: { id: Id<"feedback">; status: "open" | "under-review" | "planned" | "in-progress" | "completed" | "closed" }) => Promise<void>;
        toggleVote: (args: { feedbackId: Id<"feedback">; voterEmail: string }) => Promise<{ voted: boolean; }>;
    };
    summaries: {
        generate: (args: { limit?: number }) => Promise<Id<"summaries"> | null>;
        list: (args?: {}) => Promise<import("./dataModel.js").Doc_summaries[]>;
        remove: (args: { id: Id<"summaries"> }) => Promise<void>;
        store: (args: { title: string; summary: string; feedbackIds: Array<Id<"feedback">> }) => Promise<Id<"summaries">>;
    };
}

const callRegistered = async <R>(context: CallerCtx, functionPath: string, args: Record<string, unknown> | undefined): Promise<R> => {
    const registered = LUNORA_FUNCTIONS[functionPath];

    if (!registered) {
        throw new LunoraError("FUNCTION_NOT_FOUND", `function not registered: ${functionPath}`);
    }

    return (await registered.handler(context, args ?? {})) as R;
};

/** Build a {@link Caller} bound to `context` (typically a handler's `ctx`). */
export const createCaller = (context: CallerCtx): Caller => ({
    feedback: {
        addComment: (args) => callRegistered(context, "feedback:addComment", args),
        comments: (args) => callRegistered(context, "feedback:comments", args),
        create: (args) => callRegistered(context, "feedback:create", args),
        get: (args) => callRegistered(context, "feedback:get", args),
        list: (args) => callRegistered(context, "feedback:list", args),
        myVotes: (args) => callRegistered(context, "feedback:myVotes", args),
        remove: (args) => callRegistered(context, "feedback:remove", args),
        setStatus: (args) => callRegistered(context, "feedback:setStatus", args),
        toggleVote: (args) => callRegistered(context, "feedback:toggleVote", args),
    },
    summaries: {
        generate: (args) => callRegistered(context, "summaries:generate", args),
        list: (args) => callRegistered(context, "summaries:list", args),
        remove: (args) => callRegistered(context, "summaries:remove", args),
        store: (args) => callRegistered(context, "summaries:store", args),
    },
});

/**
 * Single registered data migration, narrowed to the shape the per-shard runner
 * consumes. `up`/`down` are erased to a structural transform; the authoring
 * validation lives in `defineMigration`.
 */
export interface RegisteredDataMigration {
    batchSize?: number;
    down?: (document: Record<string, unknown>) => Record<string, unknown> | undefined;
    id: string;
    table: string;
    up: (document: Record<string, unknown>) => Record<string, unknown> | undefined;
}

/**
 * Registry of online data migrations keyed by `defineMigration`'s `id`. The
 * shard DO's admin RPC and the CLI resolve migrations to run through this map.
 */
export const LUNORA_MIGRATIONS: Record<string, RegisteredDataMigration> = {};
