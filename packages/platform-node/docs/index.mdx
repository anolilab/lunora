---
title: "@lunora/platform-node"
description: "The Node host for the @lunora/platform contracts — experimental, no lunora dev --target node yet, and no SemVer promise until it graduates."
---

`@lunora/platform-node` is the Node **host**: the `@lunora/platform` contracts
(`ShardHost`, `SocketHost`, `ShardDirectory`, `ShardKvStore`, `SchedulerHost`)
implemented over `better-sqlite3` and an in-process registry, plus `.global()`
tables over `@lunora/sql-store`, workflows over `@visulima/workflow`, and object
storage on the local filesystem.

```ts
import { createNodePlatform } from "@lunora/platform-node";

const platform = createNodePlatform({ path: "./shard.sqlite3" });

// …or with the emulated tiers bound. Each is a declaration: omit it and the
// corresponding member is `undefined` rather than an empty host.
const full = createNodePlatform({
    globalTablesPath: "./.lunora/global.sqlite3", // → platform.globalTables, its own file
    objectStorageDirectory: "./.lunora/bucket", // → platform.objectStorage
    onQueueBatch: dispatchQueueBatch, // required alongside `queues`
    path: "./shard.sqlite3",
    queues: { emails }, // → platform.queues
    workflows: { orderPipeline }, // → platform.workflows, runs in the same SQLite file
});
```

## Status

**Experimental, and not a shipping target.** There is no `lunora dev --target
node`; this package ships no dev server. A `@lunora/config` deploy driver
(`node`) does exist, but `provision` is a reporting step, not a writing one: it
tells you, once, which declared features this target cannot serve, and it has
no toolchain to shell out to (no `wrangler`-equivalent), because there is
nothing remote to deploy to, tail logs from, or store secrets in.

Its API snapshot is gated at the **experimental** tier
(`scripts/api-snapshot.js`): a diff here is a public-API change and must be
reviewed as one. This package is Experimental: the snapshot records how its surface
moves, and carries **no SemVer promise until the package graduates**
(`ROADMAP.md`'s [graduation bar](https://github.com/anolilab/lunora/blob/alpha/ROADMAP.md#experimental--stable-the-graduation-bar)).
`scripts/check-roadmap-tiers.js` fails the install if the roadmap and the
snapshot tier ever disagree.

Read the table below as an honest self-rating, not an invitation to run
production traffic on it.

## Capability matrix

Reproduced from `NODE_CAPABILITIES` in `@lunora/platform`
(`packages/platform/src/capabilities.ts`). This is the same matrix codegen
gates `ctx.*` surfaces on, not a second copy of it.
`pnpm run lint:node-capabilities-docs` fails if this table and the source
disagree.

| Feature                   | Level         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shardedState`            | `emulated`    | One better-sqlite3 database per shard key, one process — no distributed placement or failover. Shard keys are percent-encoded into basenames with A-Z escaped, so `Tenant` and `tenant` stay two databases on a case-insensitive volume (APFS, NTFS) rather than folding into one. There is also no input gate: Cloudflare defers every other dispatch for the span of a mutation, whereas this host can only refuse — SQL issued from another task while a transaction is open throws a retryable `SHARD_UNAVAILABLE` (503) rather than reading rows that are about to roll back, so a read that merely arrived mid-mutation is retried instead of failing the request                                                                                                                                                                                                                                                                         |
| `globalTables`            | `emulated`    | The @lunora/sql-store core on its own SQLite file via the reference sqliteDialect — full store semantics, but one node with no replication                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `websocketHibernation`    | `emulated`    | Socket registry with attachments/tags persisted to SQLite, so subscription state survives a process restart; nothing is ever actually evicted from memory, so this is durability without hibernation's memory saving                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `durableStreams`          | `unsupported` | The transcript store is host-neutral (@lunora/shard-engine), but the attach/produce state machine lives in @lunora/do and nothing in this host mounts it. Gate-bearing: codegen refuses an app that declares a durable stream on this target, rather than emitting one that silently behaves as an ephemeral stream                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `localSql`                | `native`      | better-sqlite3 (synchronous, embedded)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `commitOrderedTables`     | `emulated`    | The sequence orders commits correctly, but the serialization it depends on is Lunora's per-shard write gate rather than a platform property — one process, one better-sqlite3 handle per shard key. Correct here; not something the host guarantees the way a Durable Object does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `memoryTables`            | `emulated`    | Same shape as Cloudflare and for a different reason: better-sqlite3 CAN open `:memory:`, but a shard's memory tables share the one handle its durable tables use, so they are cleared rather than never written. A host process also outlives far more than a Durable Object does, so cold starts — and therefore `onShardInit` — are much rarer here than in production on Cloudflare; do not use this target to judge how often a memory table is actually empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `serverReactors`          | `emulated`    | Same engine-level implementation as Cloudflare; the per-shard serialization it depends on is the host's own write gate rather than a platform guarantee                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `shardAlarms`             | `emulated`    | setTimeout over a durable row, dispatched to onAlarm and re-armed on construction, so an alarm survives a restart and one whose time elapsed while the process was down fires late rather than never. Delivery is at-least-once as it is on workerd: a handler that throws is re-delivered with exponential backoff (6 attempts, from 100ms) and then abandoned, and an alarm set or deleted inside a transaction is armed only if that transaction commits                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `shardPlacement`          | `unsupported` | One process — every shard lives where the process does, so a location hint has nowhere to place it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `shardReadReplicas`       | `unsupported` | One process and one region: a replica here would be a second copy of a database already on the same disk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `crossShardFanout`        | `emulated`    | @lunora/runtime's query coordinator over the in-process shard registry; listShardKeys is seeded from the shard files on disk, and answers every shard rather than only those holding the table (a correct superset, at the cost of visiting shards with nothing to say)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `queues`                  | `emulated`    | createNodeQueueHost (@lunora/platform-node) — a QueueBindingLike producer per declared queue over a durable \_lunora_queue_messages table, and a batched consumer feeding the same dispatchQueueBatch the Cloudflare host uses. delaySeconds (capped at 12h), all four content types, maxBatchSize/maxBatchTimeout assembly, per-message ack/retry with workerd's implicit-ack-on-return and retry-on-throw, maxRetries into a declared deadLetterQueue (or parked in place, never dropped), and a visibility window so a crash mid-handler redelivers. Delivery is driven by poll(); there is no timer, because this host has no dev server to own one. mode: "pull" queues are written but not consumed — nothing here serves the HTTP pull endpoint                                                                                                                                                                                          |
| `workflows`               | `emulated`    | createNodeWorkflowHost (@lunora/platform-node) compiles defineWorkflow handlers onto the @visulima/workflow engine (createRuntime): step/sleep/waitForEvent are durable + replay-safe, status maps to complete/errored/waiting/terminated, create({ id }) is honoured through a durable alias row (so ctx.spawn resolves and a retried create is one run), and runs survive a restart when backed by createNodeWorkflowStore (a SQLite WorkflowStore; the store is required, so no caller silently gets in-process-only state). terminate is a barrier within the process: a terminated run's writes are dropped, so an activation already in flight cannot overwrite the tombstone — it is not a barrier across processes, which would need the lease rather than a set. Gaps: no pause/restart; ctx.run dispatches to an endpoint no Node HTTP server serves; ctx.parallel's synchronous join cannot interleave within one trigger activation |
| `scheduler`               | `emulated`    | SQLite job table dispatched to onDispatch and re-armed on construction, with retry backoff and a dead-letter queue. It is also the only host implementing runtime cron registration (SchedulerHost.cron), which Cloudflare cannot offer — but nothing walks an app's DECLARED crons into that method, which is why cronTriggers is rated separately and unsupported here. This rating covers the imperative surface only: ctx.scheduler.runAfter/runAt do dispatch on this host                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `cronTriggers`            | `unsupported` | No runtime walks the generated LUNORA_CRONS map into SchedulerHost.cron, so the conformance suite is that method's only caller and a declared cron does not fire on this host. Gate-bearing: codegen refuses an app that declares one here rather than letting it deploy green and never run. Schedule the work explicitly with ctx.scheduler.runAfter/runAt instead                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `agents`                  | `unsupported` | Nothing here mounts the generated agent classes: createNodeWorkflowHost compiles defineWorkflow handlers onto the @visulima/workflow engine, and an agent is a generated WorkflowEntrypoint resolved off an AGENT\_ prefixed env binding this host never provides. The loop's inference has no home either — ai is unsupported on this target                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `objectStorageBackups`    | `emulated`    | The commands work unchanged, but the bucket underneath is createNodeR2Bucket — a directory on the same machine the CLI runs on, so a bucket-backed backup here is not the separate failure domain it is on Cloudflare. The scheduled half additionally needs this host's scheduler, which exists but is not a shipping target                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `objectStorageCdcArchive` | `emulated`    | createNodeR2Bucket implements the `startAfter` seek the segment index needs, so the read-back behaves as it does on R2. Same caveat as the backups above: the bucket is a directory on the machine running the host, so archiving the changelog here moves it off SQLite but not off the disk that would take the shard with it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `objectStorage`           | `emulated`    | createNodeR2Bucket (@lunora/platform-node) — an R2BucketLike over the local filesystem (fs/promises, head/list/range). One file per object with the metadata in a trailer, so the single rename that publishes the bytes publishes their checksum and content-type with them, and a get reads body and metadata through one handle rather than reopening the path. put streams into the staged file and .body streams the requested range; .arrayBuffer()/.text() still allocate the range they return. The body is single-use, as R2's is. Keys are percent-escaped per path segment (`%`, `A-Z`, `:`, and a trailing `.` or space), so `A` and `a` stay two objects on a case-insensitive volume exactly as they are on R2, and a lowercase key still maps to a byte-identical filename. No multipart uploads, no presigned URLs                                                                                                              |
| `keyValueStore`           | `emulated`    | better-sqlite3 table behind the ShardKvStore API — not a dedicated KV product                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `vectorStore`             | `unsupported` | No Vectorize-equivalent binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ai`                      | `unsupported` | No Workers AI-equivalent binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `browser`                 | `unsupported` | No headless-browser binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `images`                  | `unsupported` | No Images-equivalent binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `containers`              | `unsupported` | No container orchestration implemented, so there is nothing for ctx.containers.<name>.exec to run a command in either                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `analytics`               | `unsupported` | No Analytics Engine-equivalent binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `pipelines`               | `unsupported` | No Pipelines-equivalent binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `mail`                    | `unsupported` | The queue tier this host lacked when the rating was written now exists (createNodeQueueHost), but nothing here composes a @lunora/mail transport or the queued-send consumer, so a send would be accepted and never delivered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `secrets`                 | `unsupported` | No Secrets Store-equivalent binding implemented (a real host would likely map this to env vars). Gate-bearing, and it has to be: ctx.secrets is a core built-in spliced into every context, so codegen refuses an app that reads it on this target instead of emitting a surface that throws on first use                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `hyperdrive`              | `unsupported` | No connection-pooling binding implemented                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `httpCache`               | `unsupported` | Nothing sits in front of this host to cache its responses, and Node exposes no Web Cache API global — the runtime's REST edge cache finds no HttpCacheLike here and degrades to emitting Cache-Control alone, which browsers and any CDN in front still honour                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `identityProxy`           | `unsupported` | Nothing sits in front of this host to authenticate callers, so it never populates the execution context's access identity. @lunora/cloudflare-access still works here through its Cf-Access-Jwt-Assertion fallback, which is a plain header check and needs no host support                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

Almost nothing here is rated `native`, and that is the matrix's own definition
doing its job rather than a hedge: `native` means the platform itself provides
the feature, and a bare Node process provides essentially none of them.
`localSql` is the exception, because SQLite genuinely is the platform primitive
there.

## What it is for today

Dev and test. It exists to run the same conformance suite
(`@lunora/platform/conformance`, `@lunora/shard-engine/conformance`) against a
second host so portability is a construction check rather than a claim, and it
is a useful in-process double for exercising app code without Cloudflare
credentials. It is not wired into `lunora dev`, is not a deploy target, and the
capabilities above (one process, no distributed placement or failover, no
Cloudflare product bindings) mean it is not a substitute for the Cloudflare
host in anything that needs them.

## Known divergences

**`node-kv-store`'s serializer is not Cloudflare's, and is not even a stable
wire format on its own.** `ShardKvStore.put`'s contract says a value must be
structured-clonable and hosts serialize it durably, without pinning a wire
format. Cloudflare's `state.storage` uses the platform's own structured-clone
implementation. This host stores values as `node:v8`'s `serialize`/`deserialize`
output, the same structured-clone algorithm V8 uses for `postMessage` and
`structuredClone` itself, so it round-trips `Date`, `Map`, `Set`, `RegExp`, and
typed arrays, genuinely closer to Cloudflare's fidelity than `JSON.stringify`
would be. But `node:v8`'s format is explicitly documented by Node as **not** a
stable cross-version wire format: a value written under one Node/V8 version is
not guaranteed to `deserialize` under another. A SQLite file carried across a
Node upgrade can fail to read back values it wrote. See
`packages/platform-node/src/node-kv-store.ts` and
[`plans/234-node-host-findings.md`](https://github.com/anolilab/lunora/blob/alpha/plans/234-node-host-findings.md).
