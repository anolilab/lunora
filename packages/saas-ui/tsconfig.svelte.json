{
    // The Svelte port, checked by `svelte-check` (it compiles `.svelte` and
    // type-checks the result; plain `tsc` cannot read the files at all).
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "jsx": "preserve",
        "types": ["node"],
        // Svelte 5 runes (`$props`, `$state`, `$derived`) come from these ambient types.
        "verbatimModuleSyntax": false
    },
    "include": ["src/svelte/**/*", "src/core/**/*", "__tests__/svelte/**/*"],
    "exclude": []
}
