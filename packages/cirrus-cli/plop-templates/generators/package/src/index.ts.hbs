/**
 * @cirrus/{{dashCase name}} — {{description}}
 *
 * Replace this stub with your public API. Anything exported from this file
 * is what consumers of `@cirrus/{{dashCase name}}` will see.
 */
export const VERSION = "0.0.0";
