// Minimal wrangler config used ONLY by `@cloudflare/vitest-pool-workers`
// to boot the test Worker in a real workerd runtime.
{
    "$schema": "../../../../node_modules/wrangler/config-schema.json",
    "name": "cirrus-runtime-test-worker",
    "main": "test-worker.ts",
    "compatibility_date": "2026-04-07",
    "compatibility_flags": ["nodejs_compat"],
    "durable_objects": {
        "bindings": [{ "name": "SHARD", "class_name": "TestShardDO" }],
    },
    "migrations": [{ "tag": "v1", "new_sqlite_classes": ["TestShardDO"] }],
}
