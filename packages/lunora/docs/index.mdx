---
title: "lunora"
description: "Unscoped umbrella \u2014 one install for the base packages (server, values, runtime, do, client) via subpaths, plus the lunora CLI bin."
---

`lunora` — Unscoped umbrella — one install for the base packages (server, values, runtime, do, client) via subpaths, plus the lunora CLI bin.

> Reference documentation for this package is in progress. In the meantime, see
> the [getting started guide](/docs/getting-started) and the
> [concepts](/docs/concepts/schema) section.
