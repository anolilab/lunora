{
    "name": "lunora-builder",
    "tags": ["type:app", "category:builder"],
    "targets": {
        "lint:eslint": {
            "cache": true,
            "inputs": ["default", "{workspaceRoot}/eslint.config.js"],
            "command": "eslint ."
        },
        "lint:eslint:fix": {
            "cache": true,
            "inputs": ["default", "{workspaceRoot}/eslint.config.js"],
            "command": "eslint . --fix"
        }
    }
}
