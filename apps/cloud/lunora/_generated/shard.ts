// GENERATED by @lunora/codegen — do not edit.
// Run `lunora codegen` to regenerate.

import type { AdvisoryFinding, DatabaseWriterLike, DataMigrationLike, LogSink, MaskPoliciesResult, MigrationRunResult, RunShardApplyCdcArgs, RunShardMigrationArgs, RlsPoliciesResult, RunShardRankBeforeArgs, RunShardRankPageArgs, RunShardWriteArgs, RunShardWriteResult, SchedulerLike, SchemaLike, ShardDOState, ShardRankPageResult, SqlExec, StorageRulesResult, StudioFeaturesResult, SystemReaderStorageLike } from "@lunora/do";
import { applyCdcChanges, createShardCtxDb, runDataMigration, runShardMigrations, serveRelationFanout, ShardDO as ShardDOBase } from "@lunora/do";
import { asBucketStorage, createSecrets } from "@lunora/server";
import { bindOrm, bindTableFacade } from "@lunora/server";
import type { LunoraDatabaseLike as LunoraPaymentDbLike, LunoraPayment, PaymentsFromContextOptions } from "@lunora/payment";
import { paymentsFromContext } from "@lunora/payment";

import schema from "../schema.js";
import { LUNORA_FUNCTIONS, LUNORA_LIFECYCLE_HOOKS, LUNORA_MIGRATIONS } from "./functions.js";

type FunctionKind = "action" | "mutation" | "query";

interface FunctionReference {
    __lunoraRef: string;
}

/** Foreign-key columns per table (`v.id("target")` fields) for the data browser. */
const LUNORA_TABLE_REFS: Record<string, Record<string, string>> = {
    "organizations": {
        "cellId": "cells"
    },
    "members": {
        "organizationId": "organizations"
    },
    "projects": {
        "organizationId": "organizations"
    },
    "deployments": {
        "organizationId": "organizations",
        "projectId": "projects"
    },
    "deployKeys": {
        "organizationId": "organizations",
        "projectId": "projects"
    },
    "githubInstallations": {
        "organizationId": "organizations"
    },
    "builds": {
        "organizationId": "organizations",
        "projectId": "projects"
    },
    "buildLogs": {
        "buildId": "builds",
        "organizationId": "organizations"
    },
    "domains": {
        "organizationId": "organizations",
        "projectId": "projects"
    },
    "auditLog": {
        "organizationId": "organizations"
    },
    "invitations": {
        "organizationId": "organizations"
    },
    "platformUsage": {
        "deploymentId": "deployments",
        "organizationId": "organizations"
    },
    "secrets": {
        "organizationId": "organizations",
        "projectId": "projects"
    }
};

/** Declared indexes per table (secondary, search, rank, vector) for the schema viewer. */
const LUNORA_TABLE_INDEXES: Record<string, Array<{ fields: string[]; name: string; type: "index" | "rank" | "search" | "vector"; unique?: boolean }>> = {
    "cells": [
        {
            "fields": [
                "name"
            ],
            "name": "by_name",
            "type": "index",
            "unique": true
        }
    ],
    "organizations": [
        {
            "fields": [
                "slug"
            ],
            "name": "by_slug",
            "type": "index",
            "unique": true
        }
    ],
    "members": [
        {
            "fields": [
                "organizationId",
                "userId"
            ],
            "name": "by_org_user",
            "type": "index"
        }
    ],
    "projects": [
        {
            "fields": [
                "organizationId",
                "slug"
            ],
            "name": "by_org_slug",
            "type": "index",
            "unique": true
        },
        {
            "fields": [
                "githubRepo"
            ],
            "name": "by_github_repo",
            "type": "index"
        }
    ],
    "deployments": [
        {
            "fields": [
                "scriptName"
            ],
            "name": "by_script",
            "type": "index"
        },
        {
            "fields": [
                "projectId"
            ],
            "name": "by_project",
            "type": "index"
        },
        {
            "fields": [
                "kind"
            ],
            "name": "by_kind",
            "type": "index"
        },
        {
            "fields": [
                "alias"
            ],
            "name": "by_alias",
            "type": "index"
        }
    ],
    "deployKeys": [
        {
            "fields": [
                "organizationId"
            ],
            "name": "by_org",
            "type": "index"
        },
        {
            "fields": [
                "hashedKey"
            ],
            "name": "by_hash",
            "type": "index",
            "unique": true
        }
    ],
    "githubInstallations": [
        {
            "fields": [
                "organizationId"
            ],
            "name": "by_org",
            "type": "index"
        },
        {
            "fields": [
                "installationId"
            ],
            "name": "by_installation",
            "type": "index",
            "unique": true
        }
    ],
    "builds": [
        {
            "fields": [
                "projectId",
                "commitSha"
            ],
            "name": "by_project_commit",
            "type": "index"
        },
        {
            "fields": [
                "projectId"
            ],
            "name": "by_project",
            "type": "index"
        }
    ],
    "buildLogs": [
        {
            "fields": [
                "buildId"
            ],
            "name": "by_build",
            "type": "index"
        }
    ],
    "domains": [
        {
            "fields": [
                "projectId"
            ],
            "name": "by_project",
            "type": "index"
        },
        {
            "fields": [
                "hostname"
            ],
            "name": "by_hostname",
            "type": "index",
            "unique": true
        }
    ],
    "auditLog": [
        {
            "fields": [
                "organizationId"
            ],
            "name": "by_org",
            "type": "index"
        }
    ],
    "invitations": [
        {
            "fields": [
                "tokenHash"
            ],
            "name": "by_token",
            "type": "index",
            "unique": true
        },
        {
            "fields": [
                "organizationId"
            ],
            "name": "by_org",
            "type": "index"
        }
    ],
    "platformUsage": [
        {
            "fields": [
                "organizationId"
            ],
            "name": "by_org",
            "type": "index"
        }
    ],
    "secrets": [
        {
            "fields": [
                "projectId",
                "name"
            ],
            "name": "by_project_name",
            "type": "index",
            "unique": true
        },
        {
            "fields": [
                "projectId"
            ],
            "name": "by_project",
            "type": "index"
        }
    ],
    "customers": [
        {
            "fields": [
                "referenceId"
            ],
            "name": "by_reference",
            "type": "index"
        },
        {
            "fields": [
                "provider",
                "providerCustomerId"
            ],
            "name": "by_provider_customer",
            "type": "index",
            "unique": true
        }
    ],
    "events": [
        {
            "fields": [
                "provider",
                "providerEventId"
            ],
            "name": "by_provider_event",
            "type": "index",
            "unique": true
        }
    ],
    "paymentSessions": [
        {
            "fields": [
                "referenceId"
            ],
            "name": "by_reference",
            "type": "index"
        },
        {
            "fields": [
                "provider",
                "providerSessionId"
            ],
            "name": "by_provider_session",
            "type": "index",
            "unique": true
        }
    ],
    "subscriptions": [
        {
            "fields": [
                "referenceId"
            ],
            "name": "by_reference",
            "type": "index"
        },
        {
            "fields": [
                "provider",
                "providerSubscriptionId"
            ],
            "name": "by_provider_subscription",
            "type": "index",
            "unique": true
        }
    ],
    "usageEvents": [
        {
            "fields": [
                "referenceId",
                "featureId"
            ],
            "name": "by_reference_feature",
            "type": "index"
        },
        {
            "fields": [
                "provider",
                "idempotencyKey"
            ],
            "name": "by_idempotency",
            "type": "index",
            "unique": true
        }
    ]
};

/** Columns per table (typed, with PK/FK markers) for the studio's schema diagram, served via `__lunora_admin__:describeTable`. */
const LUNORA_TABLE_COLUMNS: Record<string, Array<{ isStorage?: boolean; name: string; optional: boolean; pk?: boolean; ref?: string; type: string }>> = {
    "cells": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "cloudflareAccountId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "dispatchNamespacePrefix",
            "optional": false,
            "type": "string"
        },
        {
            "name": "jurisdiction",
            "optional": true,
            "type": "string"
        },
        {
            "name": "name",
            "optional": false,
            "type": "string"
        },
        {
            "name": "status",
            "optional": false,
            "type": "any"
        }
    ],
    "organizations": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "cellId",
            "optional": false,
            "type": "id",
            "ref": "cells"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "name",
            "optional": false,
            "type": "string"
        },
        {
            "name": "slug",
            "optional": false,
            "type": "string"
        },
        {
            "name": "spendCapMinor",
            "optional": true,
            "type": "number"
        },
        {
            "name": "suspendedAt",
            "optional": true,
            "type": "number"
        }
    ],
    "members": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "role",
            "optional": false,
            "type": "any"
        },
        {
            "name": "userId",
            "optional": false,
            "type": "string"
        }
    ],
    "projects": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "activeDeploymentId",
            "optional": true,
            "type": "string"
        },
        {
            "name": "activeScriptName",
            "optional": true,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "framework",
            "optional": true,
            "type": "string"
        },
        {
            "name": "githubRepo",
            "optional": true,
            "type": "string"
        },
        {
            "name": "name",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "slug",
            "optional": false,
            "type": "string"
        }
    ],
    "deployments": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "adminToken",
            "optional": true,
            "type": "string"
        },
        {
            "name": "alias",
            "optional": true,
            "type": "string"
        },
        {
            "name": "branch",
            "optional": true,
            "type": "string"
        },
        {
            "name": "cronSpecs",
            "optional": true,
            "type": "array"
        },
        {
            "name": "bundleHash",
            "optional": true,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdBy",
            "optional": false,
            "type": "string"
        },
        {
            "name": "expiresAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "kind",
            "optional": false,
            "type": "any"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "projectId",
            "optional": false,
            "type": "id",
            "ref": "projects"
        },
        {
            "name": "scriptName",
            "optional": false,
            "type": "string"
        },
        {
            "name": "status",
            "optional": false,
            "type": "any"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "url",
            "optional": true,
            "type": "string"
        },
        {
            "name": "version",
            "optional": true,
            "type": "number"
        },
        {
            "name": "queuedAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "provisioningAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "verifyingAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "liveAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "supersededAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "failedAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "destroyedAt",
            "optional": true,
            "type": "number"
        }
    ],
    "deployKeys": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "hashedKey",
            "optional": false,
            "type": "string"
        },
        {
            "name": "lastUsedAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "name",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "projectId",
            "optional": true,
            "type": "id",
            "ref": "projects"
        },
        {
            "name": "revokedAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "type",
            "optional": false,
            "type": "any"
        }
    ],
    "githubInstallations": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "accountLogin",
            "optional": false,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "installationId",
            "optional": false,
            "type": "number"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        }
    ],
    "builds": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "branch",
            "optional": false,
            "type": "string"
        },
        {
            "name": "bundleHash",
            "optional": true,
            "type": "string"
        },
        {
            "name": "commitSha",
            "optional": false,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "deploymentId",
            "optional": true,
            "type": "string"
        },
        {
            "name": "error",
            "optional": true,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "processingBy",
            "optional": true,
            "type": "string"
        },
        {
            "name": "processingStartedAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "projectId",
            "optional": false,
            "type": "id",
            "ref": "projects"
        },
        {
            "name": "status",
            "optional": false,
            "type": "union"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "buildingAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "successfulAt",
            "optional": true,
            "type": "number"
        },
        {
            "name": "failedAt",
            "optional": true,
            "type": "number"
        }
    ],
    "buildLogs": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "buildId",
            "optional": false,
            "type": "id",
            "ref": "builds"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "level",
            "optional": false,
            "type": "union"
        },
        {
            "name": "line",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        }
    ],
    "domains": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "customHostnameId",
            "optional": true,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "hostname",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "projectId",
            "optional": false,
            "type": "id",
            "ref": "projects"
        },
        {
            "name": "redirectStatusCode",
            "optional": true,
            "type": "number"
        },
        {
            "name": "redirectTo",
            "optional": true,
            "type": "string"
        },
        {
            "name": "txtToken",
            "optional": false,
            "type": "string"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "verifiedAt",
            "optional": true,
            "type": "number"
        }
    ],
    "auditLog": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "action",
            "optional": false,
            "type": "string"
        },
        {
            "name": "actorUserId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "target",
            "optional": true,
            "type": "string"
        }
    ],
    "invitations": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "email",
            "optional": false,
            "type": "string"
        },
        {
            "name": "expiresAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "invitedBy",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "role",
            "optional": false,
            "type": "any"
        },
        {
            "name": "status",
            "optional": false,
            "type": "union"
        },
        {
            "name": "tokenHash",
            "optional": false,
            "type": "string"
        }
    ],
    "platformUsage": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "deploymentId",
            "optional": true,
            "type": "id",
            "ref": "deployments"
        },
        {
            "name": "kind",
            "optional": false,
            "type": "union"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "periodStart",
            "optional": false,
            "type": "number"
        },
        {
            "name": "quantity",
            "optional": false,
            "type": "number"
        }
    ],
    "secrets": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "ciphertext",
            "optional": false,
            "type": "string"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "iv",
            "optional": false,
            "type": "string"
        },
        {
            "name": "name",
            "optional": false,
            "type": "string"
        },
        {
            "name": "organizationId",
            "optional": false,
            "type": "id",
            "ref": "organizations"
        },
        {
            "name": "projectId",
            "optional": false,
            "type": "id",
            "ref": "projects"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        }
    ],
    "customers": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "email",
            "optional": true,
            "type": "string"
        },
        {
            "name": "provider",
            "optional": false,
            "type": "string"
        },
        {
            "name": "providerCustomerId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "referenceId",
            "optional": false,
            "type": "string"
        }
    ],
    "events": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "processedAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "provider",
            "optional": false,
            "type": "string"
        },
        {
            "name": "providerEventId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "type",
            "optional": false,
            "type": "string"
        }
    ],
    "paymentSessions": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "amountMinor",
            "optional": false,
            "type": "bigint"
        },
        {
            "name": "capturedMinor",
            "optional": false,
            "type": "bigint"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "currency",
            "optional": false,
            "type": "string"
        },
        {
            "name": "provider",
            "optional": false,
            "type": "string"
        },
        {
            "name": "providerSessionId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "referenceId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "refundedMinor",
            "optional": false,
            "type": "bigint"
        },
        {
            "name": "state",
            "optional": false,
            "type": "string"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        }
    ],
    "subscriptions": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "cancelAtPeriodEnd",
            "optional": false,
            "type": "boolean"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "currentPeriodEnd",
            "optional": true,
            "type": "number"
        },
        {
            "name": "currentPeriodStart",
            "optional": true,
            "type": "number"
        },
        {
            "name": "priceId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "provider",
            "optional": false,
            "type": "string"
        },
        {
            "name": "providerSubscriptionId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "quantity",
            "optional": false,
            "type": "number"
        },
        {
            "name": "referenceId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "state",
            "optional": false,
            "type": "string"
        },
        {
            "name": "updatedAt",
            "optional": false,
            "type": "number"
        }
    ],
    "usageEvents": [
        {
            "name": "_id",
            "optional": false,
            "pk": true,
            "type": "id"
        },
        {
            "name": "_creationTime",
            "optional": false,
            "type": "number"
        },
        {
            "name": "createdAt",
            "optional": false,
            "type": "number"
        },
        {
            "name": "featureId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "idempotencyKey",
            "optional": false,
            "type": "string"
        },
        {
            "name": "provider",
            "optional": false,
            "type": "string"
        },
        {
            "name": "quantity",
            "optional": false,
            "type": "number"
        },
        {
            "name": "referenceId",
            "optional": false,
            "type": "string"
        },
        {
            "name": "reportedToProvider",
            "optional": false,
            "type": "boolean"
        }
    ]
};

/** Storage-key columns per table (`v.storage(...)` fields) for the file browser's records↔files join. */
const LUNORA_STORAGE_COLUMNS: Record<string, string[]> = {};

/** Static schema advisories (computed by @lunora/advisor at codegen time) served via `__lunora_admin__:getAdvisories`. */
const LUNORA_ADVISORIES: AdvisoryFinding[] = [
    {
        "cacheKey": "duplicate_index:builds:by_project",
        "categories": [
            "PERFORMANCE"
        ],
        "description": "A secondary index is redundant because another index already covers every lookup it serves (its columns are a leading prefix of the other's). The redundant index costs storage and is maintained on every write for no read benefit.",
        "detail": "Index \"by_project\" on table \"builds\" (projectId) is redundant — index \"by_project_commit\" (projectId, commitSha) already covers its lookups.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "coveredBy": {
                "fields": [
                    "projectId",
                    "commitSha"
                ],
                "name": "by_project_commit"
            },
            "fields": [
                "projectId"
            ],
            "index": "by_project",
            "table": "builds"
        },
        "name": "duplicate_index",
        "remediation": "Drop the redundant index; the covering index already serves its lookups.",
        "title": "Duplicate / redundant index"
    },
    {
        "cacheKey": "duplicate_index:secrets:by_project",
        "categories": [
            "PERFORMANCE"
        ],
        "description": "A secondary index is redundant because another index already covers every lookup it serves (its columns are a leading prefix of the other's). The redundant index costs storage and is maintained on every write for no read benefit.",
        "detail": "Index \"by_project\" on table \"secrets\" (projectId) is redundant — index \"by_project_name\" (projectId, name) already covers its lookups.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "coveredBy": {
                "fields": [
                    "projectId",
                    "name"
                ],
                "name": "by_project_name"
            },
            "fields": [
                "projectId"
            ],
            "index": "by_project",
            "table": "secrets"
        },
        "name": "duplicate_index",
        "remediation": "Drop the redundant index; the covering index already serves its lookups.",
        "title": "Duplicate / redundant index"
    },
    {
        "cacheKey": "table_without_insert:customers",
        "categories": [
            "SCHEMA"
        ],
        "description": "No function inserts into this table via `ctx.db.insert(\"<table>\", …)`. It may be read-only by design (seeded by a migration, replicated, or written through a path the advisor can't see) — or it may be dead schema.",
        "detail": "No function calls `ctx.db.insert(\"customers\", …)` — table \"customers\" has no discovered insert path.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "table": "customers"
        },
        "name": "table_without_insert",
        "remediation": "If the table should be writable, add a mutation that calls `ctx.db.insert(\"<table>\", …)`. If it is read-only or seeded elsewhere, this advisory can be ignored.",
        "title": "Table has no insert path"
    },
    {
        "cacheKey": "table_without_insert:events",
        "categories": [
            "SCHEMA"
        ],
        "description": "No function inserts into this table via `ctx.db.insert(\"<table>\", …)`. It may be read-only by design (seeded by a migration, replicated, or written through a path the advisor can't see) — or it may be dead schema.",
        "detail": "No function calls `ctx.db.insert(\"events\", …)` — table \"events\" has no discovered insert path.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "table": "events"
        },
        "name": "table_without_insert",
        "remediation": "If the table should be writable, add a mutation that calls `ctx.db.insert(\"<table>\", …)`. If it is read-only or seeded elsewhere, this advisory can be ignored.",
        "title": "Table has no insert path"
    },
    {
        "cacheKey": "table_without_insert:paymentSessions",
        "categories": [
            "SCHEMA"
        ],
        "description": "No function inserts into this table via `ctx.db.insert(\"<table>\", …)`. It may be read-only by design (seeded by a migration, replicated, or written through a path the advisor can't see) — or it may be dead schema.",
        "detail": "No function calls `ctx.db.insert(\"paymentSessions\", …)` — table \"paymentSessions\" has no discovered insert path.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "table": "paymentSessions"
        },
        "name": "table_without_insert",
        "remediation": "If the table should be writable, add a mutation that calls `ctx.db.insert(\"<table>\", …)`. If it is read-only or seeded elsewhere, this advisory can be ignored.",
        "title": "Table has no insert path"
    },
    {
        "cacheKey": "table_without_insert:subscriptions",
        "categories": [
            "SCHEMA"
        ],
        "description": "No function inserts into this table via `ctx.db.insert(\"<table>\", …)`. It may be read-only by design (seeded by a migration, replicated, or written through a path the advisor can't see) — or it may be dead schema.",
        "detail": "No function calls `ctx.db.insert(\"subscriptions\", …)` — table \"subscriptions\" has no discovered insert path.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "table": "subscriptions"
        },
        "name": "table_without_insert",
        "remediation": "If the table should be writable, add a mutation that calls `ctx.db.insert(\"<table>\", …)`. If it is read-only or seeded elsewhere, this advisory can be ignored.",
        "title": "Table has no insert path"
    },
    {
        "cacheKey": "table_without_insert:usageEvents",
        "categories": [
            "SCHEMA"
        ],
        "description": "No function inserts into this table via `ctx.db.insert(\"<table>\", …)`. It may be read-only by design (seeded by a migration, replicated, or written through a path the advisor can't see) — or it may be dead schema.",
        "detail": "No function calls `ctx.db.insert(\"usageEvents\", …)` — table \"usageEvents\" has no discovered insert path.",
        "facing": "INTERNAL",
        "level": "INFO",
        "metadata": {
            "table": "usageEvents"
        },
        "name": "table_without_insert",
        "remediation": "If the table should be writable, add a mutation that calls `ctx.db.insert(\"<table>\", …)`. If it is read-only or seeded elsewhere, this advisory can be ignored.",
        "title": "Table has no insert path"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:audit-log:27:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in record (audit-log:27) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "record",
            "file": "audit-log",
            "kind": "mutation",
            "line": 27
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:builds:65:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in recordPush (builds:65) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "recordPush",
            "file": "builds",
            "kind": "mutation",
            "line": 65
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:builds:87:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in claimNext (builds:87) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "claimNext",
            "file": "builds",
            "kind": "mutation",
            "line": 87
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:builds:131:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in appendLog (builds:131) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "appendLog",
            "file": "builds",
            "kind": "mutation",
            "line": 131
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:builds:140:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in complete (builds:140) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "complete",
            "file": "builds",
            "kind": "mutation",
            "line": 140
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:builds:159:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in fail (builds:159) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "fail",
            "file": "builds",
            "kind": "mutation",
            "line": 159
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:cells:52:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in register (cells:52) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "register",
            "file": "cells",
            "kind": "mutation",
            "line": 52
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deploy-keys:71:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in issue (deploy-keys:71) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "issue",
            "file": "deploy-keys",
            "kind": "mutation",
            "line": 71
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deploy-keys:89:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in revoke (deploy-keys:89) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "revoke",
            "file": "deploy-keys",
            "kind": "mutation",
            "line": 89
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deploy-keys:127:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in verify (deploy-keys:127) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "verify",
            "file": "deploy-keys",
            "kind": "mutation",
            "line": 127
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deployments:155:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in create (deployments:155) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "create",
            "file": "deployments",
            "kind": "mutation",
            "line": 155
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deployments:201:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in activate (deployments:201) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "activate",
            "file": "deployments",
            "kind": "mutation",
            "line": 201
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deployments:234:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in rollback (deployments:234) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "rollback",
            "file": "deployments",
            "kind": "mutation",
            "line": 234
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deployments:282:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in cleanupExpiredPreviews (deployments:282) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "cleanupExpiredPreviews",
            "file": "deployments",
            "kind": "mutation",
            "line": 282
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:deployments:337:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in updateStatus (deployments:337) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "updateStatus",
            "file": "deployments",
            "kind": "mutation",
            "line": 337
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:domains:57:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in add (domains:57) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "add",
            "file": "domains",
            "kind": "mutation",
            "line": 57
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:domains:107:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in markVerified (domains:107) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "markVerified",
            "file": "domains",
            "kind": "mutation",
            "line": 107
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:domains:108:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in markVerified (domains:108) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "markVerified",
            "file": "domains",
            "kind": "mutation",
            "line": 108
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:github-installations:51:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in record (github-installations:51) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "record",
            "file": "github-installations",
            "kind": "mutation",
            "line": 51
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:invitations:53:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in invite (invitations:53) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "invite",
            "file": "invitations",
            "kind": "mutation",
            "line": 53
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:invitations:98:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in accept (invitations:98) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "accept",
            "file": "invitations",
            "kind": "mutation",
            "line": 98
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:invitations:106:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in accept (invitations:106) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "accept",
            "file": "invitations",
            "kind": "mutation",
            "line": 106
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:members:50:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in add (members:50) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "add",
            "file": "members",
            "kind": "mutation",
            "line": 50
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:organizations:55:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in create (organizations:55) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "create",
            "file": "organizations",
            "kind": "mutation",
            "line": 55
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:projects:59:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in create (projects:59) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "create",
            "file": "projects",
            "kind": "mutation",
            "line": 59
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:secrets:41:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in store (secrets:41) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "store",
            "file": "secrets",
            "kind": "mutation",
            "line": 41
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:usage:28:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in record (usage:28) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "record",
            "file": "usage",
            "kind": "mutation",
            "line": 28
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:usage:56:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in ingest (usage:56) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "ingest",
            "file": "usage",
            "kind": "mutation",
            "line": 56
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "nondeterministic_query_mutation:usage:182:Date.now",
        "categories": [
            "SCHEMA"
        ],
        "description": "A `query`/`mutation` handler calls a non-deterministic API (`Date.now`, `Math.random`, `crypto.randomUUID`, `crypto.getRandomValues`, or `fetch`). These handlers may be re-run on OCC retry / subscription re-evaluation, so they must be deterministic — time, randomness, and network belong in an `action`.",
        "detail": "`Date.now(…)` in enforceSpendCaps (usage:182) runs inside a mutation handler — query/mutation handlers must be deterministic. Compute it in an `action` and pass the value into the mutation as an argument.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callee": "Date.now",
            "exportName": "enforceSpendCaps",
            "file": "usage",
            "kind": "mutation",
            "line": 182
        },
        "name": "nondeterministic_query_mutation",
        "remediation": "Move the non-deterministic call into an `action(...)` (which runs once and may use ambient APIs), then pass the computed value into the mutation as an argument — e.g. compute `Date.now()` in the action and call `ctx.runMutation(api.…, { now })`. Take generated ids/timestamps as args instead of producing them in the handler.",
        "title": "Non-deterministic call in query/mutation handler"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:audit-log:record",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `record` (audit-log) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "record",
            "file": "audit-log",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:billing:checkout",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public action `checkout` (billing) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "checkout",
            "file": "billing",
            "kind": "action",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:billing:portal",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public action `portal` (billing) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "portal",
            "file": "billing",
            "kind": "action",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:billing:processWebhook",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public action `processWebhook` (billing) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "processWebhook",
            "file": "billing",
            "kind": "action",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:builds:recordPush",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `recordPush` (builds) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "recordPush",
            "file": "builds",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:cells:register",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `register` (cells) has no rate limit — its name suggests an auth/abuse-sensitive endpoint, so this is high-risk. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "register",
            "file": "cells",
            "kind": "mutation",
            "sensitive": true
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deploy-keys:issue",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `issue` (deploy-keys) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "issue",
            "file": "deploy-keys",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deploy-keys:revoke",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `revoke` (deploy-keys) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "revoke",
            "file": "deploy-keys",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deploy-keys:verify",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `verify` (deploy-keys) has no rate limit — its name suggests an auth/abuse-sensitive endpoint, so this is high-risk. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "verify",
            "file": "deploy-keys",
            "kind": "mutation",
            "sensitive": true
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deployments:create",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `create` (deployments) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "create",
            "file": "deployments",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deployments:activate",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `activate` (deployments) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "activate",
            "file": "deployments",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deployments:rollback",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `rollback` (deployments) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "rollback",
            "file": "deployments",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:deployments:updateStatus",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `updateStatus` (deployments) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "updateStatus",
            "file": "deployments",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:domains:add",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `add` (domains) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "add",
            "file": "domains",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:domains:remove",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `remove` (domains) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "remove",
            "file": "domains",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:domains:markVerified",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `markVerified` (domains) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "markVerified",
            "file": "domains",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:github-installations:record",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `record` (github-installations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "record",
            "file": "github-installations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:github-installations:remove",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `remove` (github-installations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "remove",
            "file": "github-installations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:invitations:invite",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `invite` (invitations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "invite",
            "file": "invitations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:invitations:revoke",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `revoke` (invitations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "revoke",
            "file": "invitations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:invitations:accept",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `accept` (invitations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "accept",
            "file": "invitations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:members:add",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `add` (members) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "add",
            "file": "members",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:members:remove",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `remove` (members) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "remove",
            "file": "members",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:organizations:create",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `create` (organizations) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "create",
            "file": "organizations",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:projects:create",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `create` (projects) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "create",
            "file": "projects",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:secrets:store",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `store` (secrets) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "store",
            "file": "secrets",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:secrets:remove",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `remove` (secrets) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "remove",
            "file": "secrets",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "public_mutation_without_ratelimit:usage:ingest",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` has no `rateLimit` middleware. Publicly-callable writes are flood and brute-force targets — an attacker can exhaust writes, mail quota, or credits, or guess credentials on auth-shaped endpoints.",
        "detail": "Public mutation `ingest` (usage) has no rate limit. Add `.use(rateLimit(...))` or `.use(protectPublic({ rateLimit }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "exportName": "ingest",
            "file": "usage",
            "kind": "mutation",
            "sensitive": false
        },
        "name": "public_mutation_without_ratelimit",
        "remediation": "Attach a rate limit: `.use(rateLimit(limiter, \"<bucket>\"))` from `@lunora/ratelimit`, or wrap the recommended public-procedure guards with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Genuinely-open writes can be acknowledged by adding a permissive limiter.",
        "title": "Public write without a rate limit"
    },
    {
        "cacheKey": "user_creating_mutation_without_captcha:invitations:accept",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` that creates a user/session or sends mail has no CAPTCHA / bot check. Account-creating and mail-sending endpoints are prime automated-abuse targets (credential stuffing, mailbox flooding, disposable-account farming).",
        "detail": "Public mutation `accept` (invitations) writes a user/session table but has no CAPTCHA check. Add `.use(verifyTurnstile(...))` or `.use(protectPublic({ captcha }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callsMail": false,
            "exportName": "accept",
            "file": "invitations",
            "kind": "mutation",
            "writesUserTable": true
        },
        "name": "user_creating_mutation_without_captcha",
        "remediation": "Add a server-verified human check: `.use(verifyTurnstile({ secret, token }))` from `@lunora/auth`, or wrap it with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Pair with a rate limit for defense in depth.",
        "title": "Account-creating / mail-sending write without a CAPTCHA"
    },
    {
        "cacheKey": "user_creating_mutation_without_captcha:members:add",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` that creates a user/session or sends mail has no CAPTCHA / bot check. Account-creating and mail-sending endpoints are prime automated-abuse targets (credential stuffing, mailbox flooding, disposable-account farming).",
        "detail": "Public mutation `add` (members) writes a user/session table but has no CAPTCHA check. Add `.use(verifyTurnstile(...))` or `.use(protectPublic({ captcha }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callsMail": false,
            "exportName": "add",
            "file": "members",
            "kind": "mutation",
            "writesUserTable": true
        },
        "name": "user_creating_mutation_without_captcha",
        "remediation": "Add a server-verified human check: `.use(verifyTurnstile({ secret, token }))` from `@lunora/auth`, or wrap it with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Pair with a rate limit for defense in depth.",
        "title": "Account-creating / mail-sending write without a CAPTCHA"
    },
    {
        "cacheKey": "user_creating_mutation_without_captcha:organizations:create",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `mutation`/`action` that creates a user/session or sends mail has no CAPTCHA / bot check. Account-creating and mail-sending endpoints are prime automated-abuse targets (credential stuffing, mailbox flooding, disposable-account farming).",
        "detail": "Public mutation `create` (organizations) writes a user/session table but has no CAPTCHA check. Add `.use(verifyTurnstile(...))` or `.use(protectPublic({ captcha }))`.",
        "facing": "EXTERNAL",
        "level": "WARN",
        "metadata": {
            "callsMail": false,
            "exportName": "create",
            "file": "organizations",
            "kind": "mutation",
            "writesUserTable": true
        },
        "name": "user_creating_mutation_without_captcha",
        "remediation": "Add a server-verified human check: `.use(verifyTurnstile({ secret, token }))` from `@lunora/auth`, or wrap it with `.use(protectPublic({ rateLimit, captcha }))` from `@lunora/server`. Pair with a rate limit for defense in depth.",
        "title": "Account-creating / mail-sending write without a CAPTCHA"
    },
    {
        "cacheKey": "unbounded_string_arg:audit-log:record:action",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `action` of public procedure `record` (audit-log:19) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "action",
            "exportName": "record",
            "file": "audit-log",
            "line": 19
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:audit-log:record:target",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `target` of public procedure `record` (audit-log:19) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "target",
            "exportName": "record",
            "file": "audit-log",
            "line": 19
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:checkout:cancelUrl",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `cancelUrl` of public procedure `checkout` (billing:38) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "cancelUrl",
            "exportName": "checkout",
            "file": "billing",
            "line": 38
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:checkout:priceId",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `priceId` of public procedure `checkout` (billing:38) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "priceId",
            "exportName": "checkout",
            "file": "billing",
            "line": 38
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:checkout:successUrl",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `successUrl` of public procedure `checkout` (billing:38) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "successUrl",
            "exportName": "checkout",
            "file": "billing",
            "line": 38
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:portal:returnUrl",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `returnUrl` of public procedure `portal` (billing:60) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "returnUrl",
            "exportName": "portal",
            "file": "billing",
            "line": 60
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:processWebhook:body",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `body` of public procedure `processWebhook` (billing:114) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "body",
            "exportName": "processWebhook",
            "file": "billing",
            "line": 114
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:billing:processWebhook:signature",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `signature` of public procedure `processWebhook` (billing:114) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "signature",
            "exportName": "processWebhook",
            "file": "billing",
            "line": 114
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:builds:recordPush:branch",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `branch` of public procedure `recordPush` (builds:48) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "branch",
            "exportName": "recordPush",
            "file": "builds",
            "line": 48
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:builds:recordPush:commitSha",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `commitSha` of public procedure `recordPush` (builds:48) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "commitSha",
            "exportName": "recordPush",
            "file": "builds",
            "line": 48
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:builds:recordPush:repository",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `repository` of public procedure `recordPush` (builds:48) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "repository",
            "exportName": "recordPush",
            "file": "builds",
            "line": 48
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:cells:register:cloudflareAccountId",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `cloudflareAccountId` of public procedure `register` (cells:40) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "cloudflareAccountId",
            "exportName": "register",
            "file": "cells",
            "line": 40
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:cells:register:dispatchNamespacePrefix",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `dispatchNamespacePrefix` of public procedure `register` (cells:40) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "dispatchNamespacePrefix",
            "exportName": "register",
            "file": "cells",
            "line": 40
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:cells:register:jurisdiction",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `jurisdiction` of public procedure `register` (cells:40) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "jurisdiction",
            "exportName": "register",
            "file": "cells",
            "line": 40
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:cells:register:name",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `name` of public procedure `register` (cells:40) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "name",
            "exportName": "register",
            "file": "cells",
            "line": 40
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deploy-keys:issue:name",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `name` of public procedure `issue` (deploy-keys:52) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "name",
            "exportName": "issue",
            "file": "deploy-keys",
            "line": 52
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deploy-keys:verify:key",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `key` of public procedure `verify` (deploy-keys:105) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "key",
            "exportName": "verify",
            "file": "deploy-keys",
            "line": 105
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:planForScript:scriptName",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `scriptName` of public procedure `planForScript` (deployments:75) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "scriptName",
            "exportName": "planForScript",
            "file": "deployments",
            "line": 75
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:create:adminToken",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `adminToken` of public procedure `create` (deployments:115) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "adminToken",
            "exportName": "create",
            "file": "deployments",
            "line": 115
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:create:branch",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `branch` of public procedure `create` (deployments:115) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "branch",
            "exportName": "create",
            "file": "deployments",
            "line": 115
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:create:cronSpecs",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `cronSpecs` of public procedure `create` (deployments:115) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "cronSpecs",
            "exportName": "create",
            "file": "deployments",
            "line": 115
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:create:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `create` (deployments:115) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "create",
            "file": "deployments",
            "line": 115
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:create:scriptName",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `scriptName` of public procedure `create` (deployments:115) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "scriptName",
            "exportName": "create",
            "file": "deployments",
            "line": 115
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:activate:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `activate` (deployments:184) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "activate",
            "file": "deployments",
            "line": 184
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:rollback:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `rollback` (deployments:219) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "rollback",
            "file": "deployments",
            "line": 219
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:routeForAlias:alias",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `alias` of public procedure `routeForAlias` (deployments:254) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "alias",
            "exportName": "routeForAlias",
            "file": "deployments",
            "line": 254
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:updateStatus:bundleHash",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `bundleHash` of public procedure `updateStatus` (deployments:309) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "bundleHash",
            "exportName": "updateStatus",
            "file": "deployments",
            "line": 309
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:updateStatus:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `updateStatus` (deployments:309) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "updateStatus",
            "file": "deployments",
            "line": 309
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:deployments:updateStatus:url",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `url` of public procedure `updateStatus` (deployments:309) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "url",
            "exportName": "updateStatus",
            "file": "deployments",
            "line": 309
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:domains:add:hostname",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `hostname` of public procedure `add` (domains:38) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "hostname",
            "exportName": "add",
            "file": "domains",
            "line": 38
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:domains:add:redirectTo",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `redirectTo` of public procedure `add` (domains:38) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "redirectTo",
            "exportName": "add",
            "file": "domains",
            "line": 38
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:domains:markVerified:customHostnameId",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `customHostnameId` of public procedure `markVerified` (domains:99) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "customHostnameId",
            "exportName": "markVerified",
            "file": "domains",
            "line": 99
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:domains:routeForHostname:hostname",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `hostname` of public procedure `routeForHostname` (domains:118) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "hostname",
            "exportName": "routeForHostname",
            "file": "domains",
            "line": 118
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:github-installations:record:accountLogin",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `accountLogin` of public procedure `record` (github-installations:31) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "accountLogin",
            "exportName": "record",
            "file": "github-installations",
            "line": 31
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:invitations:invite:email",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `email` of public procedure `invite` (invitations:49) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "email",
            "exportName": "invite",
            "file": "invitations",
            "line": 49
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:invitations:accept:token",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `token` of public procedure `accept` (invitations:85) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "token",
            "exportName": "accept",
            "file": "invitations",
            "line": 85
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:members:add:userId",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `userId` of public procedure `add` (members:30) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "userId",
            "exportName": "add",
            "file": "members",
            "line": 30
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:organizations:getBySlug:slug",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `slug` of public procedure `getBySlug` (organizations:35) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "slug",
            "exportName": "getBySlug",
            "file": "organizations",
            "line": 35
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:organizations:create:name",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `name` of public procedure `create` (organizations:46) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "name",
            "exportName": "create",
            "file": "organizations",
            "line": 46
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:organizations:create:slug",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `slug` of public procedure `create` (organizations:46) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "slug",
            "exportName": "create",
            "file": "organizations",
            "line": 46
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:projects:byGithubRepo:repository",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `repository` of public procedure `byGithubRepo` (projects:28) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "repository",
            "exportName": "byGithubRepo",
            "file": "projects",
            "line": 28
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:projects:create:framework",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `framework` of public procedure `create` (projects:43) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "framework",
            "exportName": "create",
            "file": "projects",
            "line": 43
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:projects:create:githubRepo",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `githubRepo` of public procedure `create` (projects:43) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "githubRepo",
            "exportName": "create",
            "file": "projects",
            "line": 43
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:projects:create:name",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `name` of public procedure `create` (projects:43) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "name",
            "exportName": "create",
            "file": "projects",
            "line": 43
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:projects:create:slug",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `slug` of public procedure `create` (projects:43) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "slug",
            "exportName": "create",
            "file": "projects",
            "line": 43
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:secrets:store:ciphertext",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `ciphertext` of public procedure `store` (secrets:25) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "ciphertext",
            "exportName": "store",
            "file": "secrets",
            "line": 25
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:secrets:store:iv",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `iv` of public procedure `store` (secrets:25) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "iv",
            "exportName": "store",
            "file": "secrets",
            "line": 25
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:secrets:store:name",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `name` of public procedure `store` (secrets:25) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "name",
            "exportName": "store",
            "file": "secrets",
            "line": 25
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:secrets:listEncrypted:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `listEncrypted` (secrets:81) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "listEncrypted",
            "file": "secrets",
            "line": 81
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    },
    {
        "cacheKey": "unbounded_string_arg:usage:ingest:deployKey",
        "categories": [
            "SECURITY"
        ],
        "description": "A public `v.string()` argument has no maximum-length bound. An unbounded string lets a client submit arbitrarily large input — abusing storage and CPU on every request that processes it.",
        "detail": "Arg `deployKey` of public procedure `ingest` (usage:43) is an unbounded `v.string()`. Add a max-length bound to cap payload size.",
        "facing": "EXTERNAL",
        "level": "INFO",
        "metadata": {
            "argument": "deployKey",
            "exportName": "ingest",
            "file": "usage",
            "line": 43
        },
        "name": "unbounded_string_arg",
        "remediation": "Add a max-length bound via `.check(...)` / `.meta({ maxLength })` on the string validator (e.g. cap a name at 256, a body at a few KB). Size the cap to the field's real-world maximum.",
        "title": "Public string argument has no length bound"
    }
];

/** Read-only RLS metadata (policies + roles discovered from `.use(rls(...))` chains) served via `__lunora_admin__:rlsPolicies` for the studio's RLS inspector. */
const LUNORA_RLS_METADATA: RlsPoliciesResult = {
    "policies": [],
    "roles": []
};

/** Read-only masking metadata (table + column + strategy discovered from `.use(mask(...))` chains) served via `__lunora_admin__:maskPolicies` for the studio's data-browser mask preview. */
const LUNORA_MASK_METADATA: MaskPoliciesResult = {
    "columns": []
};

/** Read-only storage access-rule metadata (discovered from `.use(storageRules(...))` chains) served via `__lunora_admin__:storageRules` for the studio's access-rules view. */
const LUNORA_STORAGE_RULES: StorageRulesResult = {
    "rules": []
};

/** Which optional package-backed features this app wires up (discovered from imports / `ctx.*` reads / schema signals) served via `__lunora_admin__:studioFeatures` so the studio hides nav pages whose package isn't enabled. */
const LUNORA_STUDIO_FEATURES: StudioFeaturesResult = {
    "analytics": false,
    "auth": true,
    "containers": false,
    "flags": false,
    "kv": false,
    "mail": true,
    "payments": true,
    "queues": false,
    "scheduler": true,
    "storage": false,
    "vectors": false,
    "workflows": false
};

export interface ShardDOConfig {
    /** Opt into change-data-capture: records a post-image to `__cdc_log` on every write (backs streaming export + replay-PITR). */
    cdc?: boolean;
    /** Optional telemetry sink. When supplied, each `ctx.log.*` call is forwarded to `sink.onLog`. Pass the SAME sink you give `createWorker({ observability })` (which drives `onRpc`) to route both RPC and log events. */
    observability?: (env: Record<string, unknown>) => LogSink | undefined;
    scheduler?: (env: Record<string, unknown>) => unknown;
    storage?: (env: Record<string, unknown>) => unknown;
    payment?: (env: Record<string, unknown>) => PaymentsFromContextOptions;
    d1?: (env: Record<string, unknown>, request?: { identity?: Record<string, unknown>; userId?: string }) => DatabaseWriterLike | undefined;
}

const schedulerStub = {
    cancel: async () => {
        throw new Error("ctx.scheduler: no scheduler configured. Pass `scheduler` to createShardDO().");
    },
    runAfter: async () => {
        throw new Error("ctx.scheduler: no scheduler configured. Pass `scheduler` to createShardDO().");
    },
    runAt: async () => {
        throw new Error("ctx.scheduler: no scheduler configured. Pass `scheduler` to createShardDO().");
    },
};

const storageStub = {
    delete: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    download: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    getMetadata: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    getSignedUrl: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    getUrl: () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    list: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
    upload: async () => {
        throw new Error("ctx.storage: no storage configured. Pass `storage` to createShardDO().");
    },
};

const globalDbStub: DatabaseWriterLike = {
    aggregate: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    count: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    delete: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    findFirst: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    findFirstOrThrow: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    findMany: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    get: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    groupBy: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    insert: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    normalizeId: () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    patch: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    query: () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    rank: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    rankPage: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
    replace: async () => {
        throw new Error("ctx.db.<globalTable>: no global backend configured. Pass `d1` or `hyperdriveGlobal` to createShardDO().");
    },
};

const paymentStub: LunoraPayment = {
    attach: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    cancelSubscription: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    check: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    createCheckout: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    createPortalSession: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    handleWebhook: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    listBalances: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    listSubscriptions: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
    track: () => {
        throw new Error("ctx.payments: no payment configured. Pass \`payment\` to createShardDO().");
    },
} as unknown as LunoraPayment;

// Bound in-process `ctx.run*` composition depth so a self- or cyclically-
// referencing call fails loudly with a clear error instead of overflowing the
// stack. Tracked across the awaited handler chain (one DO invocation is
// single-threaded), so the counter reflects true nesting depth.
const MAX_RUN_DEPTH = 32;
let runDepth = 0;

const dispatchRun = async (expected: FunctionKind, functionPath: string, args: Record<string, unknown>, ctx: unknown): Promise<unknown> => {
    const registered = LUNORA_FUNCTIONS[functionPath];

    if (!registered) {
        throw new Error(`unknown function: ${functionPath}`);
    }

    if (registered.kind !== expected) {
        throw new Error(`ctx.run${expected[0]!.toUpperCase()}${expected.slice(1)}: "${functionPath}" is registered as a ${registered.kind}, not a ${expected}`);
    }

    if (runDepth >= MAX_RUN_DEPTH) {
        throw Object.assign(new Error(`ctx.run*: composition depth limit (${MAX_RUN_DEPTH}) exceeded — likely a cyclic runQuery/runMutation`), {
            name: "LunoraError",
            code: "RUN_DEPTH_EXCEEDED",
            status: 500,
        });
    }

    runDepth += 1;

    try {
        return await registered.handler(ctx, args);
    } finally {
        runDepth -= 1;
    }
};

/**
 * Build the project's shard Durable Object. Export the result as `ShardDO`
 * from the worker entry so wrangler binds it by name.
 */
export const createShardDO = (config: ShardDOConfig = {}): new (state: ShardDOState, env: unknown) => ShardDOBase =>
    class extends ShardDOBase {
        private migrated = false;

        public override async handleRpc(functionPath: string, args: Record<string, unknown>): Promise<unknown> {
            const registered = LUNORA_FUNCTIONS[functionPath];

            // Internal functions are reachable server-side only: via `ctx.run*`
            // composition, or a trusted system dispatch (scheduler/cron, marked by
            // `isSystemDispatch()`). A client RPC never carries that flag, so its
            // internals stay not-found and never leak across the external boundary.
            if (!registered || (registered.visibility === "internal" && !this.isSystemDispatch())) {
                throw Object.assign(new Error(`function not registered: ${functionPath}`), {
                    name: "LunoraError",
                    code: "FUNCTION_NOT_FOUND",
                    status: 404,
                });
            }

            this.ensureMigrated();

            const ctx = this.buildCtx({ functionPath });

            // A mutation's writes must commit all-or-nothing: wrap its dispatch in
            // the DO's BEGIN/COMMIT span so any throw (a validator, an RLS denial,
            // an OCC conflict, a failed row mid-`insertMany`/`patchMany`) rolls
            // back every write the mutation made. Queries are read-only and actions
            // do external I/O that can't be rolled back, so both dispatch directly.
            // `ctx.run*` composition runs inside this span (it never re-enters
            // handleRpc); runInTransaction's own guard rejects accidental nesting.
            //
            // The replay bookkeeping (idempotency dedup row + custom-mutator
            // watermark advance) commits INSIDE this span via
            // `commitMutationBookkeeping`, so the writes, the dedup row, and the
            // watermark are atomic — a crash can't leave the writes durable without
            // the replay guard.
            if (registered.kind === "mutation") {
                return this.runInTransaction(async () => {
                    const result = await registered.handler(ctx, args);

                    this.commitMutationBookkeeping(result);

                    return result;
                });
            }

            return registered.handler(ctx, args);
        }

        protected override async runRelationFanoutRead(functionPath: string, args: Record<string, unknown>): Promise<unknown> {
            this.ensureMigrated();

            const { db } = this.buildCtx({ functionPath }) as { db: DatabaseWriterLike };

            return serveRelationFanout(schema as unknown as SchemaLike, db, functionPath, args);
        }

        protected override async executeSubscription(functionPath: string, args: Record<string, unknown>, identity?: { identity?: Record<string, unknown>; userId?: string }): Promise<{ result: unknown; tables: Set<string> } | null> {
            const registered = LUNORA_FUNCTIONS[functionPath];

            if (!registered || registered.kind !== "query" || registered.visibility === "internal") {
                return null;
            }

            this.ensureMigrated();

            const tables = new Set<string>();
            // Identity is threaded EXPLICITLY from the (deferred/interleaved)
            // subscription caller — never read from the shared per-request field
            // here — so a concurrent RPC can't leak its identity into this re-run.
            const ctx = this.buildCtx({ functionPath, identity, onRead: (table) => tables.add(table) });
            const result = await registered.handler(ctx, args);

            return { result, tables };
        }

        protected override executeStream(functionPath: string, args: Record<string, unknown>): null | { iterator: (signal: AbortSignal) => AsyncIterable<unknown> } {
            const registered = LUNORA_FUNCTIONS[functionPath];

            if (!registered || registered.kind !== "stream" || registered.visibility === "internal") {
                return null;
            }

            this.ensureMigrated();

            return {
                iterator: (signal) => (registered.handler as (context: unknown, args: Record<string, unknown>, signal: AbortSignal) => AsyncIterable<unknown>)(this.buildCtx({ functionPath }), args, signal),
            };
        }

        protected override lifecycleHookPaths(event: "connect" | "disconnect"): readonly string[] {
            return LUNORA_LIFECYCLE_HOOKS[event];
        }

        protected override tableRefs(table: string): Record<string, string> | undefined {
            return LUNORA_TABLE_REFS[table];
        }

        protected override tableIndexes(table: string): Array<{ fields: string[]; name: string; type: "index" | "rank" | "search" | "vector"; unique?: boolean }> {
            return LUNORA_TABLE_INDEXES[table] ?? [];
        }

        protected override tableColumns(table: string): Array<{ isStorage?: boolean; name: string; optional: boolean; pk?: boolean; ref?: string; type: string }> {
            return LUNORA_TABLE_COLUMNS[table] ?? [];
        }

        protected override storageColumns(): Record<string, string[]> {
            return LUNORA_STORAGE_COLUMNS;
        }

        protected override rlsMetadata(): RlsPoliciesResult {
            return LUNORA_RLS_METADATA;
        }

        protected override maskMetadata(): MaskPoliciesResult {
            return LUNORA_MASK_METADATA;
        }

        protected override storageRulesMetadata(): StorageRulesResult {
            return LUNORA_STORAGE_RULES;
        }

        protected override studioFeatures(): StudioFeaturesResult {
            return LUNORA_STUDIO_FEATURES;
        }

        protected override advisories(): AdvisoryFinding[] {
            return LUNORA_ADVISORIES;
        }

        protected override async runShardDataMigration(args: RunShardMigrationArgs): Promise<MigrationRunResult> {
            const migration = LUNORA_MIGRATIONS[args.id];

            if (!migration) {
                throw Object.assign(new Error(`data migration "${args.id}" is not registered`), {
                    name: "LunoraError",
                    code: "MIGRATION_NOT_FOUND",
                    status: 404,
                });
            }

            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            return runDataMigration({
                batchSize: args.batchSize,
                direction: args.direction,
                dryRun: args.dryRun,
                maxBatches: args.maxBatches,
                migration: migration as unknown as DataMigrationLike,
                // Flush after each batch so live migrationStatus subscribers see
                // progress mid-run (the base method records the reserved state
                // table the raw-SQL progress write is invisible to the tracker).
                onBatch: () => this.flushMigrationProgress(),
                sql: this.sql as SqlExec,
                writer,
            });
        }

        protected override async runShardWrite(args: RunShardWriteArgs): Promise<RunShardWriteResult> {
            const definition = (schema as unknown as SchemaLike).tables[args.table];

            if (!definition) {
                throw Object.assign(new Error(`unknown table: ${args.table}`), { name: "LunoraError", code: "UNKNOWN_TABLE", status: 404 });
            }

            // `.global()` tables live in D1, not this DO's SQLite — editing them
            // here would corrupt nothing but would fail confusingly, so reject up
            // front with a clear code the studio can surface.
            if (definition.shardMode?.kind === "global") {
                throw Object.assign(new Error(`table "${args.table}" is global; edit it through D1, not the shard`), {
                    name: "LunoraError",
                    code: "GLOBAL_TABLE_NOT_EDITABLE",
                    status: 400,
                });
            }

            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            if (args.op === "insert") {
                const id = await writer.insert(args.table, args.doc ?? {});

                return { id, op: "insert" };
            }

            if (args.op === "delete") {
                await writer.delete(args.id ?? "");

                return { id: args.id ?? null, op: "delete" };
            }

            if (args.op === "replace") {
                await writer.replace(args.id ?? "", args.doc ?? {});

                return { id: args.id ?? null, op: "replace" };
            }

            await writer.patch(args.id ?? "", args.doc ?? {});

            return { id: args.id ?? null, op: "patch" };
        }

        protected override async deleteRowThroughWriter(table: string, id: string): Promise<void> {
            const definition = (schema as unknown as SchemaLike).tables[table];

            if (!definition) {
                throw Object.assign(new Error(`unknown table: ${table}`), { name: "LunoraError", code: "UNKNOWN_TABLE", status: 404 });
            }

            // `.global()` tables live in D1, not this DO's SQLite — the same
            // guard `runShardWrite` applies to single-row edits.
            if (definition.shardMode?.kind === "global") {
                throw Object.assign(new Error(`table "${table}" is global; edit it through D1, not the shard`), {
                    name: "LunoraError",
                    code: "GLOBAL_TABLE_NOT_EDITABLE",
                    status: 400,
                });
            }

            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            // Routes through the writer (not raw SQL) so FTS / aggregate / rank
            // shadow tables stay in sync and `onDelete` cascades fire — the
            // bounded loop lives in the base `runShardBulkDelete`.
            await writer.delete(id);
        }

        protected override async runShardRankBefore(args: RunShardRankBeforeArgs): Promise<{ before: number; total: number }> {
            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            // `rankBefore` is optional on `DatabaseWriterLike` (the D1 twin omits it),
            // but the shard writer from `createShardCtxDb` always defines it.
            if (!writer.rankBefore) {
                throw Object.assign(new Error("rankBefore is unavailable on the shard writer"), { name: "LunoraError", code: "NOT_IMPLEMENTED", status: 500 });
            }

            return writer.rankBefore(args.table, args.index, {
                partitionKey: args.partitionKey,
                rowId: args.rowId,
                sortValues: args.sortValues,
            });
        }

        protected override async runShardRankPage(args: RunShardRankPageArgs): Promise<ShardRankPageResult> {
            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            // `rankPageRows` is optional on `DatabaseWriterLike` (the D1 twin omits it),
            // but the shard writer from `createShardCtxDb` always defines it. The sort
            // directions live in the schema's rankIndex, so the shard reads them itself;
            // `args.directions` is only the coordinator's comparator hint and isn't forwarded.
            if (!writer.rankPageRows) {
                throw Object.assign(new Error("rankPage is unavailable on the shard writer"), { name: "LunoraError", code: "NOT_IMPLEMENTED", status: 500 });
            }

            return writer.rankPageRows(args.table, args.index, {
                after: args.after,
                cursor: args.cursor,
                partitionKey: args.partitionKey,
                take: args.take,
            });
        }

        protected override async runShardApplyCdc(args: RunShardApplyCdcArgs): Promise<{ applied: number }> {
            this.ensureMigrated();

            const env = (this.env ?? {}) as Record<string, unknown>;
            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            const writer = createShardCtxDb({
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
            });

            await applyCdcChanges(writer, args.changes);

            return { applied: args.changes.length };
        }

        protected override ensureMigrated(): void {
            if (this.migrated) {
                return;
            }

            runShardMigrations(this.sql as SqlExec, schema as unknown as SchemaLike, { cdc: config.cdc ?? false });
            this.migrated = true;
        }

        private buildCtx(options: { functionPath?: string; identity?: { identity?: Record<string, unknown>; userId?: string }; onRead?: (table: string) => void } = {}): unknown {
            const env = (this.env ?? {}) as Record<string, unknown>;
            // When the caller threads an explicit identity (subscription seed /
            // refresh — both run in deferred/interleaved contexts), use it by
            // value and NEVER read the shared per-request identity field, which a
            // concurrent RPC may have re-set. Otherwise (the synchronous RPC
            // dispatch path) fall back to the per-request fields as before.
            const userId = options.identity ? options.identity.userId : this.getCurrentUserId();
            const identity = options.identity ? options.identity.identity : this.getCurrentIdentity();

            const secrets = createSecrets(env);

            const scheduler = (config.scheduler?.(env) ?? schedulerStub) as SchedulerLike;
            // Build the storage adapter once and share it between `ctx.storage`
            // and `ctx.db.system._storage` so both read the same R2 binding. The
            // `storageStub` fallback satisfies SystemReaderStorageLike structurally
            // (its `list`/`getMetadata` throw the "no storage configured" error).
            const storage = asBucketStorage(config.storage?.(env) ?? storageStub) as unknown as SystemReaderStorageLike;
            const globalDb: DatabaseWriterLike = config.d1?.(env, { identity, userId }) ?? globalDbStub;
            const db: DatabaseWriterLike = createShardCtxDb({
                auth: { identity: identity ?? null, userId: userId ?? null },
                broadcast: (delta) => {
                    this.recordChangedTable(delta.table);
                },
                cdc: config.cdc ?? false,
                enforceRls: true,
                onIndexUse: this.getCtxDbIndexUseHook(),
                onRead: options.onRead ?? this.getCtxDbReadHook(),
                scheduler,
                schema: schema as unknown as SchemaLike,
                sql: this.sql as SqlExec,
                storage,
                globalDb,
            });

            const facade = db as unknown as Record<string, ReturnType<typeof bindTableFacade>>;
            facade["cells"] = bindTableFacade(db, "cells");
            facade["organizations"] = bindTableFacade(db, "organizations");
            facade["members"] = bindTableFacade(db, "members");
            facade["projects"] = bindTableFacade(db, "projects");
            facade["deployments"] = bindTableFacade(db, "deployments");
            facade["deployKeys"] = bindTableFacade(db, "deployKeys");
            facade["githubInstallations"] = bindTableFacade(db, "githubInstallations");
            facade["builds"] = bindTableFacade(db, "builds");
            facade["buildLogs"] = bindTableFacade(db, "buildLogs");
            facade["domains"] = bindTableFacade(db, "domains");
            facade["auditLog"] = bindTableFacade(db, "auditLog");
            facade["invitations"] = bindTableFacade(db, "invitations");
            facade["platformUsage"] = bindTableFacade(db, "platformUsage");
            facade["secrets"] = bindTableFacade(db, "secrets");
            facade["customers"] = bindTableFacade(db, "customers");
            facade["events"] = bindTableFacade(db, "events");
            facade["paymentSessions"] = bindTableFacade(db, "paymentSessions");
            facade["subscriptions"] = bindTableFacade(db, "subscriptions");
            facade["usageEvents"] = bindTableFacade(db, "usageEvents");

            const payments: LunoraPayment = config.payment
                ? paymentsFromContext({ auth: { userId: userId ?? null }, db: db as unknown as LunoraPaymentDbLike }, config.payment(env))
                : paymentStub;

            // `ctx.log`: each call is captured + attributed to the executing
            // function and routed to the optional `observability` sink. The DO base
            // also buffers it (studio Logs panel) and emits a structured console
            // event the dev-server formats in the terminal.
            const observability = config.observability?.(env);
            const logFunctionPath = options.functionPath ?? "";
            const log = {
                debug: (...args: unknown[]) => { this.recordUserLog(logFunctionPath, "debug", args, observability); },
                error: (...args: unknown[]) => { this.recordUserLog(logFunctionPath, "error", args, observability); },
                info: (...args: unknown[]) => { this.recordUserLog(logFunctionPath, "info", args, observability); },
                log: (...args: unknown[]) => { this.recordUserLog(logFunctionPath, "log", args, observability); },
                warn: (...args: unknown[]) => { this.recordUserLog(logFunctionPath, "warn", args, observability); },
            };

            // `ctx.now`: the wall-clock instant (epoch ms) this function began,
            // captured ONCE so the whole handler body sees a single stable value.
            // Query/mutation handlers must be deterministic (they may be re-run on
            // OCC retry / subscription re-eval), so they must read time through
            // `ctx.now` instead of `Date.now()` — the `nondeterministic_query_mutation`
            // advisor flags the latter. Actions may still use ambient `Date.now()`.
            const now = Date.now();

            const ctx: Record<string, unknown> = {
                auth: {
                    getIdentity: async () => identity ?? null,
                    userId: userId ?? null,
                },
                db,
                fetch: globalThis.fetch.bind(globalThis),
                ip: this.getCurrentIp(),
                log,
                now,
                orm: bindOrm(facade),
                scheduler,
                storage,
                secrets,
                payments,
            };

            ctx.runAction = (reference: FunctionReference, fnArgs: Record<string, unknown>) => dispatchRun("action", reference.__lunoraRef, fnArgs, ctx);
            ctx.runMutation = (reference: FunctionReference, fnArgs: Record<string, unknown>) => dispatchRun("mutation", reference.__lunoraRef, fnArgs, ctx);
            ctx.runQuery = (reference: FunctionReference, fnArgs: Record<string, unknown>) => dispatchRun("query", reference.__lunoraRef, fnArgs, ctx);

            return ctx;
        }
    };
