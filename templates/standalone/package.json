{
    "name": "{{name}}",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "cirrus dev",
        "codegen": "cirrus codegen",
        "deploy": "cirrus deploy"
    },
    "dependencies": {
        "@cirrus/runtime": "^0.0.0",
        "@cirrus/server": "^0.0.0"
    },
    "devDependencies": {
        "wrangler": "^4.99.0"
    }
}
