/**
 * Next.js config. vinext auto-loads this file and merges it into the Vite build,
 * so the familiar Next options (basePath, redirects, rewrites, images, …) work
 * as you'd expect. Empty is fine to start.
 */
export default {};
