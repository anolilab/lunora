node_modules
.next
.open-next
dist
.wrangler
# `celld dev` state (holds `.dev.vars` values) and the celld driver's projection of wrangler.jsonc
.celld/
.celld.wrangler.json
.lunora/
.lunora-cache
lunora/_generated

# local env files
.env
.env.*
!.env.example
.dev.vars
