node_modules
build
dist
.react-router
.wrangler
# `celld dev` state (holds `.dev.vars` values) and the celld driver's projection of wrangler.jsonc
.celld/
.celld.wrangler.json
.env
.env.*
!.env.example
.lunora/
.lunora-cache
lunora/_generated

# local env files
.dev.vars
