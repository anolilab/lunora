node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
# `celld dev` state (holds `.dev.vars` values) and the celld driver's projection of wrangler.jsonc
.celld/
.celld.wrangler.json
.output
.vinxi
.lunora/
.lunora-cache
__unconfig*
lunora/_generated
src/routeTree.gen.ts

# local env files
.dev.vars
