{
    "$schema": "./schema/registry.schema.json",
    "items": [
        {
            "description": "Cookie-session authentication for Lunora — email/password sign-up/sign-in on top of @lunora/auth (a thin wrapper over better-auth), with the auth instance + request handler wired into your project and verification / forgot-password mail routed through @lunora/mail (captured into the studio Mail tab in dev).",
            "name": "auth",
            "title": "Auth"
        },
        {
            "description": "Sign in with Auth0 for Lunora auth. Wires Auth0 as an OIDC provider via better-auth's genericOAuth plugin, on top of the base `auth` item.",
            "name": "auth-auth0",
            "title": "Auth — Auth0"
        },
        {
            "description": "Sign in with Clerk for Lunora auth. Wires Clerk as an OIDC provider via better-auth's genericOAuth plugin, on top of the base `auth` item.",
            "name": "auth-clerk",
            "title": "Auth — Clerk"
        },
        {
            "description": "Passwordless magic-link sign-in for Lunora auth. Wires better-auth's magicLink plugin on top of the base `auth` item and delivers the sign-in link through @lunora/mail (captured into the studio Mail tab in dev).",
            "name": "auth-magic-link",
            "title": "Auth — Magic link"
        },
        {
            "description": "Passwordless email one-time-password (OTP) sign-in and verification for Lunora auth. Wires better-auth's emailOTP plugin on top of the base `auth` item and delivers the code through @lunora/mail (captured into the studio Mail tab in dev).",
            "name": "auth-otp",
            "title": "Auth — Email OTP"
        },
        {
            "description": "Managed point-in-time recovery — a cron-driven `snapshot` internalAction exports a configured set of tables to timestamped NDJSON in a dedicated R2 bucket, and a cron-driven `prune` internalAction ages snapshots out by retention window. Pairs with `lunora backup restore` (nearest snapshot + `--to` CDC replay).",
            "name": "backup",
            "title": "Backup"
        },
        {
            "description": "Code-first cron starter for Lunora — a lunora/crons.ts that declares recurring jobs with cronJobs() (interval / daily / weekly / monthly / raw cron) plus an illustrative internal function they fire, discovered by codegen and synced into wrangler.jsonc.",
            "name": "crons",
            "title": "Crons"
        },
        {
            "description": "Transactional email for Lunora — a sendEmail action (plus a fire-and-forget queueEmail) built on @lunora/mail's Cloudflare Email Workers transport, with CR/LF + header-injection-safe address handling and a dev mail catcher that surfaces every send in the studio.",
            "name": "mail",
            "title": "Mail"
        },
        {
            "description": "Provider-agnostic payments for Lunora — Stripe-first checkout, metered usage tracking, entitlement checks, billing portal, and subscription queries built on @lunora/payment. Scaffolds the checkout / track / check / portal / subscriptions / webhook functions. The provider adapter is wired into createShardDO (see docs).",
            "name": "payment",
            "title": "Payment"
        },
        {
            "description": "Collaborative-awareness presence (who's here + cursors) — a TTL'd room/session table wired into your Lunora schema, with reactive listPresent and a heartbeat upsert.",
            "name": "presence",
            "title": "Presence"
        },
        {
            "description": "Durable, ORM-backed rate limiting (token-bucket / fixed-window / sliding-window) wired into your Lunora schema and procedures.",
            "name": "ratelimit",
            "title": "Rate limit"
        },
        {
            "description": "R2-backed file storage for Lunora — direct-upload + worker-signed-URL helpers built on @lunora/storage over an R2 bucket binding, exposed as actions/queries so codegen surfaces them as api.storage.*.",
            "name": "storage",
            "title": "Storage"
        }
    ]
}
