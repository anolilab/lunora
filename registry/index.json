{
    "$schema": "./schema/registry.schema.json",
    "items": [
        {
            "description": "Cookie-session authentication for Cirrus — email/password sign-up/sign-in on top of @cirrus/auth (a thin wrapper over better-auth), with the auth instance + request handler wired into your project and verification / forgot-password mail routed through @cirrus/mail (captured into the studio Mail tab in dev).",
            "name": "auth",
            "title": "Auth"
        },
        {
            "description": "Sign in with Auth0 for Cirrus auth. Wires Auth0 as an OIDC provider via better-auth's genericOAuth plugin, on top of the base `auth` item.",
            "name": "auth-auth0",
            "title": "Auth — Auth0"
        },
        {
            "description": "Sign in with Clerk for Cirrus auth. Wires Clerk as an OIDC provider via better-auth's genericOAuth plugin, on top of the base `auth` item.",
            "name": "auth-clerk",
            "title": "Auth — Clerk"
        },
        {
            "description": "Passwordless magic-link sign-in for Cirrus auth. Wires better-auth's magicLink plugin on top of the base `auth` item and delivers the sign-in link through @cirrus/mail (captured into the studio Mail tab in dev).",
            "name": "auth-magic-link",
            "title": "Auth — Magic link"
        },
        {
            "description": "Passwordless email one-time-password (OTP) sign-in and verification for Cirrus auth. Wires better-auth's emailOTP plugin on top of the base `auth` item and delivers the code through @cirrus/mail (captured into the studio Mail tab in dev).",
            "name": "auth-otp",
            "title": "Auth — Email OTP"
        },
        {
            "description": "Managed point-in-time recovery — a cron-driven `snapshot` internalAction exports a configured set of tables to timestamped NDJSON in a dedicated R2 bucket, and a cron-driven `prune` internalAction ages snapshots out by retention window. Pairs with `cirrus backup restore` (nearest snapshot + `--to` CDC replay).",
            "name": "backup",
            "title": "Backup"
        },
        {
            "description": "Code-first cron starter for Cirrus — a cirrus/crons.ts that declares recurring jobs with cronJobs() (interval / daily / weekly / monthly / raw cron) plus an illustrative internal function they fire, discovered by codegen and synced into wrangler.jsonc.",
            "name": "crons",
            "title": "Crons"
        },
        {
            "description": "Transactional email for Cirrus — a sendEmail action (plus a fire-and-forget queueEmail) built on @cirrus/mail's Cloudflare Email Workers transport, with CR/LF + header-injection-safe address handling and a dev mail catcher that surfaces every send in the studio.",
            "name": "mail",
            "title": "Mail"
        },
        {
            "description": "Collaborative-awareness presence (who's here + cursors) — a TTL'd room/session table wired into your Cirrus schema, with reactive listPresent and a heartbeat upsert.",
            "name": "presence",
            "title": "Presence"
        },
        {
            "description": "Durable, ORM-backed rate limiting (token-bucket / fixed-window / sliding-window) wired into your Cirrus schema and procedures.",
            "name": "ratelimit",
            "title": "Rate limit"
        },
        {
            "description": "R2-backed file storage for Cirrus — direct-upload + worker-signed-URL helpers built on @cirrus/storage over an R2 bucket binding, exposed as actions/queries so codegen surfaces them as api.storage.*.",
            "name": "storage",
            "title": "Storage"
        }
    ]
}
