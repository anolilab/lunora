{
    "$schema": "../schema/registry-item.schema.json",
    "name": "backup",
    "title": "Backup",
    "description": "Managed point-in-time recovery — a cron-driven `snapshot` internalAction exports a configured set of tables to timestamped NDJSON in a dedicated R2 bucket, and a cron-driven `prune` internalAction ages snapshots out by retention window. Pairs with `cirrus backup restore` (nearest snapshot + `--to` CDC replay).",
    "docs": "Register the snapshot+prune cron pair in cirrus/crons.ts: `crons.daily(\"backup snapshot\", { hourUTC: 3, minuteUTC: 0 }, internal.backup.snapshot, {})` and `crons.daily(\"backup prune\", { hourUTC: 4, minuteUTC: 0 }, internal.backup.prune, { keepDays: 30, keepLast: 5 })`, then `cirrus codegen`. Edit the TABLES list in cirrus/backup/index.ts to cover every table you want backed up (codegen cannot enumerate tables for you). prune retention: keepDays and/or keepLast — a snapshot is deleted only if it's both older than keepDays and not among the keepLast newest. Add the BACKUP_BUCKET r2_buckets binding (added to wrangler.jsonc on install). Restore with `cirrus backup restore <id|file>` (add `--to <ISO>` for fine-grained CDC replay).",
    "requires": [],
    "deps": {
        "@cirrus/server": "workspace:*",
        "@cirrus/storage": "workspace:*"
    },
    "bindings": [
        {
            "path": ["r2_buckets"],
            "value": [
                {
                    "binding": "BACKUP_BUCKET",
                    "bucket_name": "cirrus-backups"
                }
            ]
        }
    ],
    "files": [{ "from": "backup.ts", "to": "cirrus/backup/index.ts", "merge": "create-or-skip" }]
}
