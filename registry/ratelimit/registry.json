{
    "$schema": "../schema/registry-item.schema.json",
    "name": "ratelimit",
    "title": "Rate limit",
    "description": "Durable, ORM-backed rate limiting (token-bucket / fixed-window / sliding-window) wired into your Lunora schema and procedures.",
    "docs": "Attach the middleware with c.mutation.use(ratelimit.middleware), or call api.ratelimit.consume directly. Configure named limits in lunora/ratelimit/schema.ts.",
    "requires": [],
    "deps": {
        "@lunora/ratelimit": "workspace:*",
        "@lunora/server": "workspace:*"
    },
    "files": [
        { "from": "schema.ts", "to": "lunora/ratelimit/schema.ts", "merge": "schema-extension" },
        { "from": "ratelimit.ts", "to": "lunora/ratelimit/index.ts", "merge": "create-or-skip" }
    ]
}
