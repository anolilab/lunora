"name": "Dependency review"
"on":
    "pull_request":
        "branches": ["main", "alpha", "next", "beta", "fix/**"]
        # The review diffs the dependency graph — only manifests, lockfiles,
        # vendored patches, action pins, and the python SDK can change it.
        "paths":
            - "**/package.json"
            - "pnpm-lock.yaml"
            - "pnpm-workspace.yaml"
            - "patches/**"
            - ".github/workflows/**"
            - "sdks/**"
"permissions": {}
"jobs":
    "dependency-review":
        "permissions":
            "contents": "read"
            "pull-requests": "write"
        "uses": "anolilab/workflows/.github/workflows/dependency-review.yml@045a2d464dad0661a570c8a3c19f4b32bf9efad9" # v15.3.0+
