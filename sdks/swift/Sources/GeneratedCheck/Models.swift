// GENERATED by `lunora sdk generate --lang swift` — do not edit.
// Run the command again to regenerate.

// This file was generated from JSON Schema using quicktype, do not modify it directly.
// To parse the JSON, add this file to your project and do:
//
//   let messagesListArgs = try MessagesListArgs(json)
//   let messagesSendArgs = try MessagesSendArgs(json)

import Foundation

// MARK: - MessagesListArgs
public struct MessagesListArgs: Codable {
    /// Id<"channels">
    public let channelID: String
    public let limit: Double?

    public enum CodingKeys: String, CodingKey {
        case channelID = "channelId"
        case limit = "limit"
    }

    public init(channelID: String, limit: Double?) {
        self.channelID = channelID
        self.limit = limit
    }
}

// MARK: MessagesListArgs convenience initializers and mutators

public extension MessagesListArgs {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(MessagesListArgs.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        channelID: String? = nil,
        limit: Double?? = nil
    ) -> MessagesListArgs {
        return MessagesListArgs(
            channelID: channelID ?? self.channelID,
            limit: limit ?? self.limit
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

// MARK: - MessagesSendArgs
public struct MessagesSendArgs: Codable {
    /// Id<"channels">
    public let channelID: String
    public let kind: Kind
    public let tags: [String: String]
    public let text: String

    public enum CodingKeys: String, CodingKey {
        case channelID = "channelId"
        case kind = "kind"
        case tags = "tags"
        case text = "text"
    }

    public init(channelID: String, kind: Kind, tags: [String: String], text: String) {
        self.channelID = channelID
        self.kind = kind
        self.tags = tags
        self.text = text
    }
}

// MARK: MessagesSendArgs convenience initializers and mutators

public extension MessagesSendArgs {
    init(data: Data) throws {
        self = try newJSONDecoder().decode(MessagesSendArgs.self, from: data)
    }

    init(_ json: String, using encoding: String.Encoding = .utf8) throws {
        guard let data = json.data(using: encoding) else {
            throw NSError(domain: "JSONDecoding", code: 0, userInfo: nil)
        }
        try self.init(data: data)
    }

    init(fromURL url: URL) throws {
        try self.init(data: try Data(contentsOf: url))
    }

    func with(
        channelID: String? = nil,
        kind: Kind? = nil,
        tags: [String: String]? = nil,
        text: String? = nil
    ) -> MessagesSendArgs {
        return MessagesSendArgs(
            channelID: channelID ?? self.channelID,
            kind: kind ?? self.kind,
            tags: tags ?? self.tags,
            text: text ?? self.text
        )
    }

    func jsonData() throws -> Data {
        return try newJSONEncoder().encode(self)
    }

    func jsonString(encoding: String.Encoding = .utf8) throws -> String? {
        return String(data: try self.jsonData(), encoding: encoding)
    }
}

public enum Kind: String, Codable {
    case image = "image"
    case text = "text"
}

// MARK: - Helper functions for creating encoders and decoders

func newJSONDecoder() -> JSONDecoder {
    let decoder = JSONDecoder()
    if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
        decoder.dateDecodingStrategy = .iso8601
    }
    return decoder
}

func newJSONEncoder() -> JSONEncoder {
    let encoder = JSONEncoder()
    if #available(iOS 10.0, OSX 10.12, tvOS 10.0, watchOS 3.0, *) {
        encoder.dateEncodingStrategy = .iso8601
    }
    return encoder
}

