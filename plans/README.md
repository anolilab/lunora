# Implementation Plans

Generated by the `improve` skill on 2026-06-14. Plans 001–018 were planned
against commit `151a3eca`; plans 019–021 against `dda03c28` (a follow-up audit
weighted toward the just-landed Studio schema-diagram + menu-reorg work). Each
plan is self-contained: read it fully, run its verification gates, honor its
STOP conditions, and update your row below when done.

Scope of the audit: all 30 `packages/*` (audited per-package) plus the CLI agent
skills in `packages/cli/skills/`. Findings were vetted against the live code
before planning; see "Findings considered and rejected" for what was dropped and
why.

## Execution order & status

Plans are independent unless "Depends on" says otherwise — they touch different
packages and can be executed in parallel. Recommended priority order is P1 → P2
→ P3.

| Plan | Title                                                     | Pkg                     | Pri   | Effort | Depends on      | Status                                                                                                                                                                                                                                                                                  |
| ---- | --------------------------------------------------------- | ----------------------- | ----- | ------ | --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | `useSubscription` clears value on `"skip"`                | react                   | P1    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 002  | Anchor `.dev.vars` placeholder markers (stop secret loss) | config                  | P1    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 003  | `CirrusClient.close()` releases listener registries       | client                  | P2    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 004  | Test harness mirrors internal-visibility gate             | testing                 | P2    | M      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 005  | Atomic `.dev.vars` write (temp + rename)                  | config                  | P2    | M      | 002 (same file) | DONE                                                                                                                                                                                                                                                                                    |
| 006  | Reject over-refunds & negative usage                      | payment                 | P2    | M      | —               | DONE (Step 1 only; Step 2 dropped — downward set is by-design)                                                                                                                                                                                                                          |
| 007  | Studio `fireAndForget` error channel                      | studio                  | P2    | M      | —               | DONE (Step 2 n/a: no swallowing critical sites)                                                                                                                                                                                                                                         |
| 008  | Vector sync namespace guardrail (warn)                    | vectors                 | P3    | M      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 009  | Runtime authorization denial-path tests                   | runtime                 | P2    | M      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 010  | Guard rank pagination vs empty `sortBy`                   | d1                      | P3    | M      | —               | DONE (Step 0: empty sortBy already rejected by schema builder — belt-and-suspenders guard + test)                                                                                                                                                                                       |
| 012  | Advisor Set lookups (drop O(n) includes)                  | advisor                 | P3    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 013  | `subscribeToQuery` warns off-scope                        | vue                     | P3    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 014  | Container surfaces partial error envelope                 | container               | P3    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 015  | Vite codegen cancels debounce on close                    | vite                    | P3    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 016  | Doc: build before filtered per-package tests              | repo/docs               | P3    | S      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 017  | Skills route to capabilities; shardBy vs global           | cli/skills              | P3    | S      | 018 (soft)      | DONE                                                                                                                                                                                                                                                                                    |
| 018  | Add capability skills (mail/storage/scheduler)            | cli/skills              | P3    | L      | —               | DONE                                                                                                                                                                                                                                                                                    |
| 019  | Focused unit tests for codegen `buildTableColumns`        | codegen                 | P3    | S      | —               | DONE (merged in `2c403598` on `feat/studio-schema-diagram`)                                                                                                                                                                                                                             |
| 020  | Batch `describeTables` admin RPC for the diagram          | do, studio              | P3    | M      | —               | DONE (merged in `2c403598` on `feat/studio-schema-diagram`)                                                                                                                                                                                                                             |
| 021  | Surface a column-load error in the schema diagram         | studio                  | P3    | S      | 020 (soft)      | DONE (merged in `2c403598`; rebased onto 020's batched probe — `described[0]` rejection → flag)                                                                                                                                                                                         |
| 022  | Borrow from localflare + d1-manager into Studio (7 items) | studio, do, advisor     | P0–P3 | L      | —               | IN PROGRESS (all 7 items built+verified on 4 branches off `2c403598`; awaiting merge into `feat/studio-schema-diagram`)                                                                                                                                                                 |
| 023  | Dynamic data masking (`mask()` + advisor lint + studio)   | server, advisor, studio | P3    | L      | —               | DONE (item 1 `mask()` middleware @cirrus/server 19 tests; item 2 `mask_uncovered_pii_column` lint + codegen feeder 9+6 tests; item 3 studio data-browser mask toggle + header chips — full codegen→DO→studio metadata seam, FNV-1a/redact/custom client preview fail-closed, 5+4 tests) |

> Plan number 011 was intentionally not issued — see rejected findings.

Plans 019–021 are the follow-up batch (baseline `dda03c28`) covering the new
Studio schema-diagram pipeline. None block the others, but **020 should land
before 021** — both edit `schema-viewer.tsx`'s `probeShardSchema`, and 021 is
written for the post-020 batched probe (with a pre-020 fallback noted inline).
019 is independent test-only work.

Plan 022 (baseline `2c403598`) is a different kind of plan: not a bug-fix from the
audit but a **feature/borrow roadmap** derived from `ECOSYSTEM-BORROW.md` — seven
independent Studio items lifted from the MIT-licensed localflare and d1-manager
projects. Execute it item-by-item (each is its own PR); it does not block, and is
not blocked by, the audit plans.

Plan 023 (baseline `2c403598`) is the same kind of feature/borrow note, derived
from the StarbaseDB analysis: its substrate twin (DO + SQLite) had exactly one
idea Cirrus lacks — **dynamic data masking**. StarbaseDB is **AGPL-3.0 → ideas
only** (re-implement against Cirrus's own `rls()`, copy nothing). Three
independent items (`mask()` policy → advisor lint → studio toggle); ship
per-item. Does not block, and is not blocked by, the audit plans.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED.

## Dependency notes

- **005 after 002**: both edit `packages/config/src/scaffold-dev-variables.ts`.
  Landing 002 first avoids a trivial merge conflict; they are otherwise
  independent.
- **017 ↔ 018**: 017 adds router links to capability skills; if 018 (which
  creates those skills) lands first, 017 names them, otherwise 017 routes to
  `cirrus registry list`. Either order works.
- **020 → 021**: both edit `probeShardSchema` in `schema-viewer.tsx`. 020 batches
  the per-table `describeTable` calls into one `describeTables`; 021 adds a
  per-shard "columns failed to load" signal. With 020 in place a columns-read
  failure is per-shard (one batched call), which is exactly the granularity 021
  models — so land 020 first. If 021 is done first, follow its pre-020 fallback
  (per-table rejection → shard-wide boolean) and re-verify after 020.

## Notes for executors

- `dist/` is gitignored and built on demand. Before running a package's
  `test`/`lint:types`, build dependencies once: `pnpm run build:packages` (or use
  `pnpm run test:affected` / `pnpm run lint:affected:types`). Skipping this
  produces misleading `X is not a function` / "missing export" errors. (This is
  itself the subject of plan 016.)
- The repo uses ESM with `moduleResolution: "bundler"` — **no `.js` extensions**
  in relative imports (the sole exception is `@cirrus/codegen`'s emitted output).
- Match each package's existing test style; do not introduce new test
  infrastructure.

## Findings considered and rejected

These surfaced during the audit but were dropped after vetting — recorded so they
aren't re-audited:

- **CLI/Vite "broken build, 19 failing tests" (missing `@cirrus/config`
  exports)** — FALSE POSITIVE. The audit ran filtered tests against a stale,
  gitignored `dist/`. The `detectAgentRules`/`AGENT_RULES_HINT` exports and
  `StudioHtmlConfig.rulesInstalled` all exist correctly in `packages/config/src`
  (added in HEAD `151a3eca`). Real residual captured as plan 016 (DX docs).
- **"Internal functions not enforced server-side" (server/runtime/mcp)** —
  REFUTED. The generated `handleRpc` enforces it at
  `packages/codegen/src/emit.ts:1745`
  (`visibility === "internal" && !isSystemDispatch()` → not found). The genuine
  residual (the test harness diverges) is captured as plan 004.
- **`do` subMemo cleanup "leak" on serialize failure
  (`shard-do.ts:2406-2430`)** — REJECTED. On serialize failure the code rolls
  back the in-memory unsubscribe and keeps the memo; sub-state and memo-state
  stay consistent. A "fix" that deletes the memo on rollback would _introduce_
  desync. Only a pathological repeated-serialize-failure scenario differs, not
  worth the risk. (This is why no plan 011 exists.)
- **Svelte `mutation()` recreates stores per call** — REJECTED. Creating fresh
  state per call matches React/Solid/Vue mutation hooks; `mutation()` is meant to
  be called once at component init. Calling it reactively is misuse, not a bug.
- **Mail: reject commas in Subject/header values** — REJECTED. Commas are valid
  and common in subject lines; rejecting them would break legitimate email.
  Address-field comma rejection (commas separate addresses) is correctly distinct
  and stays.
- **Weak/empty payment webhook secret = "complete bypass"** — DOWNGRADED, not
  planned. An empty/weak secret makes HMAC verification _fail_ all webhooks (the
  developer's own break), not an attacker bypass. A min-length assertion is a
  minor DX nicety; left out to keep the payment plan focused on the real ledger
  bugs (plan 006).
- **`?token=` in the WebSocket URL (do/runtime)** — BY-DESIGN, not planned. WS
  upgrades can't set `Authorization`; the code already documents this and
  recommends short-lived tokens. A larger ephemeral-WS-token design is a possible
  future hardening, not a bug fix.
- **"cursor race in bounded fan-out" (runtime/vectors)** — REJECTED. Benign on
  the single-threaded JS event loop (the reporting agents noted this themselves).

### Follow-up audit (baseline `dda03c28`, the Studio schema-diagram + menu reorg)

- **`probeShardSchema` stale-closure across shard switches (studio)** — REJECTED.
  The probe keys its results by the captured `shard` arg and writes
  `setShardColumns({ ...previous, [shard]: … })`, so a late-resolving probe for a
  previous shard updates that shard's bucket, not the active one; the render reads
  `shardColumns[shardKey]`. No stale write reaches the visible diagram.
- **`describeTable` `JSON.stringify` table-name injection (codegen/do)** —
  REJECTED. Table names are validated by `assertIdentifier` in
  `packages/codegen/src/emit.ts` before emission, and the DO dispatch keys on a
  plain `args["table"]` string used only as an object key, never interpolated into
  SQL. No injection surface.
- **Schema-diagram empty-domain nav showing blank groups (studio)** — REJECTED.
  `visibleGroups` (studio.tsx) filters tabs by `isTabVisible` and then drops
  groups with no visible tabs, so a fully-gated domain never renders an empty
  rail entry.
- **Subscription dedup "missing" in `shard-do.ts` (do)** — REJECTED (by-design).
  The deliberately-not-deduped comment at shard-do.ts:4101–4142 documents the
  trade-off; adding dedup risks correctness for a micro-optimization. Left as-is.
- **`v.id("ref")` unknown-target not validated at the diagram (studio)** —
  REJECTED. Unknown FK targets are already covered by `@cirrus/advisor`'s
  schema lints (the studio surfaces them in the Advisors table); duplicating the
  check in the diagram adds no signal.
